---
phase: 260425-g08
plan: 01
status: complete
subsystem: security
tags: [security, auth, authorization, input-validation, schema, env-validation]
dependency_graph:
  requires: []
  provides: [authorized-server-actions, sanitized-gcal-errors, validated-realtime-payload, unique-schedule-entries, startup-env-validation, script-safety-guards]
  affects: [src/actions/schedule.ts, src/auth.config.ts, src/lib/gcal/client.ts, src/components/schedule/realtime-provider.tsx, src/db/schema/domain.ts, src/lib/schedule/queries.ts, src/env.ts, src/auth.ts, src/db/reset.ts, src/db/clear-tokens.ts]
tech_stack:
  added: []
  patterns: [email-allowlist-authorization, opaque-error-messages, realtime-payload-validation, unique-db-index, startup-env-validation, destructive-script-gating]
key_files:
  created: []
  modified:
    - src/auth.config.ts
    - src/actions/schedule.ts
    - src/lib/gcal/client.ts
    - src/components/schedule/realtime-provider.tsx
    - src/db/schema/domain.ts
    - src/lib/schedule/queries.ts
    - src/env.ts
    - src/auth.ts
    - src/db/reset.ts
    - src/db/clear-tokens.ts
decisions:
  - "Authorization uses config.parents[].email allowlist check — any Google-authenticated user not in that list is rejected with Forbidden"
  - "GCal error messages use opaque strings client-side; full context logged server-side via console.error"
  - "Realtime payload validation drops (returns early) on any invalid field rather than using safe fallbacks"
  - "schema_entries_child_day_unique uniqueIndex applied and pushed to Supabase via drizzle-kit push"
  - "AUTH_SECRET included in env var validation list even though it is read internally by Auth.js, not via process.env directly"
  - "CR-01 + LO-05: Option A selected — git filter-repo --path src/config/app.ts --invert-paths will be run by the orchestrator after plan completion to scrub the committed email and calendar IDs from git history"
  - "LO-04: RLS is already enabled on all Supabase tables. Policy strictness is unconfirmed — a follow-up audit of the specific policies is tracked as a future action"
metrics:
  duration: "~20 minutes"
  completed: "2026-04-25"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 10
---

# Phase 260425-g08 Plan 01: Address Security Review Findings — Summary

Address all actionable code-side findings from SECURITY-REVIEW.md: hardened Server Action authorization, sanitized error messages, validated Realtime payloads, unique DB constraint preventing race duplicates, startup env-var validation, and destructive script gating behind --yes.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Critical and High findings — auth, authorization, error sanitization | 6766566 | src/auth.config.ts, src/actions/schedule.ts, src/lib/gcal/client.ts |
| 2 | Medium and Low findings — input validation, unique constraint, env checks, script gating | 60e9da3 | src/components/schedule/realtime-provider.tsx, src/db/schema/domain.ts, src/lib/schedule/queries.ts, src/env.ts, src/auth.ts, src/db/reset.ts, src/db/clear-tokens.ts |

## Findings Disposition Table

| ID | Severity | Title | Resolution |
|----|----------|-------|------------|
| CR-01 | CRITICAL | Real email and calendar IDs committed to git | **decided** — Option A (git filter-repo) selected; orchestrator will run `git filter-repo --path src/config/app.ts --invert-paths` separately after plan completion |
| CR-02 | CRITICAL | No authorization check in schedule mutation actions | **fixed-in-code** — requireAuthorizedParent() added to toggleCell, saveNotes, publishSchedule (commit 6766566) |
| CR-03 | CRITICAL | `allowDangerousEmailAccountLinking` enables account takeover | **fixed-in-code** — flag removed from src/auth.config.ts (commit 6766566) |
| HI-01 | HIGH | `syncCalendars` has no authentication check | **fixed-in-code** — requireAuthorizedParent() added as first call in syncCalendars (commit 6766566) |
| HI-02 | HIGH | Token exchange errors leak email and OAuth details to client | **fixed-in-code** — full error logged server-side, opaque message thrown to client (commit 6766566) |
| HI-03 | HIGH | `publishSchedule` has no owner/authorization check | **fixed-in-code** — requireAuthorizedParent() added to publishSchedule (commit 6766566) |
| ME-01 | MEDIUM | Realtime payload cast without runtime validation | **fixed-in-code** — VALID_PARENT_IDS and VALID_STATUSES allowlists; early return on invalid fields (commit 60e9da3) |
| ME-02 | MEDIUM | `newParentId` not validated server-side in `toggleCell` | **fixed-in-code** — VALID_PARENT_IDS check in toggleCell (commit 6766566) |
| ME-03 | MEDIUM | Notes field has no length limit | **fixed-in-code** — 500-char limit enforced in saveNotes (commit 6766566) |
| ME-04 | MEDIUM | Race condition in schedule auto-seeding, no unique constraint | **fixed-in-code** — uniqueIndex on (child_id, day) + onConflictDoNothing(); index pushed to Supabase (commit 60e9da3) |
| ME-05 | MEDIUM | Parent email leaked in error messages | **fixed-in-code** — email removed from all thrown errors in gcal/client.ts (commit 6766566) |
| LO-01 | LOW | Required env vars not validated at startup | **fixed-in-code** — src/env.ts now throws at import time for missing vars (commit 60e9da3) |
| LO-02 | LOW | Token refresh response not validated before use | **fixed-in-code** — response.ok checked before .json(), shape validated before use (commit 60e9da3) |
| LO-03 | LOW | Destructive DB scripts have no confirmation guard | **fixed-in-code** — both reset.ts and clear-tokens.ts require --yes flag (commit 60e9da3) |
| LO-04 | LOW | Supabase RLS posture not confirmed | **decided** — RLS confirmed enabled on all tables; specific policy strictness unconfirmed; follow-up audit of individual policies tracked as future action |
| LO-05 | LOW | Calendar IDs in git history compound CR-01 | **decided** — resolved together with CR-01 via Option A (git filter-repo) |

## Deviations from Plan

None — plan executed exactly as written. The pre-existing TypeScript errors (Cannot find module '@/config/app') are due to src/config/app.ts being gitignored and are unrelated to these changes.

## Task 3 — Checkpoint: Operator Decisions (COMPLETE)

Task 3 was a `checkpoint:human-action`. All required operator decisions have been received.

### CR-01 + LO-05: Git history scrub for src/config/app.ts — DECIDED

**Decision: Option A — git filter-repo.**
`git filter-repo --path src/config/app.ts --invert-paths` will be run by the orchestrator separately after plan completion. This rewrites history to remove the committed Gmail address and Google Calendar IDs. src/config/app.ts is already gitignored so no future commits will re-expose the data.

### LO-04: Supabase RLS audit — DECIDED

**Decision: RLS is already enabled on all Supabase tables.** The strictness of the specific policies is unconfirmed. A follow-up audit of the individual policy definitions is tracked as a future action to verify that policies are sufficiently restrictive (e.g., not allowing anon reads on tables that should only be accessible via server-side service-role key).

### Operational todos (from STATE.md)

- `APP_MOTHER_EMAIL` placeholder — outstanding; to be resolved before sharing with second parent.
- Supabase Pro upgrade timing — outstanding; must be done before handing the app to real users.

## Known Stubs

None.

## Threat Flags

None — all surfaces addressed are in the plan's threat model.

## Self-Check: PASSED

- src/auth.config.ts: FOUND, allowDangerousEmailAccountLinking removed
- src/actions/schedule.ts: FOUND, requireAuthorizedParent at 4 call sites
- src/lib/gcal/client.ts: FOUND, sanitized error messages
- src/components/schedule/realtime-provider.tsx: FOUND, VALID_PARENT_IDS validation
- src/db/schema/domain.ts: FOUND, schedule_entries_child_day_unique uniqueIndex
- src/lib/schedule/queries.ts: FOUND, onConflictDoNothing()
- src/env.ts: FOUND, Missing required env var validation
- src/auth.ts: FOUND, Unexpected token response shape check
- src/db/reset.ts: FOUND, --yes guard
- src/db/clear-tokens.ts: FOUND, --yes guard
- Schema pushed to Supabase: confirmed via drizzle-kit push output "[✓] Changes applied"
- Commits 6766566 and 60e9da3: verified in git log
