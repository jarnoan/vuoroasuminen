---
phase: 09
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01-PLAN.md
  - 09-02-PLAN.md
  - 09-03-PLAN.md
files_modified:
  - .planning/phases/09-row-level-security/09-VERIFICATION.md
autonomous: false
requirements:
  - RLS-01
  - RLS-02
  - RLS-03
  - RLS-04
tags:
  - rls
  - verification
  - human-action
  - blocking
must_haves:
  truths:
    - "supabase/policies.sql has been executed successfully in the live Supabase project (no errors reported by the SQL editor)"
    - "Supabase Dashboard shows the RLS-enabled badge on all five tables (children, schedules, schedule_entries, gcal_events, user_google_tokens)"
    - "An unauthenticated PostgREST GET against any domain table returns zero rows"
    - "An authenticated signed-in parent can read and write all rows on domain tables (verified via the running app)"
    - "Each parent can read/write only their own row in user_google_tokens (verified by attempting cross-read)"
    - "Realtime updates propagate between two simultaneous browser sessions after RLS is enabled"
    - "GCal sync still works after RLS is enabled (admin Drizzle / service_role bypasses RLS as designed)"
  artifacts:
    - path: ".planning/phases/09-row-level-security/09-VERIFICATION.md"
      provides: "Recorded outcome of the manual SQL run + the four UAT scenarios for RLS-01..RLS-04"
      contains: "RLS-01"
  key_links:
    - from: "supabase/policies.sql"
      to: "Supabase project database"
      via: "Manual SQL editor paste-and-run"
      pattern: "Dashboard → SQL editor"
---

<objective>
Apply `supabase/policies.sql` to the live Supabase project and verify that all four phase success criteria hold end-to-end. This plan is BLOCKING — it gates Phase 10 (Auth.js Removal) the same way Phase 8's GATE plan gated Phase 9. No code is modified; the only artifact produced is the `09-VERIFICATION.md` record of the four UAT outcomes.

Purpose: Closes RLS-01, RLS-02, RLS-03, RLS-04. The SQL must be run by a human (D-01 — no Supabase CLI migration system) and the verification must be observed against a real Supabase session. Without this plan, plans 01–03 have shipped only "RLS-ready" code; RLS itself is not yet on.

Output:
- Live Supabase project has RLS enabled on all 5 tables with the 19 policies from `supabase/policies.sql`
- `schedule_entries` is in the `supabase_realtime` publication
- `.planning/phases/09-row-level-security/09-VERIFICATION.md` records the result of each of the four RLS-NN scenarios (PASS / FAIL with notes)
- The phase is verifiably complete; Phase 10 can begin
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-row-level-security/09-CONTEXT.md
@.planning/phases/09-row-level-security/09-RESEARCH.md
@.planning/phases/09-row-level-security/09-01-PLAN.md
@.planning/phases/09-row-level-security/09-02-PLAN.md
@.planning/phases/09-row-level-security/09-03-PLAN.md

<interfaces>
Supabase project URL: read from `.env.local` or Supabase Dashboard (`NEXT_PUBLIC_SUPABASE_URL`).
Anon key (public, safe in browser): `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

PostgREST anon GET test contract:
```
GET https://<project-ref>.supabase.co/rest/v1/children
  apikey: <NEXT_PUBLIC_SUPABASE_ANON_KEY>
  Authorization: Bearer <NEXT_PUBLIC_SUPABASE_ANON_KEY>

Expected after RLS: HTTP 200 with body `[]` (zero rows — policy blocks anon).
Expected before RLS: HTTP 200 with non-empty array.
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human runs supabase/policies.sql in the Supabase Dashboard SQL editor</name>
  <files>supabase/policies.sql</files>
  <read_first>
    - supabase/policies.sql (the script the user is about to run)
    - .planning/phases/09-row-level-security/09-03-PLAN.md (for context on what plan 03 produced)
  </read_first>
  <action>
    PAUSE FOR HUMAN. The agent does not write code in this task. The human must apply `supabase/policies.sql` to the live Supabase database via the Dashboard SQL editor (Decision D-01 locked the delivery method to manual; there is no agent-runnable alternative).

    The agent's role in this task:
    1. Confirm `supabase/policies.sql` exists locally (`test -f supabase/policies.sql` exits 0).
    2. Print the absolute path to the file and ask the human to open it in the Supabase Dashboard SQL editor.
    3. Wait for the human's reply (resume-signal below).
    4. On reply, parse the human's output of the policy_count query and the publication query.
    5. If the human reports `Applied` and the policy counts match the expected 4/4/4/4/3 distribution and `schedule_entries` is in the publication, proceed to Task 2.
    6. If the human reports `Failed`, capture the error text from their reply, write it to `.planning/phases/09-row-level-security/09-VERIFICATION.md` under a `## Pre-flight failure` section, and halt the plan. The orchestrator will route to `/gsd-plan-phase --gaps` if needed.

    Concrete dashboard procedure for the human (paste this exact instruction block back to them):

    1. Open https://supabase.com/dashboard and select the vuoroasuminen project.
    2. Open the SQL editor (left sidebar → SQL → "New query").
    3. Run `cat /Users/jarno/src/vuoroasuminen/supabase/policies.sql` in your terminal, copy the entire output, and paste it into the SQL editor.
    4. Click Run (or Cmd+Return / Ctrl+Enter).
    5. Expect: "Success. No rows returned." and zero errors. 5 × `ALTER TABLE ENABLE ROW LEVEL SECURITY`, 19 × `CREATE POLICY`, 1 × `ALTER PUBLICATION` executed.
    6. Known-benign errors:
       - `relation "public.schedule_entries" is already member of publication "supabase_realtime"` — publication membership already correct; harmless.
       - `policy "..." already exists` — only happens if a prior run was incomplete. Drop the offending policy via `DROP POLICY "<name>" ON public.<table>;` and re-run.
    7. Known-bad errors:
       - `function auth.email() does not exist` — wrong file content. Confirm the script you pasted uses `(auth.jwt() ->> 'email')`, not `auth.email()`. Re-cat the file and re-paste.
    8. Run the verification queries listed in `<how-to-verify>` and paste their results back here.
  </action>
  <what-built>
    Plans 01–03 of this phase have produced:
    - A cookie-aware browser Supabase client (RLS-04 client side)
    - A user-authenticated token upsert in the OAuth callback (RLS-03 write path)
    - The canonical `supabase/policies.sql` script with all RLS DDL (RLS-01, RLS-02, RLS-03, RLS-04 database side)

    The SQL has not yet been applied to the live Supabase database. Until it is applied, RLS is OFF and the four success criteria cannot be verified.

    This step has no CLI/API alternative the agent can run unattended — D-01 locked the delivery method as manual via the Dashboard SQL editor (or `psql` if the user prefers).
  </what-built>
  <how-to-verify>
    Human runs the SQL and reports back. Verification queries the human must run AFTER the script and paste back:

    Visual confirmation in Dashboard → Table Editor — green shield icon / "RLS enabled" badge on each of:
    - `children`
    - `schedules`
    - `schedule_entries`
    - `gcal_events`
    - `user_google_tokens`

    Policy count query (SQL editor):
    ```sql
    SELECT schemaname, tablename, COUNT(*) AS policy_count
    FROM pg_policies
    WHERE schemaname = 'public'
      AND tablename IN ('children', 'schedules', 'schedule_entries', 'gcal_events', 'user_google_tokens')
    GROUP BY schemaname, tablename
    ORDER BY tablename;
    ```
    Expected:
    | tablename | policy_count |
    |-----------|-------------|
    | children | 4 |
    | gcal_events | 4 |
    | schedule_entries | 4 |
    | schedules | 4 |
    | user_google_tokens | 3 |

    Publication query:
    ```sql
    SELECT pubname, tablename
    FROM pg_publication_tables
    WHERE pubname = 'supabase_realtime'
    ORDER BY tablename;
    ```
    Expected: `schedule_entries` appears in the result set.
  </how-to-verify>
  <verify>
    <automated>test -f supabase/policies.sql</automated>
    Manual: human confirms the SQL ran cleanly and pastes the two verification query results. Agent visually inspects the pasted counts: expects exactly `children=4, gcal_events=4, schedule_entries=4, schedules=4, user_google_tokens=3` and `schedule_entries` in the publication.
  </verify>
  <done>
    Human reply `Applied` received; policy counts match 4/4/4/4/3; `schedule_entries` is in `supabase_realtime` publication. The five tables show "RLS enabled" in the Dashboard.
  </done>
  <resume-signal>Reply `Applied` and paste the policy_count + publication query results, OR reply `Failed: <error>` with the SQL editor error message.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — Verify RLS-01..RLS-04 end-to-end against the running app</name>
  <files>.planning/phases/09-row-level-security/09-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (RLS-01 through RLS-04 wording)
    - .planning/ROADMAP.md (Phase 9 Success Criteria — the four must-be-true statements)
    - .planning/phases/09-row-level-security/09-CONTEXT.md (D-07 and D-10 — service_role still bypasses RLS for server-side reads; this is intentional and DOES NOT count as a failure of RLS-03)
  </read_first>
  <action>
    Two sub-steps:

    **Step A — Agent creates the verification template.** Using the Write tool, create `.planning/phases/09-row-level-security/09-VERIFICATION.md` with EXACTLY the content below (no other text; the agent should fill in `<YYYY-MM-DD>`, `<name>`, `<project-ref>` placeholders with the values supplied by the human at run time):

    ```markdown
    # Phase 9 Verification — Row Level Security

    **Run date:** <YYYY-MM-DD>
    **Tester:** <name>
    **Supabase project ref:** <project-ref>

    ## Pre-flight

    - [ ] `supabase/policies.sql` applied (Task 1) — policy count query returned the expected 4/4/4/4/3 distribution
    - [ ] `schedule_entries` present in `supabase_realtime` publication
    - [ ] `npm run build` exits 0 on `main` after plans 01–02 are merged
    - [ ] Dev server starts: `npm run dev`

    ## RLS-01 — Unauthenticated request returns no data

    **Test:** From a terminal NOT signed in (no session cookie), curl PostgREST anon endpoints.

    Run (replace `<URL>` and `<ANON_KEY>` with the values from `.env.local`):

    ```bash
    curl -s -H "apikey: <ANON_KEY>" -H "Authorization: Bearer <ANON_KEY>" \
      "<URL>/rest/v1/children" | head -c 200
    curl -s -H "apikey: <ANON_KEY>" -H "Authorization: Bearer <ANON_KEY>" \
      "<URL>/rest/v1/schedules" | head -c 200
    curl -s -H "apikey: <ANON_KEY>" -H "Authorization: Bearer <ANON_KEY>" \
      "<URL>/rest/v1/schedule_entries" | head -c 200
    curl -s -H "apikey: <ANON_KEY>" -H "Authorization: Bearer <ANON_KEY>" \
      "<URL>/rest/v1/gcal_events" | head -c 200
    ```

    Expected: each curl returns `[]` (empty JSON array — RLS blocked the anon role).

    Status: [ ] PASS / [ ] FAIL
    Notes:

    ## RLS-02 — Authenticated user can CRUD domain rows

    **Test:** In the running app, sign in as one of the configured parents. Use the schedule UI to:
    1. Edit a cell (changes parent_id) and confirm it persists across page refresh.
    2. Add a new schedule via the "Extend schedule" action (if present in current UI).
    3. Clear a cell (sets parent_id to null) — confirm persistence.

    No errors should appear in the browser console or the server logs. All Server Actions succeed (they use admin Drizzle / service_role internally — bypasses RLS by D-10; this only confirms that nothing in the code path is broken by RLS being on).

    Status: [ ] PASS / [ ] FAIL
    Notes:

    ## RLS-03 — Each user can only access their own user_google_tokens row

    **Test:** Use Supabase Dashboard SQL editor "Impersonate user" (Authentication → Users → select user → "Impersonate" → SQL editor):

    1. Impersonate Parent A → run `SELECT email FROM user_google_tokens;` → expect exactly 1 row, Parent A's email.
    2. Switch impersonation to Parent B → run the same query → expect exactly 1 row, Parent B's email.

    Cross-write attempt:
    3. While impersonating Parent A, run `INSERT INTO user_google_tokens (email, refresh_token, updated_at) VALUES ('<PARENT_B_EMAIL>', 'forged', NOW());` → expect a policy violation error (`new row violates row-level security policy ...`).

    Status: [ ] PASS / [ ] FAIL
    Notes:

    ## RLS-04 — Realtime live updates work across two sessions

    **Test:** With Parent A signed in in window 1 (your default profile) and Parent B signed in in window 2 (incognito or a separate Chrome profile), both on the dashboard / schedule page:

    1. In Parent A's window, edit a schedule cell.
    2. Within ~2 seconds, Parent B's window MUST reflect the new value WITHOUT a manual refresh.
    3. Reverse the test: Parent B edits a cell, Parent A sees it live.

    Failure-mode checks:
    - Open Parent B's devtools → Network → WS. The WebSocket should show `SUBSCRIBED` status.
    - If the cell does not update live, inspect the WS frames for an `access_token` message. If the token is the anon API key rather than a JWT, plan 01's client.ts re-export was not deployed correctly.

    Status: [ ] PASS / [ ] FAIL
    Notes:

    ## GCal sync regression check (carry-forward from Phase 8 D-07)

    **Test:** Publish the schedule once via the "Publish" button.
    Expected: GCal sync succeeds and events appear in the calendar owner's Google Calendar. Service_role bypasses RLS for the token read — this is intentional (D-07). A failure here indicates a regression introduced by plan 02, not by RLS itself.

    Status: [ ] PASS / [ ] FAIL
    Notes:

    ## Overall verdict

    - [ ] All four RLS-NN scenarios pass + GCal regression check passes → Phase 9 COMPLETE; Phase 10 unblocked.
    - [ ] One or more failures → log details below; planner will open gap closure plan via `/gsd-plan-phase --gaps`.
    ```

    **Step B — Human runs the scenarios and reports.** The agent waits. When the human replies:
    - `All RLS criteria PASSED` → the agent edits `09-VERIFICATION.md` to tick every `[ ]` to `[x]`, fills in the date/name/project-ref, and adds an `Overall verdict: PASSED` line. Then commits the file via `gsd-sdk query commit`.
    - `FAILED: <scenario> — <details>` → the agent records each failure under that scenario's Notes section (verbatim from the human's reply), leaves the scenario checkbox as `[ ]`, and ticks the second Overall verdict bullet. The orchestrator will then invoke `/gsd-plan-phase --gaps`.

    The agent MUST NOT mark scenarios as PASS without the explicit human "All RLS criteria PASSED" reply. The agent MUST NOT auto-run the curl commands or impersonation queries — those require live credentials and a running Supabase project session, which only the human can drive.
  </action>
  <what-built>
    With the SQL applied (Task 1) and plans 01–02 deployed, RLS is fully active. The four success criteria must now be verified against the running app.

    All four scenarios are observable end-to-end. The agent prepares the verification scaffolding (the empty `09-VERIFICATION.md` template), the human runs the scenarios in their browser + terminal, and the agent records the PASS/FAIL outcomes back into the file.
  </what-built>
  <how-to-verify>
    Confirmation path: the human runs four scenarios (RLS-01, RLS-02, RLS-03, RLS-04) plus the GCal regression check. They reply with either:
    - `All RLS criteria PASSED` — agent marks the template all-PASS and commits.
    - `FAILED: <scenario> — <details>` for each failing scenario — agent records failures verbatim, leaves checkboxes unchecked.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/09-row-level-security/09-VERIFICATION.md && grep -q "RLS-01" .planning/phases/09-row-level-security/09-VERIFICATION.md && grep -q "RLS-02" .planning/phases/09-row-level-security/09-VERIFICATION.md && grep -q "RLS-03" .planning/phases/09-row-level-security/09-VERIFICATION.md && grep -q "RLS-04" .planning/phases/09-row-level-security/09-VERIFICATION.md</automated>
    Manual: human reports the four scenario outcomes verbatim; agent records each outcome under its scenario's Notes section.
  </verify>
  <done>
    `09-VERIFICATION.md` exists; contains a section per RLS-NN scenario plus a GCal regression section; either all checkboxes are ticked + overall verdict is PASSED, OR specific failures are recorded with notes for `/gsd-plan-phase --gaps` to act on.
  </done>
  <resume-signal>Reply `All RLS criteria PASSED` to mark the phase complete, OR reply `FAILED: <scenario> — <details>` for each failed scenario so a gap-closure plan can be opened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → Supabase Dashboard | Trusted; covered by Supabase Dashboard auth + 2FA |
| Live database after SQL run | Untrusted clients now subject to RLS evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Information Disclosure | SQL applied partially (e.g., ENABLE without policies) leaves tables inaccessible to even authenticated users, looking like a hang | mitigate | Task 1 instructs running the WHOLE file in one paste; pre-flight verification (the pg_policies count query) catches partial application before Task 2 |
| T-09-18 | Denial of Service | The publication ALTER fails on an already-published table, leaving the human in doubt | accept | Task 1 explicitly documents the "already member of publication" error as benign and harmless — the policies are already in place by that point |
| T-09-19 | Information Disclosure | Verification record file (`09-VERIFICATION.md`) contains real refresh tokens or anon keys pasted from the curl test | mitigate | Task 2 template uses `<URL>` and `<ANON_KEY>` placeholders; instructs the human to truncate output via `head -c 200`. Anon key alone in the file is acceptable (it's `NEXT_PUBLIC_*` by design); refresh tokens MUST NEVER appear in the file — agent must scrub any pasted output that looks like an OAuth token before committing. |
| T-09-20 | Tampering | Human runs an older version of `policies.sql` (e.g., cached in clipboard) — uses `auth.email()` which would fail | mitigate | Task 1 step 3 instructs `cat supabase/policies.sql` to ensure fresh content; step 7 explicitly calls out the `auth.email() does not exist` failure mode |
</threat_model>

<verification>
The plan itself produces no code. Verification is the deliverable: `09-VERIFICATION.md` exists and records PASS for the four RLS-NN scenarios plus the GCal regression check.
</verification>

<success_criteria>
After this plan completes:
1. The four RLS requirements (RLS-01..RLS-04) all show PASS in `09-VERIFICATION.md`
2. GCal sync regression check passes (D-07 service_role bypass still works)
3. `pg_policies` shows 4/4/4/4/3 distribution across the five tables
4. `schedule_entries` is confirmed in the `supabase_realtime` publication
5. ROADMAP.md can be updated to mark Phase 9 complete; Phase 10 (Auth.js Removal) can begin
</success_criteria>

<output>
After completion, create `.planning/phases/09-row-level-security/09-04-SUMMARY.md` covering:
- Confirmation that `supabase/policies.sql` was applied to the live project
- Final policy count from the pg_policies query (must match the expected 4/4/4/4/3)
- Final verdict for RLS-01, RLS-02, RLS-03, RLS-04 (PASS or specifics of any failure)
- GCal regression check result
- Note that this plan unblocks Phase 10
</output>
