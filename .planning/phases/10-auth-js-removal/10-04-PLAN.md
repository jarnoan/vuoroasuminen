---
phase: 10
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified: []
autonomous: false
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
tags:
  - verification
  - checkpoint
  - schema-push

must_haves:
  truths:
    - "npm run build exits 0 (TypeScript + Next.js compile succeed)"
    - "npx vitest run exits 0 (all tests pass)"
    - "npm run db:push reports no schema changes pending (Drizzle schema in sync with the database after table drops + auth schema deletion)"
    - "Repo-wide grep finds zero references to next-auth, @auth/drizzle-adapter, @/auth, @/auth.config, schema/auth, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, AUTH_SECRET, or AUTH_URL outside of .planning/, .git/, and node_modules/"
    - "Both parents have re-signed in via Supabase OAuth, the dashboard loads, and at least one publish→GCal sync round-trip succeeds end-to-end"
  artifacts:
    - path: ".planning/phases/10-auth-js-removal/10-04-SUMMARY.md"
      provides: "End-to-end verification record for CLEAN-01..CLEAN-03"
  key_links:
    - from: "Drizzle schema (domain.ts + tokens.ts only)"
      to: "Supabase Postgres"
      via: "npm run db:push (no-op expected)"
      pattern: "No schema changes|nothing to push"
    - from: "Both parents' browsers"
      to: "Supabase Auth → GCal API"
      via: "sign-in → publish → calendar event creation"
      pattern: "GCal event created for owner email"
---

<objective>
[BLOCKING] Verify Phase 10 closure end-to-end: schema is in sync (Drizzle no longer pushes any DDL for the dropped Auth.js tables), build + tests pass, no stale Auth.js references survive anywhere in the repo, and both parents can complete the post-deployment re-sign-in + publish + GCal sync round-trip.

Purpose: All three CLEAN requirements (CLEAN-01 package uninstall, CLEAN-02 table drops, CLEAN-03 env var removal) must be cross-verified together. This plan is the gate that confirms Phase 10 is shippable. The human checkpoint at the end satisfies the deployment note in STATE.md: "Both parents must re-sign-in after Phase 10 deployment — auth cookies are incompatible; GCal sync fails until both parents have new Supabase sessions."
Output: A SUMMARY file documenting build output, test output, db:push output, repo-wide grep results, and the operator's confirmation of the two-parent re-sign-in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-auth-js-removal/10-01-PLAN.md
@.planning/phases/10-auth-js-removal/10-02-PLAN.md
@.planning/phases/10-auth-js-removal/10-03-PLAN.md

<interfaces>
<!-- After Plans 01, 02, and 03 complete, the following state MUST hold:

     Database (from Plan 01):
       - public.users, public.accounts, public.sessions, public."verificationTokens" do not exist
       - public.user_google_tokens is unchanged

     Source tree (from Plan 02):
       - src/auth.ts, src/auth.config.ts, src/types/next-auth.d.ts, src/db/schema/auth.ts,
         src/db/clear-tokens.ts, src/app/api/auth/[...nextauth]/route.ts all deleted
       - src/app/api/auth/ directory removed
       - src/db/index.ts no longer imports authSchema
       - src/middleware.ts matcher no longer carves out api/auth
       - src/actions/schedule.test.ts mocks @/lib/supabase/server

     Packages + env (from Plan 03):
       - package.json has no next-auth, no @auth/drizzle-adapter, no db:clear-tokens script
       - node_modules has no next-auth and no @auth/drizzle-adapter
       - src/env.ts and .env.example use GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET (no AUTH_*)
       - src/lib/gcal/client.ts reads process.env.GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET

     drizzle.config.ts globs src/db/schema/*.ts — with auth.ts deleted, the glob
     picks up only domain.ts + tokens.ts. db:push must then report "no changes"
     because the database already has those tables and the four Auth.js tables
     no longer exist in either the schema files or the DB.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide grep — confirm no surviving Auth.js references</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - .planning/phases/10-auth-js-removal/10-01-PLAN.md
    - .planning/phases/10-auth-js-removal/10-02-PLAN.md
    - .planning/phases/10-auth-js-removal/10-03-PLAN.md
  </read_first>
  <action>
    Run a strict repo-wide grep for every Auth.js-related token. Exclude `.git`, `node_modules`, `.next`, `.vercel`, and `.planning` (history artifacts may legitimately reference old vars; tracked code must not).

    ```bash
    # Token list: package names, env vars, import paths, internal aliases
    TOKENS='next-auth|@auth/drizzle-adapter|@/auth\b|@/auth\.config|@/db/schema/auth|AUTH_GOOGLE_ID|AUTH_GOOGLE_SECRET|AUTH_SECRET|AUTH_URL|NEXTAUTH_SECRET|NEXTAUTH_URL'

    # Search tracked sources, configs, and docs only — exclude planning + generated dirs
    grep -rnE "$TOKENS" . \
      --include="*.ts" --include="*.tsx" \
      --include="*.js" --include="*.mjs" --include="*.cjs" \
      --include="*.json" --include="*.example" --include="*.md" \
      --exclude-dir=node_modules \
      --exclude-dir=.git \
      --exclude-dir=.next \
      --exclude-dir=.vercel \
      --exclude-dir=.planning \
      || echo "OK: zero matches"
    ```

    Expected: the only line printed is `OK: zero matches`.

    If ANY match is reported in tracked source files (`.ts`, `.tsx`, `.js`, `.mjs`, `.cjs`, `.json`, `.example`, `.md` outside `.planning/`), STOP. Surface the file paths and offending lines to the user with `## ⚠ Phase 10 verification failure: surviving Auth.js references` and refuse to mark the phase complete.
  </action>
  <verify>
    <automated>! grep -rnE "next-auth|@auth/drizzle-adapter|@/auth\b|@/auth\.config|@/db/schema/auth|AUTH_GOOGLE_ID|AUTH_GOOGLE_SECRET|AUTH_SECRET|AUTH_URL|NEXTAUTH_SECRET|NEXTAUTH_URL" . --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" --include="*.cjs" --include="*.json" --include="*.example" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.next --exclude-dir=.vercel --exclude-dir=.planning</automated>
  </verify>
  <acceptance_criteria>
    - The grep command above prints zero matches (only "OK: zero matches" or empty output)
    - Specifically: zero hits for `next-auth` in any tracked file outside .planning
    - Specifically: zero hits for `@auth/drizzle-adapter` in any tracked file outside .planning
    - Specifically: zero hits for `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`, `AUTH_SECRET`, `AUTH_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL` in any tracked file outside .planning
    - Specifically: zero hits for `@/auth` (word boundary) or `@/auth.config` or `@/db/schema/auth` in any tracked file outside .planning
  </acceptance_criteria>
  <done>The grep gauntlet returns zero matches; no Auth.js token survives in tracked source.</done>
</task>

<task type="auto">
  <name>Task 2: Build + tests + db:push must all succeed (in that order)</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - package.json (confirm the three scripts exist: build, test command via vitest, db:push)
    - drizzle.config.ts (confirm schema glob picks up only domain.ts + tokens.ts)
  </read_first>
  <action>
    Run the three verification commands in this exact order. Each must exit 0 before the next runs.

    1. TypeScript + Next.js production build:
       ```bash
       npm run build
       ```
       Expected: exits 0, prints `✓ Compiled successfully` (or Next 16 equivalent), no errors about missing modules `next-auth`, `@auth/drizzle-adapter`, or `@/auth`.

    2. Vitest suite:
       ```bash
       npx vitest run
       ```
       Expected: exits 0; the schedule.test.ts file (migrated in Plan 02) reports 10/10 passing; no test file fails to resolve `@/auth`.

    3. Drizzle schema sync verification — `db:push` MUST be a no-op (no DDL):
       ```bash
       npm run db:push
       ```
       Expected output (drizzle-kit): a phrase like `No changes detected`, `[i] No schema changes, nothing to migrate`, or `Your SQL schema is in sync` — i.e. drizzle-kit prints nothing pending. If drizzle-kit reports any pending CREATE/DROP/ALTER, STOP and surface to the user — that means either Plan 01 missed a table or Plan 02 missed a schema file.

       Cross-check via psql that the public schema has exactly the expected three Drizzle tables plus any Supabase-managed tables:
       ```bash
       set -a; source .env.local; set +a
       psql "$DATABASE_URL" -tAc "SELECT tablename FROM pg_tables WHERE schemaname='public' ORDER BY tablename;"
       ```
       Expected: contains `children`, `schedule_entries`, `schedules`, `gcal_events`, `user_google_tokens` (Drizzle-managed). Does NOT contain `users`, `accounts`, `sessions`, `verificationTokens`.

    If any of the three commands fails:
    - Capture the failing output verbatim
    - Surface to user with `## ⚠ Phase 10 verification failure: <command> failed` and the diagnostic output
    - Do NOT proceed to the human checkpoint task
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run &amp;&amp; npm run db:push</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Build output does NOT contain "Cannot find module 'next-auth'" or "Cannot find module '@auth/drizzle-adapter'" or "Cannot find module '@/auth'"
    - `npx vitest run` exits 0
    - Vitest output reports schedule.test.ts with 10 passing tests (or the same count it had before — must not have decreased)
    - `npm run db:push` exits 0
    - `npm run db:push` output contains either "No changes" or "nothing to" or "in sync" (case-insensitive)
    - `psql -c "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('users','accounts','sessions','verificationTokens')"` returns 0 rows
    - `psql -c "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('children','schedules','schedule_entries','gcal_events','user_google_tokens')"` returns 5 rows
  </acceptance_criteria>
  <done>Build, tests, and db:push all green; database contains the five domain/token tables and none of the four Auth.js tables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [HUMAN] Both parents re-sign-in + GCal publish round-trip</name>
  <files>(no files modified — human checkpoint)</files>
  <read_first>
    - .planning/STATE.md (specifically the "Coordinate simultaneous re-sign-in by both parents" item under Pending Todos)
  </read_first>
  <what-built>
    - Auth.js packages uninstalled
    - Auth.js DB tables dropped from Supabase Postgres
    - Auth.js env vars removed (AUTH_GOOGLE_* renamed to GOOGLE_CLIENT_*; AUTH_SECRET + AUTH_URL gone)
    - Middleware no longer carves out `/api/auth`
    - All consumer files cleaned (db/index.ts, schedule.test.ts)

    Pre-requisite: before performing the verification below, the operator MUST
    have set `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` env vars in BOTH
    `.env.local` (local) AND the Vercel deployment environment. The values are
    the same Google Cloud Console client_id / client_secret that were previously
    stored as `AUTH_GOOGLE_ID` / `AUTH_GOOGLE_SECRET` — rename, do not regenerate.
    `AUTH_SECRET` and `AUTH_URL` can be deleted from both environments.
  </what-built>
  <action>
    Pause execution and present the verification steps below to the operator. Do not proceed to write the SUMMARY until the operator replies with "approved" or "failed: ...".

    Verification steps (also reproduced in `<how-to-verify>` for the operator-facing prompt):

    1. Father signs in via the deployed app in a fresh incognito window; complete Google OAuth consent; confirm dashboard loads with no missing-token banner; verify `user_google_tokens` row for father has fresh `updated_at` and a refresh_token >50 chars.

    2. Mother signs in via a separate incognito window; same checks for mother's row.

    3. Either parent clicks "Julkaise" (Publish); confirm GCal events appear in the calendar OWNER's Google Calendar (not necessarily the publisher's).

    4. Browser navigates to `/api/auth/anything` and receives 404 (the Auth.js route directory is gone).

    5. Operator replies `approved` (all five checks pass) or `failed: <details>`.
  </action>
  <how-to-verify>
    Perform these steps with BOTH parents present (or coordinated by phone):

    1. **Father signs in**
       - Open the deployed app in a fresh incognito browser window
       - Click "Sign in with Google"
       - Complete the Google OAuth consent screen (consent prompt is expected — Phase 8 D-05 forces `prompt:consent` every sign-in)
       - Confirm: dashboard loads, no warning banner about missing calendar owner token
       - Confirm: header shows father's email/name
       - In a separate terminal, verify the token row was upserted:
         `psql "$DATABASE_URL" -c "SELECT email, length(refresh_token) > 50 AS has_refresh_token, updated_at FROM user_google_tokens WHERE email = '<father email>';"`
         Expected: 1 row, `has_refresh_token = t`, `updated_at` is within the last 60 seconds.

    2. **Mother signs in** (different incognito window, or same machine after clearing cookies)
       - Same flow as above
       - Confirm token row for mother's email exists with fresh `updated_at`

    3. **Publish round-trip** (either parent can perform this)
       - On the dashboard, click "Julkaise" (Publish)
       - Confirm: publish succeeds, no error toast
       - Open Google Calendar in the calendar owner's account
       - Confirm: events for the published date range are present
       - Confirm: events are created in the OWNER's calendar (not the publisher's, if they differ — Phase 8 D-01 ownerEmail behavior)

    4. **Middleware regression check**
       - Navigate to `/api/auth/anything` in the browser
       - Expected: 404 (Next.js default — the route directory is gone)
       - NOT expected: 200, 302 to a NextAuth-style sign-in page, or any indication the old NextAuth handler responded

    5. **Reply with one of:**
       - `approved` — all five checks pass
       - `failed: <details>` — describe what broke; planner will produce a gap-closure plan

    Note: if either parent's sign-in fails with `invalid_grant` or `redirect_uri_mismatch`, that is almost certainly a Supabase Dashboard / Google Cloud Console configuration drift, NOT a code defect in Phase 10 — capture the exact error and surface it before re-planning.
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint:human-verify task; resume-signal "approved" gates progression. SUMMARY captures: psql output for both parents' user_google_tokens rows + curl -s -o /dev/null -w '%{http_code}' "${APP_URL}/api/auth/foo" returning 404.</automated>
  </verify>
  <resume-signal>Type "approved" (all checks pass) or describe failures.</resume-signal>
  <acceptance_criteria>
    - Operator types `approved` and the planner records the response verbatim in the SUMMARY
    - SUMMARY contains the psql output proving both parents have fresh refresh_token rows
    - SUMMARY contains a screenshot or operator-confirmed note that GCal events appeared in the owner's calendar
    - SUMMARY records the 404 response (or equivalent) when hitting `/api/auth/anything`
  </acceptance_criteria>
  <done>Operator confirms all five checks pass; phase is shippable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production deployment ↔ both parents' browsers | Re-sign-in flow must complete for each parent before GCal sync works for either |
| Drizzle schema files ↔ Supabase Postgres | Schema-DB sync must be confirmed via db:push so future Phase X migrations land on a clean baseline |
| Repo-wide grep ↔ tracked source | Verification gate that prevents shipping with surviving Auth.js code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Spoofing | Stale browser cookie from old Auth.js session presented to new Supabase middleware | mitigate | Human verification uses fresh incognito sessions — cookies are isolated from the operator's existing browser state |
| T-10-18 | Tampering | A surviving import of `next-auth` slips through into the bundle | mitigate | Task 1 grep gate fails the phase if any tracked file matches the Auth.js token list |
| T-10-19 | Repudiation | Phase marked complete without operator verification of GCal sync | mitigate | Task 3 is a blocking human checkpoint; SUMMARY must record the operator's literal "approved" reply |
| T-10-20 | Information Disclosure | Old Auth.js JWT cookies still resolve to a user in middleware | mitigate | Middleware now uses Supabase getUser() exclusively; the Auth.js cookie schema is unparseable by Supabase and will fail to validate, redirecting to home |
| T-10-21 | Denial of Service | One parent fails to re-sign-in, blocking GCal sync indefinitely | mitigate | Task 3 requires BOTH parents complete sign-in before approval; SUMMARY records who signed in when |
| T-10-22 | Elevation of Privilege | RLS bypass via the deleted /api/auth route | mitigate | Route directory deleted in Plan 02, middleware matcher updated in Plan 02; Task 3 step 4 confirms `/api/auth/*` returns 404 |
</threat_model>

<verification>
1. Task 1: zero grep matches for the Auth.js token list across tracked source
2. Task 2: `npm run build`, `npx vitest run`, and `npm run db:push` all exit 0
3. Task 2: db has the five expected Drizzle tables and none of the four Auth.js tables
4. Task 3: operator approves after verifying both parents can sign in, publish triggers GCal sync, and `/api/auth/*` returns 404
</verification>

<success_criteria>
- All three CLEAN requirements (CLEAN-01, CLEAN-02, CLEAN-03) verified
- Build + tests + db:push all green
- Operator-confirmed end-to-end sign-in and GCal publish round-trip
- Repo contains no Auth.js references in tracked source
</success_criteria>

<output>
After completion, create `.planning/phases/10-auth-js-removal/10-04-SUMMARY.md` including:
- Task 1 grep output (must be empty or "OK: zero matches")
- Task 2 outputs: `npm run build` tail (last 20 lines), vitest summary line, `npm run db:push` output, psql public-schema tablename list
- Task 3 outputs: operator's verbatim reply, psql `user_google_tokens` query output for both parents, confirmation of GCal events and `/api/auth/*` 404
- Final phase status: CLEAN-01 ✓, CLEAN-02 ✓, CLEAN-03 ✓
</output>
