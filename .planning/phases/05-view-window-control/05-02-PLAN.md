---
phase: 05-view-window-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/popover.tsx
  - src/components/ui/calendar.tsx
autonomous: true
requirements:
  - VIEW-03
must_haves:
  truths:
    - "shadcn popover and calendar components are installed and importable from @/components/ui/"
    - "react-day-picker is present in package.json (auto-added by shadcn install)"
  artifacts:
    - path: "src/components/ui/popover.tsx"
      provides: "Popover, PopoverContent, PopoverTrigger — base-nova preset using @base-ui/react"
      exports: ["Popover", "PopoverContent", "PopoverTrigger"]
    - path: "src/components/ui/calendar.tsx"
      provides: "Calendar component backed by react-day-picker"
      exports: ["Calendar"]
  key_links:
    - from: "src/components/schedule/view-toolbar.tsx"
      to: "src/components/ui/popover.tsx"
      via: "import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover'"
      pattern: "from \"@/components/ui/popover\""
    - from: "src/components/schedule/view-toolbar.tsx"
      to: "src/components/ui/calendar.tsx"
      via: "import { Calendar } from '@/components/ui/calendar'"
      pattern: "from \"@/components/ui/calendar\""
---

<objective>
Install the shadcn/ui popover and calendar components required for the date picker in ViewToolbar.

Purpose: The date picker (D-09) is the mechanism for VIEW-03 (explicit start date). Both components must exist before ViewToolbar can import them in Plan 03.

Output: `src/components/ui/popover.tsx` and `src/components/ui/calendar.tsx` copied from the shadcn canary registry. `react-day-picker` added to package.json as a dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-view-window-control/05-CONTEXT.md
@.planning/phases/05-view-window-control/05-RESEARCH.md

<interfaces>
<!-- Existing UI components for reference — the new ones must follow the same preset -->

From src/components/ui/button.tsx (already installed — base-nova pattern):
```typescript
// Base-nova preset — @base-ui/react primitives, not Radix
// Available variants: "default" | "outline" | "secondary" | "ghost" | "destructive" | "link"
// Available sizes: "default" | "sm" | "xs" | "lg" | "icon" | "icon-sm" | "icon-xs" | "icon-lg"
export { buttonVariants }
export function Button({ className, variant, size, ...props }: ButtonProps): JSX.Element
```

From src/components/ui/dialog.tsx (already installed — base-nova trigger pattern):
```typescript
// PopoverTrigger uses the same render prop pattern as DialogTrigger:
// <DialogTrigger render={<Button variant="default" size="sm" />}>Label</DialogTrigger>
// NOT asChild — this is Base UI, not Radix UI
import { Dialog, DialogTrigger, DialogContent, ... } from "@/components/ui/dialog"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn popover and calendar components</name>
  <files>src/components/ui/popover.tsx, src/components/ui/calendar.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (verify base-nova preset is in use — popover must match)
    - src/components/ui/dialog.tsx (verify render prop pattern — PopoverTrigger follows same pattern)
    - package.json (verify react-day-picker is NOT yet present before install)
  </read_first>
  <action>
Run the following command from the project root to install both components:

```bash
npx shadcn@canary add popover calendar
```

This command:
1. Copies `popover.tsx` into `src/components/ui/popover.tsx` (uses @base-ui/react Popover — already installed)
2. Copies `calendar.tsx` into `src/components/ui/calendar.tsx` (uses react-day-picker)
3. Auto-adds `react-day-picker` to package.json dependencies and runs npm install

**If the CLI prompts "overwrite existing file?"** — answer No for any existing file. Both popover.tsx and calendar.tsx should not exist yet (verified: only button.tsx and dialog.tsx exist in src/components/ui/).

**If the CLI prompts for a different install command** — accept the default. Do not pass `--no-deps` flag; react-day-picker must be installed.

After install, verify the files were created and react-day-picker appears in package.json.
  </action>
  <verify>
    <automated>cd /Users/jarno/src/vuoroasuminen && ls src/components/ui/ && grep "react-day-picker" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/ui/` shows `popover.tsx` and `calendar.tsx` in output
    - `grep "react-day-picker" package.json` returns a line with the package name and a version
    - `grep "PopoverTrigger" src/components/ui/popover.tsx` returns a line (exported name exists)
    - `grep "Calendar" src/components/ui/calendar.tsx` returns a line (exported name exists)
    - `npx tsc --noEmit 2>&1 | head -5` shows no errors caused by the new component files
  </acceptance_criteria>
  <done>popover.tsx and calendar.tsx exist in src/components/ui/; react-day-picker is in package.json; TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry → local codebase | Third-party component source code copied into project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | shadcn registry install | accept | Using official shadcn registry only (`npx shadcn@canary add`) — no third-party registries. RESEARCH.md "Registry safety gate: not required" confirmed. |
| T-05-05 | Tampering | react-day-picker (new dep) | accept | react-day-picker is a well-maintained library (9.x); used by shadcn calendar officially; no user input is passed to it unsanitized |
</threat_model>

<verification>
After task completes:

```bash
# Both files exist
ls /Users/jarno/src/vuoroasuminen/src/components/ui/popover.tsx
ls /Users/jarno/src/vuoroasuminen/src/components/ui/calendar.tsx

# react-day-picker in package.json
grep "react-day-picker" /Users/jarno/src/vuoroasuminen/package.json

# No new TypeScript errors
cd /Users/jarno/src/vuoroasuminen && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- `src/components/ui/popover.tsx` exists with `PopoverTrigger` export
- `src/components/ui/calendar.tsx` exists with `Calendar` export
- `react-day-picker` present in package.json dependencies
- No TypeScript errors introduced by the new component files
</success_criteria>

<output>
After completion, create `.planning/phases/05-view-window-control/05-02-SUMMARY.md`
</output>
