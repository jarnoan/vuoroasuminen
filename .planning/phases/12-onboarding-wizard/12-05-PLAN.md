---
phase: 12-onboarding-wizard
plan: 05
type: execute
wave: 4
depends_on: [12-02, 12-03, 12-04]
files_modified:
  - src/env.ts
  - .env.example
autonomous: false
requirements:
  - ONBR-03
  - ONBR-04
tags:
  - env
  - cleanup
  - verification
  - checkpoint

must_haves:
  truths:
    - "src/env.ts no longer lists PARENT_*, APP_CHILDREN, APP_START_DATE as required"
    - "GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, DATABASE_URL, NEXT_PUBLIC_SUPABASE_* remain required"
    - "App builds and runs without any APP_PARENT* env vars (ONBR-04 success criterion proven)"
    - "End-to-end wizard flow on local dev environment completes successfully"
    - "User has been informed about Vercel env-var cleanup as a manual followup"
  artifacts:
    - path: "src/env.ts"
      provides: "Updated REQUIRED_ENV_VARS list — only infrastructure vars remain"
      contains: "GOOGLE_CLIENT_ID"
    - path: ".env.example"
      provides: "Reflects updated env contract"
      contains: "DATABASE_URL"
  key_links:
    - from: "Wizard /setup completion"
      to: "/dashboard (loads from family_config, not env vars)"
      via: "saveWizardConfig → family_config → getAppConfig"
      pattern: "family_config"
---

<objective>
Close out Phase 12 by:
1. Removing the legacy `PARENT_*` and `APP_*` family-config env vars from `src/env.ts` REQUIRED_ENV_VARS — proves ONBR-04's success criterion "APP_PARENT* env vars no longer required for app function" (D-08, D-09).
2. Updating `.env.example` to reflect the new env contract.
3. Running a full local smoke test of the wizard flow as a `checkpoint:human-verify` task.
4. Listing the Vercel/operational followups (delete env vars from Vercel; CR-01 git history scrub already tracked in STATE.md) so the user knows what manual actions remain after this plan commits.

Purpose: This is the final gate proving the app no longer needs the legacy env vars. The phase closes when a human confirms the local end-to-end wizard → dashboard flow works without those vars present.

Output: 1 modified env config file + 1 modified .env.example + 1 human verification checkpoint + a list of operational followups handed back to the user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding-wizard/12-CONTEXT.md
@.planning/research/ONBOARDING-STACK.md
@.planning/phases/12-onboarding-wizard/12-01-PLAN.md
@.planning/phases/12-onboarding-wizard/12-02-PLAN.md
@.planning/phases/12-onboarding-wizard/12-03-PLAN.md
@.planning/phases/12-onboarding-wizard/12-04-PLAN.md

<interfaces>
<!-- Current src/env.ts (must shrink) -->
```typescript
import { config } from "dotenv"
config({ path: ".env.local" })

const REQUIRED_ENV_VARS = [
  "GOOGLE_CLIENT_ID",
  "GOOGLE_CLIENT_SECRET",
  "DATABASE_URL",
  "NEXT_PUBLIC_SUPABASE_URL",
  "NEXT_PUBLIC_SUPABASE_ANON_KEY",
  "PARENT_FATHER_EMAIL",  // REMOVE
  "PARENT_MOTHER_EMAIL",  // REMOVE
  "APP_CHILDREN",          // REMOVE
  "APP_START_DATE",        // REMOVE
] as const

for (const key of REQUIRED_ENV_VARS) {
  if (!process.env[key]) {
    throw new Error(`Missing required env var: ${key}`)
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim REQUIRED_ENV_VARS in src/env.ts + update .env.example</name>
  <files>src/env.ts, .env.example</files>
  <read_first>
    - src/env.ts (current REQUIRED_ENV_VARS list — lines 5-15)
    - .env.example (current example — may reference the legacy vars)
    - .planning/research/ONBOARDING-STACK.md §Step 4 "Remove env-var config entries from src/env.ts"
    - .planning/phases/12-onboarding-wizard/12-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
**Modify `src/env.ts`:**

Replace the `REQUIRED_ENV_VARS` array contents with ONLY the infrastructure vars:

```typescript
import { config } from "dotenv"

config({ path: ".env.local" })

const REQUIRED_ENV_VARS = [
  "GOOGLE_CLIENT_ID",
  "GOOGLE_CLIENT_SECRET",
  "DATABASE_URL",
  "NEXT_PUBLIC_SUPABASE_URL",
  "NEXT_PUBLIC_SUPABASE_ANON_KEY",
] as const

for (const key of REQUIRED_ENV_VARS) {
  if (!process.env[key]) {
    throw new Error(`Missing required env var: ${key}`)
  }
}
```

The four removed entries (`PARENT_FATHER_EMAIL`, `PARENT_MOTHER_EMAIL`, `APP_CHILDREN`, `APP_START_DATE`) are no longer needed because family_config is the source of truth as of Plan 02.

**Modify `.env.example`:**

Read the current `.env.example` first (`cat .env.example`). If the file lists any of the following legacy vars, REMOVE those lines:
- `PARENT_FATHER_EMAIL`
- `PARENT_FATHER_NAME`
- `PARENT_FATHER_CALENDAR_ID`
- `PARENT_MOTHER_EMAIL`
- `PARENT_MOTHER_NAME`
- `PARENT_MOTHER_CALENDAR_ID`
- `APP_CHILDREN`
- `APP_START_DATE`
- `APP_FIRST_PARENT`
- `APP_CALENDAR_OWNER_EMAIL`

Also add a comment block at the top explaining the new model:
```
# Family configuration (parents, children, calendars, start date, first parent)
# is now stored in the family_config DB table — populated by the /setup wizard
# during first-run onboarding. No env vars required for that data after Phase 12.
```

Keep the infrastructure vars: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `DATABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

If `.env.example` doesn't exist, create it with just the 5 infrastructure vars and the explanatory comment block.
  </action>
  <verify>
    <automated>! grep -E "PARENT_FATHER_EMAIL|PARENT_MOTHER_EMAIL|APP_CHILDREN|APP_START_DATE" src/env.ts; grep -q "GOOGLE_CLIENT_ID" src/env.ts; grep -q "DATABASE_URL" src/env.ts; ! grep -E "PARENT_FATHER|PARENT_MOTHER|APP_CHILDREN|APP_START_DATE|APP_FIRST_PARENT|APP_CALENDAR_OWNER_EMAIL" .env.example; grep -q "GOOGLE_CLIENT_ID" .env.example; grep -q "DATABASE_URL" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PARENT_FATHER_EMAIL" src/env.ts` returns 0
    - `grep -c "PARENT_MOTHER_EMAIL" src/env.ts` returns 0
    - `grep -c "APP_CHILDREN" src/env.ts` returns 0
    - `grep -c "APP_START_DATE" src/env.ts` returns 0
    - `grep -c "GOOGLE_CLIENT_ID" src/env.ts` returns 1
    - `grep -c "GOOGLE_CLIENT_SECRET" src/env.ts` returns 1
    - `grep -c "DATABASE_URL" src/env.ts` returns 1
    - `grep -c "NEXT_PUBLIC_SUPABASE_URL" src/env.ts` returns 1
    - `grep -c "NEXT_PUBLIC_SUPABASE_ANON_KEY" src/env.ts` returns 1
    - `wc -l src/env.ts | awk '{print $1}'` returns at most 15 (was ~21, now smaller)
    - `.env.example` does NOT contain `PARENT_`, `APP_CHILDREN`, `APP_START_DATE`, `APP_FIRST_PARENT`, `APP_CALENDAR_OWNER_EMAIL`
    - `.env.example` DOES contain `GOOGLE_CLIENT_ID`, `DATABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`
    - `npx tsc --noEmit src/env.ts` exits 0
  </acceptance_criteria>
  <done>src/env.ts only requires infrastructure env vars; .env.example reflects the new contract.</done>
</task>

<task type="auto">
  <name>Task 2: Verify local dev boots without legacy env vars + family_config seeded</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - .env.local (developer's local env — DO NOT commit; just confirm what's defined)
    - src/env.ts (already trimmed per Task 1)
    - src/db/schema/domain.ts (must contain familyConfig)
  </read_first>
  <action>
This task is verification-only. The goal is to confirm that `npm run dev` boots and the app behaves correctly with the legacy env vars REMOVED from the local environment.

Steps to execute:

1. **Backup `.env.local`** to `.env.local.bak` (in case rollback is needed):
   ```bash
   cp .env.local .env.local.bak
   ```

2. **Edit `.env.local`** — comment out or remove the following lines if present (but DO NOT remove the infrastructure vars):
   ```
   # PARENT_FATHER_EMAIL=...
   # PARENT_FATHER_NAME=...
   # PARENT_FATHER_CALENDAR_ID=...
   # PARENT_MOTHER_EMAIL=...
   # PARENT_MOTHER_NAME=...
   # PARENT_MOTHER_CALENDAR_ID=...
   # APP_CHILDREN=...
   # APP_START_DATE=...
   # APP_FIRST_PARENT=...
   # APP_CALENDAR_OWNER_EMAIL=...
   ```

3. **Truncate the family_config table** (so the dashboard redirect-to-/setup path is exercised):
   ```bash
   psql "$DATABASE_URL" -c "DELETE FROM public.family_config WHERE id = 1;"
   ```
   (This works against the live Supabase DB. If you want to keep a backup, run a `SELECT * FROM public.family_config WHERE id = 1;` first and save the row.)

4. **Run `npm run dev`** and confirm:
   - Server starts without "Missing required env var" errors.
   - Console does not mention PARENT_* or APP_CHILDREN.
   - No JavaScript runtime error about a missing default export of `@/config/app`.

5. **Run `npm run build`** in a separate shell to confirm production build also succeeds:
   ```bash
   npm run build
   ```
   Expected: completes with no errors. The build step no longer invokes the deleted `generate-app-config.js` script. TypeScript compiles. Next.js renders the routes.

6. If steps 4 or 5 fail, surface the exact error output. Do NOT proceed to the checkpoint task until the dev and build both pass.

7. Leave `.env.local` in its modified state for Task 3's checkpoint testing — the human checkpoint walks through the wizard flow next.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tee /tmp/12-05-build.log; ! grep -E "Missing required env var: (PARENT_|APP_CHILDREN|APP_START_DATE)" /tmp/12-05-build.log; grep -q "Compiled successfully\|✓ Compiled" /tmp/12-05-build.log || grep -q "Generating static pages" /tmp/12-05-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with `.env.local` lacking the legacy family-config vars
    - Build output does NOT contain "Missing required env var: PARENT_..." or similar
    - `family_config` table is empty (zero rows) — confirms next checkpoint exercises the un-onboarded path
    - `.env.local.bak` exists (rollback safety net)
  </acceptance_criteria>
  <done>Production build proven to work without legacy env vars; DB is in pre-onboarding state ready for the human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human end-to-end wizard verification</name>
  <what-built>
The complete Phase 12 onboarding flow:
- /setup wizard with 4 steps (Perhetiedot → Kalenteri → Tarkista → Valmis)
- saveWizardConfig persists to family_config
- Dashboard reads via getAppConfig and redirects to /setup when missing
- All client components consume parent/children via props instead of env vars
- src/env.ts no longer requires the legacy PARENT_*/APP_* vars
  </what-built>
  <how-to-verify>
**Setup (one-time, before testing):**
1. Confirm `.env.local` does NOT contain `PARENT_FATHER_EMAIL`, `PARENT_MOTHER_EMAIL`, `APP_CHILDREN`, `APP_START_DATE` (those were commented out in Task 2).
2. Confirm `family_config` row was deleted in Task 2 (`psql "$DATABASE_URL" -c "SELECT count(*) FROM public.family_config WHERE id=1;"` → expect 0).
3. Start the dev server: `npm run dev`. Confirm no startup errors mentioning the legacy env vars.

**Test sequence — happy path:**
1. Open http://localhost:3000 in an incognito window.
2. Click "Sign in with Google" → complete Google OAuth as the calendar-owner account (jarnoan@gmail.com or whichever is your "first parent").
3. After sign-in, the app should redirect to `/dashboard` → which detects no family_config → redirects to `/setup`.
4. Step 1 (Perhetiedot):
   - Your name pre-filled from Google display name. Editable.
   - Enter Toinen vanhempi nimi: e.g., "Äiti".
   - Enter Toinen vanhempi sähköposti: a real Google account email (the other parent's).
   - Add children: "Taimi", "Eino", "Hilla" (or whatever your kids' names are). Verify "+ Lisää lapsi" adds rows and the X button removes them. Confirm the X is disabled on the last remaining row.
   - Pick a start date: confirm only Mondays are selectable; pick a Monday.
   - Select first parent: pick "Isä" (or whichever).
   - Click "Jatka" → advances to Step 2.
5. Step 2 (Kalenteri):
   - Confirm a loading spinner shows on the Combobox while calendars load.
   - Open the Combobox: confirm your Google calendars list appears (by friendly name).
   - Pick your own calendar from the list → the raw ID auto-fills the "Kalenterin tunnus" text input below.
   - In the Parent B calendar field, paste a calendar ID for the other parent's calendar (or the same one for testing).
   - Expand "Mistä löydät kalenterin tunnuksen?" → confirm the Finnish numbered instructions appear.
   - Click "Jatka" → advances to Step 3.
6. Step 3 (Tarkista):
   - Confirm all entered data is shown in the definition-list summary.
   - Calendar IDs are truncated to ~40 chars with ellipsis.
   - Click "Tallenna ja jatka".
   - Button shows "Tallennetaan…" with aria-busy=true.
   - On success → advances to Step 4.
7. Step 4 (Valmis):
   - "Asennus valmis!" heading shows.
   - Body text "Perhetiedot on tallennettu. Voit nyt siirtyä aikatauluun."
   - Phase 13 hand-off Alert: "Toisen vanhemman kutsuminen on tulossa. Toistaiseksi aikataulua voi käyttää yksin."
   - Click "Siirry aikatauluun" → lands on `/dashboard`.
8. Dashboard verification:
   - Schedule table renders with parent names (Isä/Äiti) in cells.
   - Stats panel shows correct child count and parent names.
   - Clear panel shows the correct number of children in its preview.
   - DB sanity check: `psql "$DATABASE_URL" -c "SELECT id, parent1_email, parent2_email, children FROM public.family_config;"` → expect one row with your entered values.

**Test sequence — guard rails:**
9. Re-visit `/setup` while signed in → confirm redirect to `/dashboard` (already-onboarded redirect, page.tsx try/catch).
10. Sign out, visit `/setup` → confirm redirect to `/`.

**Test sequence — validation:**
11. (Optional, fresh DB) Try saving with parent1Email === parent2Email → Step 3 saveWizardConfig returns error displayed inline as an Alert.
12. (Optional) Try entering an invalid email → Step 1 inline error "Syötä kelvollinen sähköpostiosoite".
13. (Optional) Try removing all children → Step 1 inline error "Lisää vähintään yksi lapsen nimi".
14. (Optional) Try clicking "Jatka" without picking a start date → inline error "Valitse aloituspäivä".

**Test sequence — rollback safety:**
15. After verification: optionally restore `.env.local` from `.env.local.bak`. The app should now treat those vars as undefined but, since src/env.ts no longer requires them, no error occurs.

**Report results:**
Reply with either:
- "approved" if all test sequences pass
- "issues: …" listing every failure
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Operational followups handed back to the user</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current pending todos, blockers, deferred items)
  </read_first>
  <action>
Append to `.planning/STATE.md` under `### Pending Todos (Operational)` the new operational items unblocked by Phase 12 completion. These are tasks the user must perform manually outside the codebase:

```markdown
- Remove legacy env vars from Vercel project settings (Settings → Environment Variables): `PARENT_FATHER_EMAIL`, `PARENT_FATHER_NAME`, `PARENT_FATHER_CALENDAR_ID`, `PARENT_MOTHER_EMAIL`, `PARENT_MOTHER_NAME`, `PARENT_MOTHER_CALENDAR_ID`, `APP_CHILDREN`, `APP_START_DATE`, `APP_FIRST_PARENT`, `APP_CALENDAR_OWNER_EMAIL` — Phase 12 made these unused
- After Vercel env cleanup, redeploy and re-confirm the production app boots (the production family_config row should already exist if you completed the wizard on production during Phase 12 verification; if not, sign in on production and complete the wizard)
- ~~CR-01 git history scrub~~ — pre-existing, still tracked; Phase 12 did NOT add new secrets to git
```

Also update the `## Current Position` section header values to reflect Phase 12 close:

```markdown
Phase: 13
Plan: Not started
Status: Ready to plan (Phase 12 complete)
Last activity: <today's date>
```

(Use whatever today's date is when this task runs.)

And update `## Session Continuity` `Last activity:` to a one-liner like:
```
Last activity: <date> - Phase 12 (Onboarding Wizard) complete; DB-driven family config replaces env vars
Stopped at: Phase 12 verified end-to-end on local
```

Do NOT modify `### Decisions`, `### Blockers/Concerns`, `### Deferred Items`, or `## Performance Metrics` blocks beyond the items explicitly listed above.
  </action>
  <verify>
    <automated>grep -q "Remove legacy env vars from Vercel" .planning/STATE.md &amp;&amp; grep -q "Phase 12 (Onboarding Wizard) complete" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains the bullet "Remove legacy env vars from Vercel project settings"
    - `.planning/STATE.md` "Current Position" Phase line reads `Phase: 13`
    - `.planning/STATE.md` Session Continuity line references "Phase 12" and "complete"
    - Pre-existing Decisions/Blockers/Deferred sections are not corrupted (do a final diff before committing)
  </acceptance_criteria>
  <done>State file reflects phase completion; user knows what manual operational steps remain.</done>
</task>

</tasks>

<threat_model>
## Assets at Risk

- **Production env vars** — leaving stale PARENT_*/APP_* values in Vercel after the code stops reading them is operational hygiene, not a vuln
- **Local .env.local** — modifying it for the smoke test must not lose other secrets

## Threat Actors

- **Developer misstep** — committing `.env.local.bak` to git or leaving secrets in test logs

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-30 | Information Disclosure | .env.local.bak accidentally committed to git | mitigate | `.env*` is already in `.gitignore` for this project (verify with `git check-ignore .env.local.bak` returning the file). Task 2 uses a `.bak` suffix which keeps the file inside the same .env* glob. |
| T-12-31 | Tampering | Stale Vercel env vars create a divergence between code and platform expectations | mitigate | Task 4 explicitly hands back the Vercel env-var deletion as a tracked todo in STATE.md. Even if the user forgets, the new src/env.ts code does not READ those vars — they are harmless ghosts. |
| T-12-32 | Information Disclosure | Smoke test reveals user emails in shell history | accept | Two-user app, developer is also one of those users. Acceptable per project model. |

## ASVS L1 Controls Applied

- **V14.1.4** (secrets management) — env vars never touch git; `.gitignore` enforces.

No high-severity unmitigated threats.
</threat_model>

<verification>
1. `npm run build` exits 0.
2. `grep -E "PARENT_FATHER_EMAIL|PARENT_MOTHER_EMAIL|APP_CHILDREN|APP_START_DATE" src/env.ts` returns 0 matches.
3. `git check-ignore .env.local.bak` exits 0 (file is gitignored).
4. Human checkpoint "approved" response captured in the SUMMARY.
5. STATE.md updated with Vercel-cleanup followup and Phase 12 closure.
</verification>

<success_criteria>
Plan 05 is complete when:
- [ ] `src/env.ts` REQUIRED_ENV_VARS contains only 5 infra vars
- [ ] `.env.example` mirrors the trimmed contract
- [ ] `npm run build` succeeds without legacy family-config env vars
- [ ] Human checkpoint walks through all 4 wizard steps + dashboard reload + already-onboarded redirect → "approved"
- [ ] STATE.md updated: Vercel cleanup todo added, Phase 12 marked complete, current position advanced to Phase 13
- [ ] No `.env.local.bak` or other working files committed
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-wizard/12-05-SUMMARY.md` documenting:
- env var changes (before/after)
- Smoke test outcome verbatim (paste user's "approved" or "issues: ..." response)
- Vercel cleanup followup (mirror what was added to STATE.md)
- Phase 12 closure note linking to Phase 13 (Invite + Access Gate)
</output>
