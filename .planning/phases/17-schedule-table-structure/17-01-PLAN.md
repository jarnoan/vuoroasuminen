---
phase: 17-schedule-table-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schedule/schedule-table.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "A 'Viikko X' label row appears above every Monday in the view, including the first week"
    - "The existing hairline separator row (h-px bg-border) is gone — the label row IS the separator"
    - "The week number is the ISO week number computed from day.date via date-fns getISOWeek"
    - "The label uses muted, small typography consistent with other secondary labels in the table"
  artifacts:
    - path: "src/components/schedule/schedule-table.tsx"
      provides: "Week label row rendered when day.isWeekStart is true (for every week including first)"
      contains: "getISOWeek"
  key_links:
    - from: "src/components/schedule/schedule-table.tsx"
      to: "date-fns getISOWeek"
      via: "named import"
      pattern: "import \\{[^}]*getISOWeek[^}]*\\} from \"date-fns\""
    - from: "schedule-table.tsx week-label <tr>"
      to: "day.isWeekStart and day.date"
      via: "JSX conditional + getISOWeek(new Date(day.date))"
      pattern: "day\\.isWeekStart[\\s\\S]*?getISOWeek"
---

<objective>
Replace the existing hairline separator row in `ScheduleTable` with a "Viikko X" week-number label row that appears above every Monday in the view, including the first week. The label row uses muted small typography and computes the ISO week from each day's date via `date-fns`.

Purpose: Give parents a clear week-boundary anchor on the schedule (UI-01). Replacing the hairline with the label preserves table compactness while adding labeling.

Output: Updated `schedule-table.tsx` with `getISOWeek` import, no hairline separator row, and a week label row keyed off `day.isWeekStart` only (no `index > 0` guard).
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-schedule-table-structure/17-CONTEXT.md
@.planning/phases/17-schedule-table-structure/17-PATTERNS.md
@src/components/schedule/schedule-table.tsx
@src/lib/schedule/types.ts

<interfaces>
<!-- Key types and contracts extracted from codebase. Use directly — no exploration needed. -->

From `src/lib/schedule/types.ts` — `ScheduleDay` already provides:
- `date: string` (ISO date for Monday detection)
- `isWeekStart: boolean` (true for Mondays — already drives the current separator row)
- `isToday: boolean`
- `dayLabel: string`
- `cells: ScheduleCell[]`
- `notes: string`, `notesEntryId: string | null`

From `date-fns` v4 (already in dependencies):
- `getISOWeek(date: Date): number` — returns the ISO week number (1-53)

Existing date-fns named-import style in this codebase (do not use subpath imports):
- `src/lib/schedule/queries.ts`: `import { format, addDays, isToday as isTodayFn } from "date-fns"`
- `src/lib/schedule/generate-default.ts`: `import { addDays, differenceInCalendarWeeks, startOfToday, startOfWeek, format, parseISO } from "date-fns"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add date-fns getISOWeek import and replace separator row with week label row</name>
  <files>src/components/schedule/schedule-table.tsx</files>
  <read_first>
    - src/components/schedule/schedule-table.tsx (current state — separator row at lines 277-284, colCount at line 251, no date-fns import yet)
    - .planning/phases/17-schedule-table-structure/17-PATTERNS.md (Change 3 section — exact target JSX, decisions D-01 through D-05)
    - .planning/phases/17-schedule-table-structure/17-CONTEXT.md (Decisions D-01 through D-05)
    - src/lib/schedule/queries.ts (lines 1-10) — reference for date-fns named-import style
    - src/lib/schedule/types.ts — confirm ScheduleDay.isWeekStart and ScheduleDay.date are present (no changes needed)
  </read_first>
  <action>
    Two edits to `src/components/schedule/schedule-table.tsx`:

    Edit A — Add `getISOWeek` import. The file currently has no `date-fns` import; add a top-level named import next to the other top-of-file imports (after the existing React/lucide-react/sonner imports, before the local `@/...` imports — match the existing import ordering). Per D-05 and the project pattern, use a single named import: `import { getISOWeek } from "date-fns"` — do NOT use the subpath form `from "date-fns/getISOWeek"`.

    Edit B — Replace the existing hairline separator `<tr>` (currently at lines 277-284 of the file) with a "Viikko X" label row. Concrete target state, per D-01/D-02/D-03/D-04/D-05:
      - Render the row whenever `day.isWeekStart` is truthy. Per D-04, REMOVE the `index > 0` guard so the label also appears for the first week in the view.
      - The row is a single `<tr>` containing a single `<td>` with `colSpan={colCount}` (D-02). `colCount` is already defined at line 251 as `childNames.length + 3` — use the same variable; do not redefine.
      - The `<td>` content is the literal Finnish string `Viikko ` followed by the ISO week number computed inline: `getISOWeek(new Date(day.date))` (D-05).
      - The `<td>` className must be exactly `"px-3 pt-3 pb-1 text-xs text-muted-foreground"` per D-01 + D-03 (replaces the previous `"h-px bg-border"`). Text is left-aligned by default — do not add `text-left`.
      - Remove the `h-px bg-border` hairline classes entirely — the label row IS the separator (D-01). Do not retain a separate hairline `<tr>`.

    Do NOT modify:
      - The outer wrapper `<div>` and its `sm:overflow-y-auto sm:h-[calc(100svh-8rem)]` classes — that change belongs to Plan 02 (UI-02), not this plan.
      - The `useEffect` auto-scroll at lines 85-90 — that change belongs to Plan 02.
      - The today `<tr>` classes — that change belongs to Plan 02.
      - The `<thead>` element and its sticky classes — D-08 says they are correct as-is.
      - Any handler functions (`handleToggle`, `handleClear`, `handleNoteSave`, `handleAssignEmpty`).
      - The `colCount` definition or the `childNames` derivation.
      - The mobile notes row (`<tr className="max-sm:table-row sm:hidden">`) at lines 342-361.

    Keep the row wrapped in the existing `React.Fragment` `{days.map((day, index) => (...))}` block — the parent fragment and `key={day.date}` stay unchanged.
  </action>
  <verify>
    <automated>
      # 1. TypeScript compile + Next build succeeds (catches missing/wrong imports and JSX errors)
      npm run build 2>&1 | tail -40

      # 2. ESLint passes for the modified file (no unused imports, no JSX rule violations)
      npm run lint -- src/components/schedule/schedule-table.tsx 2>&1 | tail -20

      # 3. The getISOWeek named import is present exactly once (anchored to module top, not inside JSX)
      grep -nE '^import \{[^}]*getISOWeek[^}]*\} from "date-fns"' src/components/schedule/schedule-table.tsx
      # Expected: exactly one match

      # 4. No subpath import accidentally introduced
      ! grep -nE 'from "date-fns/' src/components/schedule/schedule-table.tsx
      # Expected: exit 0 (no matches)

      # 5. The hairline separator row is gone — `h-px bg-border` no longer present anywhere in the file
      ! grep -n 'h-px bg-border' src/components/schedule/schedule-table.tsx
      # Expected: exit 0

      # 6. The week label row exists and uses the target className
      grep -nE 'px-3 pt-3 pb-1 text-xs text-muted-foreground' src/components/schedule/schedule-table.tsx
      # Expected: exactly one match

      # 7. The Finnish label literal "Viikko " appears exactly once (in JSX, not a comment)
      grep -v '^\s*//' src/components/schedule/schedule-table.tsx | grep -v '^\s*\*' | grep -cE 'Viikko \{getISOWeek'
      # Expected: 1

      # 8. The `index > 0` guard on the week-start row is removed
      ! grep -nE 'day\.isWeekStart && index > 0' src/components/schedule/schedule-table.tsx
      # Expected: exit 0

      # 9. The colCount variable is still used for the new row (colSpan match)
      grep -nE 'colSpan=\{colCount\}' src/components/schedule/schedule-table.tsx | wc -l
      # Expected: >= 2 (one for the new week label row, one for the existing mobile notes row)
    </automated>
  </verify>
  <acceptance_criteria>
    - src/components/schedule/schedule-table.tsx contains `import { getISOWeek } from "date-fns"` exactly once at module top
    - src/components/schedule/schedule-table.tsx does NOT contain the string `h-px bg-border`
    - src/components/schedule/schedule-table.tsx does NOT contain the substring `day.isWeekStart && index > 0`
    - src/components/schedule/schedule-table.tsx contains the JSX literal `Viikko {getISOWeek(new Date(day.date))}` exactly once
    - src/components/schedule/schedule-table.tsx contains a `<td>` with className `px-3 pt-3 pb-1 text-xs text-muted-foreground` and `colSpan={colCount}`
    - The week label row is gated on `day.isWeekStart` alone (no `index > 0` guard)
    - `npm run build` exits 0
    - `npm run lint -- src/components/schedule/schedule-table.tsx` exits 0
    - No `from "date-fns/..."` subpath imports introduced
  </acceptance_criteria>
  <done>
    The file imports `getISOWeek` via named import from `date-fns`. The previous `h-px bg-border` separator `<tr>` is replaced by a single `<tr>` containing one `<td colSpan={colCount} className="px-3 pt-3 pb-1 text-xs text-muted-foreground">Viikko {getISOWeek(new Date(day.date))}</td>` that renders for every `day.isWeekStart` row (including the first week). Build and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render (browser DOM) | Component renders user-visible text derived from server-fetched `ScheduleDay.date` strings. No new untrusted input crosses this boundary in this plan. |
| date-fns library | Existing third-party dependency (date-fns ^4.1.0); no new packages installed in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `Viikko {getISOWeek(new Date(day.date))}` rendered in JSX | accept | React auto-escapes the interpolated number; `getISOWeek` returns a primitive number (1-53). No untrusted string is rendered. No mitigation required. |
| T-17-02 | Information Disclosure | Week label row | accept | Week numbers are not sensitive data — they are publicly derivable from any calendar date the parents already see in `dayLabel`. |
| T-17-03 | Denial of Service | `new Date(day.date)` inside JSX render | accept | `day.date` is server-produced ISO date string (already validated server-side in `queries.ts`); the cost of constructing a Date and calling `getISOWeek` for each Monday in a ~12-week window is negligible. |
| T-17-SC | Tampering | npm installs | accept | No new packages installed in this plan; `date-fns` ^4.1.0 is already a project dependency. No Package Legitimacy Audit required because no install task is present. |
</threat_model>

<verification>
- Build (`npm run build`) succeeds and produces a production bundle.
- Lint (`npm run lint -- src/components/schedule/schedule-table.tsx`) is clean.
- A `<tr>` containing `Viikko {getISOWeek(new Date(day.date))}` renders above every Monday row in the schedule, including the first week of the view window.
- The previous `h-px bg-border` hairline `<tr>` is gone — no duplicate separator artifacts visible.
</verification>

<success_criteria>
- UI-01 functionally complete at the component level: ScheduleTable emits a single "Viikko N" label row keyed off `day.isWeekStart` for every week in the rendered range, using `getISOWeek` from `date-fns`, with `text-xs text-muted-foreground` typography and `colSpan={colCount}`.
- `must_haves.truths` all true as a code+render fact (visual verification deferred to Plan 03 checkpoint).
- No regression in handler/render logic elsewhere in `schedule-table.tsx`.
</success_criteria>

<output>
Create `.planning/phases/17-schedule-table-structure/17-01-SUMMARY.md` when done.
</output>
