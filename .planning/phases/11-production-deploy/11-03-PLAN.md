---
phase: 11-production-deploy
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified: []
autonomous: false
requirements:
  - DPLY-01
  - DPLY-02
  - DPLY-03
user_setup:
  - service: vercel
    why: "Production hosting; auto-deploys main branch; env var injection at build and runtime"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials (existing client; same as local .env.local)"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials"
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string. Use POOLER (port 6543) with ?pgbouncer=true suffix (D-10)"
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL (https://wsdrguowmcjyfrsjsywn.supabase.co)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role secret (Phase 8 D-11)"
      - name: PARENT_FATHER_EMAIL
        source: ".env.local"
      - name: PARENT_MOTHER_EMAIL
        source: ".env.local"
      - name: APP_FATHER_NAME
        source: ".env.local"
      - name: APP_FATHER_EMAIL
        source: ".env.local"
      - name: APP_FATHER_CALENDAR_ID
        source: ".env.local"
      - name: APP_MOTHER_NAME
        source: ".env.local"
      - name: APP_MOTHER_EMAIL
        source: ".env.local"
      - name: APP_MOTHER_CALENDAR_ID
        source: ".env.local"
      - name: APP_CHILDREN
        source: ".env.local"
      - name: APP_START_DATE
        source: ".env.local"
      - name: APP_FIRST_PARENT
        source: ".env.local"
      - name: APP_CALENDAR_OWNER_EMAIL
        source: ".env.local"
    dashboard_config:
      - task: "Confirm project is linked to the GitHub repo so main branch auto-deploys"
        location: "Vercel Dashboard -> vuoroasuminen project -> Settings -> Git"
  - service: google_cloud_console
    why: "Authorize Supabase's auth callback URL so Google OAuth redirects succeed in production (DPLY-02)"
    env_vars: []
    dashboard_config:
      - task: "Add https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback to Authorized redirect URIs on the OAuth 2.0 Client ID used by this app"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> [client ID] -> Authorized redirect URIs"
  - service: supabase_auth
    why: "Authorize the Vercel production URL as a valid post-auth redirect target so signed-in users land on the app (DPLY-03)"
    env_vars: []
    dashboard_config:
      - task: "Set Site URL to https://vuoroasuminen.vercel.app (or the actual Vercel-assigned URL if different)"
        location: "Supabase Dashboard -> Project (wsdrguowmcjyfrsjsywn) -> Authentication -> URL Configuration -> Site URL"
      - task: "Add https://vuoroasuminen.vercel.app/** to Redirect URLs allowlist"
        location: "Supabase Dashboard -> Project (wsdrguowmcjyfrsjsywn) -> Authentication -> URL Configuration -> Redirect URLs"

must_haves:
  truths:
    - "Visiting the production URL in a browser loads the app's home page (DPLY-01)"
    - "Clicking 'Sign in with Google' on the production URL goes to Google's OAuth consent screen without a redirect_uri_mismatch error (DPLY-02)"
    - "Completing Google OAuth on the production URL returns the user to the production app signed in (DPLY-03)"
    - "All env vars listed in .env.example are set in Vercel production environment"
    - "Vercel build does NOT silently succeed when env vars are missing — Plan 01 fix is honored (sanity check)"
    - "Both parents (father + mother) can sign in on the production URL and reach the schedule view"
  artifacts:
    - path: "(no new code files)"
      provides: "This plan is operational — code changes are in Plans 01 and 02. This plan deploys those changes and configures external services."
      contains: "N/A"
  key_links:
    - from: "Google Cloud Console OAuth client"
      to: "Supabase auth server callback (https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback)"
      via: "Authorized redirect URIs entry"
      pattern: "wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback"
    - from: "Supabase Auth Site URL setting"
      to: "https://vuoroasuminen.vercel.app (or actual deployed URL)"
      via: "Supabase Dashboard URL Configuration"
      pattern: "vercel.app"
    - from: "Vercel production env vars"
      to: "Build-time generate-app-config.js + runtime Supabase/Drizzle"
      via: "vercel env add ... production CLI"
      pattern: "vercel env add"
---

<objective>
Deploy the app to Vercel production, configure Google Cloud Console and Supabase Auth for the production domain, set every required env var in Vercel, and verify end-to-end sign-in on the production URL.

Purpose: DPLY-01, DPLY-02, DPLY-03 — the code is ready (Plans 01 and 02), but the app is not yet running anywhere a parent can use it. This plan does the operational work: initial deploy to obtain the URL, register that URL with Supabase, register Supabase's callback URL with Google Cloud Console, set all env vars, and verify sign-in works end-to-end.

Output: A live production URL (https://vuoroasuminen.vercel.app or Vercel-assigned suffix) at which both parents can sign in with Google and reach the schedule view.

NOTE: This plan is necessarily NOT fully autonomous. Three operations require human action:
1. Authentication into Vercel CLI (if not already logged in) and confirming the project link.
2. Entering secret values into `vercel env add` prompts (secrets must not pass through shell history or git).
3. Editing OAuth dashboard settings in Google Cloud Console and Supabase Dashboard (no scriptable API for these in our setup).
Every checkpoint below is paired with the maximum automation that is possible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-deploy/11-CONTEXT.md
@.planning/phases/11-production-deploy/11-RESEARCH.md
@.planning/phases/11-production-deploy/11-PATTERNS.md
@.planning/phases/11-production-deploy/11-01-PLAN.md
@.planning/phases/11-production-deploy/11-02-PLAN.md
@.env.example
@.env.local
@.vercel/project.json

<interfaces>
<!-- Operational reference values -->

Supabase project:
- project ref: `wsdrguowmcjyfrsjsywn`
- auth callback URL (Supabase's own server, what Google must authorize): `https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback`

Vercel project:
- linked already in .vercel/project.json (projectId: prj_g67bSccUpUtIKGZNjnYDPVQUh0X6, projectName: vuoroasuminen)
- expected URL: `https://vuoroasuminen.vercel.app` (D-05) — Vercel may append a suffix if the project name was taken globally; first deploy reveals the actual URL

Production DATABASE_URL pattern (D-10):
```
postgresql://postgres.wsdrguowmcjyfrsjsywn:[password]@aws-1-eu-north-1.pooler.supabase.com:6543/postgres?pgbouncer=true
```
Use the POOLER (port 6543) with `?pgbouncer=true`. Local `.env.local` uses port 5432 — keep that local, do NOT copy it to Vercel.

Complete list of env vars to set in Vercel production (18 vars):
| # | Var Name | Source |
|---|----------|--------|
| 1 | GOOGLE_CLIENT_ID | .env.local |
| 2 | GOOGLE_CLIENT_SECRET | .env.local |
| 3 | DATABASE_URL | Supabase pooler URL (D-10 pattern, NOT .env.local) |
| 4 | NEXT_PUBLIC_SUPABASE_URL | .env.local |
| 5 | NEXT_PUBLIC_SUPABASE_ANON_KEY | .env.local |
| 6 | SUPABASE_SERVICE_ROLE_KEY | Supabase Dashboard -> Settings -> API -> service_role secret |
| 7 | PARENT_FATHER_EMAIL | .env.local |
| 8 | PARENT_MOTHER_EMAIL | .env.local |
| 9 | APP_FATHER_NAME | .env.local |
| 10 | APP_FATHER_EMAIL | .env.local |
| 11 | APP_FATHER_CALENDAR_ID | .env.local |
| 12 | APP_MOTHER_NAME | .env.local |
| 13 | APP_MOTHER_EMAIL | .env.local |
| 14 | APP_MOTHER_CALENDAR_ID | .env.local |
| 15 | APP_CHILDREN | .env.local |
| 16 | APP_START_DATE | .env.local |
| 17 | APP_FIRST_PARENT | .env.local |
| 18 | APP_CALENDAR_OWNER_EMAIL | .env.local |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Vercel CLI auth and project link</name>
  <files>(no file modifications — read-only checks)</files>
  <read_first>
    - /Users/jarno/src/vuoroasuminen/.vercel/project.json (confirm projectId and projectName)
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-RESEARCH.md (section "Vercel Project Status" — confirms the link already exists)
  </read_first>
  <action>
    Verify the local environment is ready to deploy. Run these commands and capture results:

    Step 1 — Confirm Vercel CLI is installed and version is current:
    ```bash
    vercel --version
    ```
    Expected: `54.1.0` or newer.

    Step 2 — Confirm logged-in identity:
    ```bash
    vercel whoami
    ```
    Expected: A username/email (NOT an auth error). If this returns "Not authenticated" or similar, STOP this task and create a sub-checkpoint asking the user to run `vercel login` interactively, then resume.

    Step 3 — Confirm project link details match `.vercel/project.json`:
    ```bash
    cat /Users/jarno/src/vuoroasuminen/.vercel/project.json
    ```
    Expected: contains `"projectId": "prj_g67bSccUpUtIKGZNjnYDPVQUh0X6"` and `"projectName": "vuoroasuminen"`.

    Step 4 — Confirm the linked project is reachable:
    ```bash
    cd /Users/jarno/src/vuoroasuminen && vercel project ls 2>&1 | head -30
    ```
    Expected: list of projects on the account includes `vuoroasuminen`.

    Do NOT run `vercel link` again if a project is already linked — re-linking can change the projectId.
    Do NOT run `vercel deploy` in this task — deploy is Task 2.
    Do NOT pass `--token` flags with secrets; rely on the user's existing Vercel CLI session.
  </action>
  <verify>
    <automated>vercel --version && vercel whoami && grep -q 'prj_g67bSccUpUtIKGZNjnYDPVQUh0X6' /Users/jarno/src/vuoroasuminen/.vercel/project.json && grep -q '"projectName":\s*"vuoroasuminen"' /Users/jarno/src/vuoroasuminen/.vercel/project.json && echo "OK: CLI ready, project linked"</automated>
  </verify>
  <acceptance_criteria>
    - `vercel --version` exits 0 and prints a version string
    - `vercel whoami` exits 0 and prints a non-empty identity (not "Not authenticated")
    - `.vercel/project.json` contains both the expected projectId and projectName values (verified by grep above)
    - `vercel project ls` exits 0 and the output includes a row for `vuoroasuminen`
    - If `vercel whoami` fails, a checkpoint is raised asking the user to `vercel login` before continuing
  </acceptance_criteria>
  <done>Vercel CLI is authenticated, version is current, and the local project link matches the expected projectId/projectName.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Initial deploy to discover production URL</name>
  <files>(no code files — operational deploy that returns a Vercel URL)</files>
  <read_first>
    - /Users/jarno/src/vuoroasuminen/.vercel/project.json (confirm we are still linked to the same project)
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-RESEARCH.md (section "Common Pitfalls" — Pitfall 2 about Vercel URL suffixes)
  </read_first>
  <action>
    Run `vercel deploy --prod` to trigger the first production build and obtain the actual public URL Vercel assigns.

    Exact command:
    ```bash
    cd /Users/jarno/src/vuoroasuminen && vercel deploy --prod 2>&1 | tee /tmp/11-03-initial-deploy.log
    ```

    The build will EXIT 1 in `scripts/generate-app-config.js` because no APP_* env vars are set in Vercel production yet. This is EXPECTED behavior that confirms Plan 01's fix is working. Vercel still prints the URL it would have used for a successful build. That URL is what we record as `PRODUCTION_URL` and reuse in Tasks 4-7.

    Look for a line of the form:
    ```
    Inspect: https://vercel.com/<account>/vuoroasuminen/<deployment-id> [<time>]
    Production: https://vuoroasuminen.vercel.app [<time>]
    ```
    or
    ```
    Production: https://vuoroasuminen-<hash>.vercel.app [<time>]
    ```

    The "Production: https://..." line contains the URL. Extract that URL exactly and present it to the user in the resume signal.

    Do NOT set env vars in this task — Task 3 owns env setup. The deploy is expected to fail at build time, and that is fine.
    Do NOT retry the deploy with `--force` or pass additional flags to suppress the build failure — we WANT it to fail loudly here because that confirms Plan 01.
    Do NOT use a preview deploy (`vercel deploy` without `--prod`) — we need the production URL, not a preview URL.
  </action>
  <verify>
    <automated>grep -qE 'Production:\s+https?://[a-zA-Z0-9.-]+\.vercel\.app' /tmp/11-03-initial-deploy.log && echo "OK: production URL printed" || { echo "FAIL: no production URL in deploy log"; tail -50 /tmp/11-03-initial-deploy.log; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/11-03-initial-deploy.log` exists and contains a line matching `Production:\s+https?://[a-zA-Z0-9.-]+\.vercel\.app`
    - The user has captured the exact URL printed by Vercel CLI and recorded it as `PRODUCTION_URL` for Tasks 4-7
    - The deploy log MAY contain `FATAL: missing required env vars` (expected at this stage — Plan 01's exit-1 fix is firing)
    - The deploy log does NOT contain `Not authenticated` or `Project not linked` (those would indicate Task 1 was skipped)
  </acceptance_criteria>
  <what-built>
    A production deploy has been triggered. The build is expected to fail in generate-app-config.js (env vars not set yet — Plan 01's exit-1 fix). Vercel CLI prints the URL that the production deployment is assigned to, even when the build fails. We capture that URL for use in Tasks 4-7.
  </what-built>
  <how-to-verify>
    1. Run: `cd /Users/jarno/src/vuoroasuminen && vercel deploy --prod 2>&1 | tee /tmp/11-03-initial-deploy.log`
    2. The CLI prints a `Production: https://...vercel.app` line. Copy that URL exactly.
    3. Open the URL in a browser — you will see a Vercel "build failed" or "deployment error" page. This is EXPECTED (env vars missing; Plan 01's exit-1 is working).
    4. Reply with the URL and `approved` to continue.
  </how-to-verify>
  <resume-signal>
    Reply with: `PRODUCTION_URL=https://...` (the exact URL) AND `approved` to continue to env var setup.
    Reply with `reject: <reason>` if the deploy did not start (e.g., auth failure, not linked).
  </resume-signal>
  <done>The Vercel-assigned PRODUCTION_URL is captured (recorded in the user's reply) and the user has approved continuing to Task 3.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Set all production env vars via vercel env add</name>
  <files>(no code files — Vercel platform state change)</files>
  <read_first>
    - /Users/jarno/src/vuoroasuminen/.env.local (source of 15 of the 18 values)
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-RESEARCH.md (section "Complete Env Var Checklist for Vercel" — confirms the 18-var list)
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-CONTEXT.md (D-08 — interactive entry per var; D-10 — pooler DATABASE_URL pattern)
  </read_first>
  <action>
    Per D-08, set each of the 18 required Vercel production env vars interactively via `vercel env add VAR_NAME production`. Values must be entered at the prompt — they MUST NOT appear in shell history or git.

    Run these 18 commands sequentially, entering each value when prompted:

    ```bash
    cd /Users/jarno/src/vuoroasuminen
    vercel env add GOOGLE_CLIENT_ID production
    vercel env add GOOGLE_CLIENT_SECRET production
    vercel env add DATABASE_URL production
    vercel env add NEXT_PUBLIC_SUPABASE_URL production
    vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY production
    vercel env add SUPABASE_SERVICE_ROLE_KEY production
    vercel env add PARENT_FATHER_EMAIL production
    vercel env add PARENT_MOTHER_EMAIL production
    vercel env add APP_FATHER_NAME production
    vercel env add APP_FATHER_EMAIL production
    vercel env add APP_FATHER_CALENDAR_ID production
    vercel env add APP_MOTHER_NAME production
    vercel env add APP_MOTHER_EMAIL production
    vercel env add APP_MOTHER_CALENDAR_ID production
    vercel env add APP_CHILDREN production
    vercel env add APP_START_DATE production
    vercel env add APP_FIRST_PARENT production
    vercel env add APP_CALENDAR_OWNER_EMAIL production
    ```

    Per-var value sources:

    - **GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET**: copy from `.env.local` lines `GOOGLE_CLIENT_ID=` and `GOOGLE_CLIENT_SECRET=`.
    - **DATABASE_URL** (DO NOT copy from .env.local): construct the POOLER URL using the password from `.env.local`'s DATABASE_URL value:
      `postgresql://postgres.wsdrguowmcjyfrsjsywn:[PASSWORD_FROM_ENV_LOCAL]@aws-1-eu-north-1.pooler.supabase.com:6543/postgres?pgbouncer=true`
      The password is the substring in `.env.local`'s DATABASE_URL between `postgres.<ref>:` and `@`. Port MUST be 6543 (NOT 5432). The query string `?pgbouncer=true` is REQUIRED per D-10 to prevent connection exhaustion on Vercel serverless functions.
    - **NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY**: copy from `.env.local`.
    - **SUPABASE_SERVICE_ROLE_KEY**: this is NOT in `.env.local` (STATE.md tracks it as a pending todo). Get the value from Supabase Dashboard -> Settings -> API -> `service_role` secret (the long JWT-looking string under "Project API keys"). After getting it, also add it to `.env.local` so local admin operations work (this is a side benefit, not the main goal of Task 3).
    - **PARENT_FATHER_EMAIL, PARENT_MOTHER_EMAIL**: copy from `.env.local`.
    - **APP_FATHER_NAME, APP_FATHER_EMAIL, APP_FATHER_CALENDAR_ID, APP_MOTHER_NAME, APP_MOTHER_EMAIL, APP_MOTHER_CALENDAR_ID**: copy from `.env.local`. If your `.env.local` only has `PARENT_FATHER_*` and `PARENT_MOTHER_*` (the older names), copy those values into the corresponding `APP_*` Vercel vars.
    - **APP_CHILDREN, APP_START_DATE, APP_FIRST_PARENT**: copy from `.env.local`.
    - **APP_CALENDAR_OWNER_EMAIL**: copy from `.env.local` if present. If not present, use the parent email that owns the GCal token (Phase 8 D-11 — the parent who completed local sign-in and granted GCal access).

    After all 18 commands complete, verify the list:
    ```bash
    vercel env ls production | tee /tmp/11-03-envls.log
    ```

    Count expected names present in the output:
    ```bash
    COUNT=$(grep -cE '^(GOOGLE_CLIENT_ID|GOOGLE_CLIENT_SECRET|DATABASE_URL|NEXT_PUBLIC_SUPABASE_URL|NEXT_PUBLIC_SUPABASE_ANON_KEY|SUPABASE_SERVICE_ROLE_KEY|PARENT_FATHER_EMAIL|PARENT_MOTHER_EMAIL|APP_FATHER_NAME|APP_FATHER_EMAIL|APP_FATHER_CALENDAR_ID|APP_MOTHER_NAME|APP_MOTHER_EMAIL|APP_MOTHER_CALENDAR_ID|APP_CHILDREN|APP_START_DATE|APP_FIRST_PARENT|APP_CALENDAR_OWNER_EMAIL)\b' /tmp/11-03-envls.log)
    echo "Vars present: $COUNT / 18"
    ```

    Expected: 18.

    Do NOT pass values via `--value` or `--token` flags — that defeats D-08's "values never touch shell history" goal.
    Do NOT use `vercel env add VAR_NAME` without `production` suffix — that would target preview/dev environments.
    Do NOT use `vercel env pull` to copy from another environment — set each value explicitly so production values are intentional.
  </action>
  <verify>
    <automated>vercel env ls production 2>&1 | tee /tmp/11-03-envls.log >/dev/null; COUNT=$(grep -cE '^(GOOGLE_CLIENT_ID|GOOGLE_CLIENT_SECRET|DATABASE_URL|NEXT_PUBLIC_SUPABASE_URL|NEXT_PUBLIC_SUPABASE_ANON_KEY|SUPABASE_SERVICE_ROLE_KEY|PARENT_FATHER_EMAIL|PARENT_MOTHER_EMAIL|APP_FATHER_NAME|APP_FATHER_EMAIL|APP_FATHER_CALENDAR_ID|APP_MOTHER_NAME|APP_MOTHER_EMAIL|APP_MOTHER_CALENDAR_ID|APP_CHILDREN|APP_START_DATE|APP_FIRST_PARENT|APP_CALENDAR_OWNER_EMAIL)\b' /tmp/11-03-envls.log); test "$COUNT" -eq 18 && echo "OK: 18/18 vars present" || { echo "FAIL: $COUNT/18 vars present"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `vercel env ls production` exit status is 0
    - The output (captured in `/tmp/11-03-envls.log`) contains a line for EACH of the 18 var names in the table above (verified by the grep count returning 18)
    - No var has a `(missing)` or `(empty)` marker
    - DATABASE_URL value in Vercel uses port 6543 and the `?pgbouncer=true` suffix (this cannot be verified via `env ls` which redacts values — verify visually during `vercel env add DATABASE_URL production` by re-typing the URL into a separate scratch file and confirming pattern match: `:6543/postgres\?pgbouncer=true`)
  </acceptance_criteria>
  <what-built>
    All 18 required Vercel production env vars are set (15 from `.env.local`, DATABASE_URL with pooler suffix per D-10, SUPABASE_SERVICE_ROLE_KEY from Supabase Dashboard).
  </what-built>
  <how-to-verify>
    Run `vercel env ls production` — output must show all 18 var names from the table in the &lt;interfaces&gt; block above. The automated check counts matches with grep and prints `OK: 18/18 vars present` on success.
  </how-to-verify>
  <resume-signal>
    Reply `approved` once `vercel env ls production` shows all 18 vars present (the automated check above prints `OK: 18/18`).
    Reply `reject: <reason>` if any var fails to add or the count is below 18.
  </resume-signal>
  <done>All 18 production env vars are confirmed set in Vercel; `vercel env ls production` exit code 0; grep count of expected names returns 18.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Configure Google Cloud Console authorized redirect URI (DPLY-02)</name>
  <files>(no code files — Google Cloud Console state change)</files>
  <read_first>
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-CONTEXT.md (D-06 — the exact callback URL to register)
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-RESEARCH.md (section "Pitfall 1" — wrong URI causes redirect_uri_mismatch)
  </read_first>
  <action>
    Add Supabase's auth server callback URL as an Authorized redirect URI on the Google OAuth 2.0 Client used by this app. Per D-06 the exact value is:

    `https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback`

    Steps:

    1. Open: https://console.cloud.google.com/
    2. Select the GCP project that owns the OAuth client matching `GOOGLE_CLIENT_ID` (the same client ID just entered into Vercel in Task 3).
    3. Navigate: APIs &amp; Services -> Credentials.
    4. Under "OAuth 2.0 Client IDs", click the row whose Client ID matches `GOOGLE_CLIENT_ID`.
    5. Scroll to "Authorized redirect URIs".
    6. Click "+ ADD URI" and paste exactly: `https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback`
    7. Click "Save" at the bottom of the page.
    8. Confirm the URI appears in the saved list after the page reloads.

    Do NOT add `https://vuoroasuminen.vercel.app/auth/callback` (the Next.js route) — that is the wrong URI per D-06 and Pitfall 1. Supabase handles the OAuth code exchange on ITS server (`wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback`), then redirects to the Next.js app afterwards.
    Do NOT add wildcard subdomains for the Supabase URL — register the exact callback URL only.
    Do NOT remove the existing local dev URI (e.g., `http://localhost:54321/auth/v1/callback` or similar, if present) — local sign-in must continue to work.
    Do NOT create a new OAuth client; use the existing one already paired with `GOOGLE_CLIENT_ID`.

    There is no programmatic verification path for this step — the Google Cloud Console OAuth client config is not exposed via a public read API in our setup. Human visual confirmation in the dashboard is required.
  </action>
  <verify>
    <automated>echo "MANUAL — verify via dashboard: https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback present in Google Cloud Console -> OAuth client -> Authorized redirect URIs"</automated>
  </verify>
  <acceptance_criteria>
    - The Authorized redirect URIs list on the OAuth 2.0 Client (matching `GOOGLE_CLIENT_ID`) contains exactly the string `https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback`
    - The dashboard "Save" action completed without error (no `Invalid URI format` rejection)
    - The pre-existing local dev redirect URI is still present (not deleted)
    - The user has visually confirmed the URI appears in the saved list AFTER the page reload (i.e., the save persisted)
  </acceptance_criteria>
  <what-built>
    Google Cloud Console OAuth 2.0 Client now lists Supabase's auth server callback URL (`https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback`) as an Authorized redirect URI, allowing Supabase to complete the OAuth code exchange for production sign-ins.
  </what-built>
  <how-to-verify>
    Open https://console.cloud.google.com/apis/credentials, click the OAuth client matching GOOGLE_CLIENT_ID, scroll to "Authorized redirect URIs", confirm `https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback` is listed (post-save).
  </how-to-verify>
  <resume-signal>
    Reply `approved` once the Supabase callback URI is listed in Authorized redirect URIs and saved.
    Reply `reject: <reason>` if the OAuth client cannot be located or the save fails.
  </resume-signal>
  <done>The Supabase auth server callback URL is registered as an Authorized redirect URI on the Google OAuth 2.0 Client used by this app, and the save has persisted.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Configure Supabase Auth Site URL and Redirect allowlist (DPLY-03)</name>
  <files>(no code files — Supabase Dashboard state change)</files>
  <read_first>
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-CONTEXT.md (D-07 — Site URL + allowlist configuration)
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-RESEARCH.md (section "Pitfall 2" — wrong Site URL causes post-auth redirect failure)
  </read_first>
  <action>
    Per D-07, configure Supabase Auth so the production URL is accepted as a valid post-OAuth redirect target.

    Use the PRODUCTION_URL captured from Task 2 (e.g., `https://vuoroasuminen.vercel.app` — substitute the actual URL captured if Vercel assigned a suffix).

    Steps:

    1. Open: https://supabase.com/dashboard/project/wsdrguowmcjyfrsjsywn
    2. Navigate: Authentication -> URL Configuration.
    3. Site URL field — set to the PRODUCTION_URL exactly (e.g., `https://vuoroasuminen.vercel.app`). Save.
    4. Redirect URLs allowlist — add `https://vuoroasuminen.vercel.app/**` (or `<PRODUCTION_URL>/**` if the actual URL differs). The `/**` wildcard allows any callback path under the production host.
    5. Save.
    6. Confirm both settings persist after page reload.

    Do NOT remove the existing local dev entry (e.g., `http://localhost:3000/**`) from the Redirect URLs — local sign-in must keep working.
    Do NOT set Site URL to the Supabase callback URL (`https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback`) — Site URL is the APP's URL, not Supabase's auth server.
    Do NOT use a literal `*` wildcard for the Site URL — it must be a single exact URL.
    Do NOT change other Auth settings (providers, email templates, etc.) — Phase 11 scope is URL configuration only.

    There is no programmatic verification path — Supabase Auth URL config is not exposed via a public CLI in our setup. Human visual confirmation is required.
  </action>
  <verify>
    <automated>echo "MANUAL — verify via Supabase Dashboard: Site URL = PRODUCTION_URL; Redirect URLs allowlist contains PRODUCTION_URL/**"</automated>
  </verify>
  <acceptance_criteria>
    - Supabase Dashboard -> Authentication -> URL Configuration -> "Site URL" displays the exact PRODUCTION_URL captured in Task 2
    - Supabase Dashboard -> Authentication -> URL Configuration -> "Redirect URLs" contains an entry matching `<PRODUCTION_URL>/**` (with the `/**` wildcard suffix)
    - The pre-existing local dev redirect entry (e.g., `http://localhost:3000/**`) is STILL present (not removed)
    - The user has visually confirmed both settings persisted AFTER a page reload
  </acceptance_criteria>
  <what-built>
    Supabase Auth now accepts the production URL as a valid post-OAuth redirect target — Site URL points to the production deployment and the production URL (with `/**` suffix) is in the Redirect URLs allowlist.
  </what-built>
  <how-to-verify>
    Open https://supabase.com/dashboard/project/wsdrguowmcjyfrsjsywn -> Authentication -> URL Configuration. Confirm Site URL field shows PRODUCTION_URL, and the Redirect URLs list contains an entry matching `<PRODUCTION_URL>/**`. Reload the page and confirm the values persist.
  </how-to-verify>
  <resume-signal>
    Reply `approved` once Site URL and Redirect URLs are saved with the PRODUCTION_URL value and the settings persist after page reload.
    Reply `reject: <reason>` if the dashboard rejects the URL or the save does not persist.
  </resume-signal>
  <done>Supabase Site URL is set to PRODUCTION_URL and `<PRODUCTION_URL>/**` is in the Redirect URLs allowlist; both persist after reload.</done>
</task>

<task type="auto">
  <name>Task 6: Trigger production redeploy with env vars now set</name>
  <files>(no file modifications — triggers a Vercel build only)</files>
  <read_first>
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-RESEARCH.md (section "Common Pitfalls" — Pitfalls 2, 4, 6 for diagnosing build failures)
  </read_first>
  <action>
    With all 18 env vars set in Vercel production (Task 3) and external services configured (Tasks 4 and 5), trigger a clean production redeploy. The build should now succeed: `generate-app-config.js` finds all vars, writes `src/config/app.ts`, `next build` completes, and `proxy.ts` (Plan 02) is recognized as the Next.js 16 proxy convention file.

    Run:
    ```bash
    cd /Users/jarno/src/vuoroasuminen && vercel deploy --prod 2>&1 | tee /tmp/11-03-deploy.log
    ```

    Expected log signals:
    - `generate-app-config: wrote src/config/app.ts` appears (build script succeeded)
    - `next build` completes (no `Compilation failed` errors)
    - No `FATAL: missing required env vars` in the log
    - The CLI prints `Production: <PRODUCTION_URL>` matching Task 2's URL
    - The deployed URL responds with the app, not a Vercel build-failure page

    If the build fails, diagnose:
    - `FATAL: missing required env vars: ...` → one or more vars from Task 3 are missing. Run `vercel env ls production` and re-add the missing names.
    - `Cannot find module 'dotenv'` → Plan 01 Task 1 was not applied on the deployed branch. Verify `package.json` on the deployed commit has `dotenv` in `devDependencies`.
    - `connection refused` to database during build → DATABASE_URL is wrong. Verify it uses port 6543 with `?pgbouncer=true` (D-10).
    - `Cannot find module '@/middleware'` → some import was broken by Plan 02. Re-check the audit from Plan 02 Task 1.

    Do NOT add new env vars in this task — Task 3 owns env setup.
    Do NOT modify code to "fix" a missing var — fix the env var, not the code.
    Do NOT pass `--force` or `--skip-build` — we WANT the build to run cleanly.
  </action>
  <verify>
    <automated>cd /Users/jarno/src/vuoroasuminen && vercel deploy --prod 2>&1 | tee /tmp/11-03-deploy.log; DEPLOY_EXIT=${PIPESTATUS[0]}; test $DEPLOY_EXIT -eq 0 || { echo "FAIL: vercel deploy exit $DEPLOY_EXIT"; tail -50 /tmp/11-03-deploy.log; exit 1; }; grep -q 'wrote src/config/app.ts' /tmp/11-03-deploy.log || { echo "FAIL: generate-app-config did not run successfully"; exit 1; }; ! grep -qi 'FATAL: missing required' /tmp/11-03-deploy.log || { echo "FAIL: env vars still missing"; exit 1; }; grep -qE 'Production:\s+https?://[a-zA-Z0-9.-]+\.vercel\.app' /tmp/11-03-deploy.log || { echo "FAIL: no Vercel URL in output"; exit 1; }; echo "OK: deploy succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `vercel deploy --prod` exits with status 0
    - `/tmp/11-03-deploy.log` contains `wrote src/config/app.ts` (Plan 01 build script succeeded)
    - `/tmp/11-03-deploy.log` does NOT contain `FATAL: missing required env vars`
    - `/tmp/11-03-deploy.log` does NOT contain `Cannot find module 'dotenv'`
    - `/tmp/11-03-deploy.log` does NOT contain `Cannot find module '@/middleware'` (Plan 02 imports preserved)
    - `/tmp/11-03-deploy.log` does NOT contain a case-insensitive match for `middleware.ts.*deprecat` (Plan 02 rename effective)
    - `/tmp/11-03-deploy.log` contains a `Production:\s+https?://[a-zA-Z0-9.-]+\.vercel\.app` line and the URL matches PRODUCTION_URL from Task 2
    - `curl -sI <PRODUCTION_URL>` returns HTTP 200 or 307 (200 = home page; 307 = proxy redirecting to "/" — both acceptable)
  </acceptance_criteria>
  <done>Production deploy completes successfully; deployed URL responds with the app; no env-var, dotenv, middleware, or import errors in the build log.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Verify end-to-end sign-in on production (DPLY-01, DPLY-02, DPLY-03)</name>
  <files>(no code files — browser-driven UAT)</files>
  <read_first>
    - /Users/jarno/src/vuoroasuminen/.planning/phases/11-production-deploy/11-RESEARCH.md (section "Common Pitfalls" — Pitfalls 1, 2, 5 are the failure signatures to watch for during sign-in)
  </read_first>
  <action>
    Manually verify the production sign-in flow for both parents in the browser. This is the closing UAT for DPLY-01 (URL loads), DPLY-02 (Google OAuth completes), DPLY-03 (Supabase accepts the production redirect).

    Steps (perform in a fresh private/incognito browser window to avoid local session leakage):

    1. **Page load (DPLY-01)**: Open PRODUCTION_URL. Confirm the app's landing/sign-in page renders (NOT a Vercel "build error" page, NOT a 500 error).

    2. **Father sign-in (DPLY-02 + DPLY-03)**:
       - Click "Sign in with Google".
       - Confirm redirect to Google's OAuth consent screen — there must be NO `Error 400: redirect_uri_mismatch` page. (If there is, return to Task 4 — Supabase callback URL is not registered correctly in Google Cloud Console.)
       - Authenticate as the father account.
       - Confirm redirect back to PRODUCTION_URL signed in — there must be NO Supabase error about redirect URL. (If there is, return to Task 5 — Site URL or allowlist is wrong.)
       - Confirm the schedule view loads (proxy.ts allows the route for authenticated users).

    3. **Mother sign-in (DPLY-02 + DPLY-03)**:
       - Open a new private window (so the father session does not interfere).
       - Repeat step 2 with the mother account.
       - Confirm the same successful flow.

    4. **Smoke check (optional but recommended)**:
       - In one parent's window, edit a schedule cell.
       - In the other parent's window, refresh and confirm the edit is visible.

    Failure signatures and which task to revisit:
    - `Error 400: redirect_uri_mismatch` (from Google) → Task 4 (Supabase callback not in Google OAuth client)
    - "Invalid redirect URL" or similar (from Supabase) → Task 5 (PRODUCTION_URL not in Supabase allowlist)
    - Loops back to landing after sign-in / cannot reach schedule view → proxy.ts may have a bug (Plan 02) or Supabase session cookies not being set; check Vercel function logs
    - `too many connections` error → Task 3 DATABASE_URL is wrong (should be pooler :6543 with ?pgbouncer=true)

    Do NOT click through warnings about "unverified app" if Google's OAuth verification status warns — that is OK for a personal app with two known users, but if Google blocks sign-in entirely, the OAuth verification process must be started (operational task tracked in STATE.md, not a Phase 11 blocker).
  </action>
  <verify>
    <automated>echo "MANUAL — both parents must complete sign-in on PRODUCTION_URL in a browser; resume signal records UAT result"</automated>
  </verify>
  <acceptance_criteria>
    - PRODUCTION_URL responds with the app's home/sign-in page in a browser (not a Vercel build-error page; not a 5xx)
    - Father account completes the Google OAuth flow on PRODUCTION_URL without `redirect_uri_mismatch` or Supabase redirect errors, and lands on the schedule view
    - Mother account completes the same flow in a separate private window with the same result
    - No `redirect_uri_mismatch` errors observed at any point (DPLY-02 verified)
    - No Supabase redirect-allowlist errors observed at any point (DPLY-03 verified)
    - The user explicitly types `approved` in the resume signal AND lists which two accounts succeeded
  </acceptance_criteria>
  <what-built>
    Both parents have successfully signed in on the production URL, confirming DPLY-01 (URL loads), DPLY-02 (Google OAuth works for the production domain), and DPLY-03 (Supabase Auth accepts the production redirect).
  </what-built>
  <how-to-verify>
    Perform the 4-step browser UAT above using two separate private windows. Confirm both parents reach the schedule view after sign-in with no `redirect_uri_mismatch` or Supabase URL-validation errors.
  </how-to-verify>
  <resume-signal>
    Reply `approved` once both parents have successfully signed in on PRODUCTION_URL and reached the schedule view. Include in the reply: "father OK, mother OK" (or note any account that did not succeed).
    Reply `reject: <reason>` describing the error observed (Error code, page text, or screenshot reference).
  </resume-signal>
  <done>Both parents have completed end-to-end Google sign-in on PRODUCTION_URL and reached the protected schedule view; no OAuth or redirect errors observed.</done>
</task>

</tasks>

<verification>
End-to-end checks for the entire plan:

1. **Vercel deploy succeeded**:
   `grep -q 'wrote src/config/app.ts' /tmp/11-03-deploy.log && ! grep -qi 'FATAL: missing' /tmp/11-03-deploy.log && echo OK || echo FAIL`

2. **All 18 env vars are set in Vercel production**:
   `vercel env ls production 2>&1 | grep -cE '^(GOOGLE_CLIENT_ID|GOOGLE_CLIENT_SECRET|DATABASE_URL|NEXT_PUBLIC_SUPABASE_URL|NEXT_PUBLIC_SUPABASE_ANON_KEY|SUPABASE_SERVICE_ROLE_KEY|PARENT_FATHER_EMAIL|PARENT_MOTHER_EMAIL|APP_FATHER_NAME|APP_FATHER_EMAIL|APP_FATHER_CALENDAR_ID|APP_MOTHER_NAME|APP_MOTHER_EMAIL|APP_MOTHER_CALENDAR_ID|APP_CHILDREN|APP_START_DATE|APP_FIRST_PARENT|APP_CALENDAR_OWNER_EMAIL)\b'` returns 18.

3. **Production URL responds**:
   `curl -sI <PRODUCTION_URL>` returns HTTP 200 or 307.

4. **Both parents signed in on production** (Task 7 human verify).

5. **No middleware deprecation warning** in the production build log:
   `! grep -qi 'middleware.ts.*deprecat' /tmp/11-03-deploy.log && echo OK || echo FAIL`
</verification>

<success_criteria>
- PRODUCTION_URL is captured and recorded in the phase summary (DPLY-01).
- Google Cloud Console has `https://wsdrguowmcjyfrsjsywn.supabase.co/auth/v1/callback` in Authorized redirect URIs (DPLY-02).
- Supabase Auth has the production URL as Site URL and `<PRODUCTION_URL>/**` in the Redirect allowlist (DPLY-03).
- All 18 env vars are set in Vercel production env.
- Both parents have completed sign-in on the production URL without errors.
- The schedule view loads for both parents after sign-in.
- No middleware-deprecation warning in the production build log (DPLY-05 carry-forward sanity check).
</success_criteria>

<output>
After completion, create `.planning/phases/11-production-deploy/11-03-SUMMARY.md` per the standard summary template, including:
- The actual PRODUCTION_URL captured in Task 2.
- A list of the 18 env vars confirmed set in Vercel production.
- Confirmation that Google Cloud Console + Supabase Auth dashboards are configured.
- Confirmation that both parents have successfully signed in on the production URL.
</output>
