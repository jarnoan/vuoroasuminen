---
phase: 19-stats-column-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schedule/stats-panel.tsx
autonomous: true
requirements:
  - UI-06

must_haves:
  truths:
    - "StatsPanel child columns are the same width as ScheduleTable child columns"
    - "Alignment holds at 360px (mobile) viewport"
    - "Alignment holds at 1280px (desktop) viewport"
    - "StatsPanel label column uses the same horizontal padding as ScheduleTable date column"
  artifacts:
    - path: "src/components/schedule/stats-panel.tsx"
      provides: "StatsPanel with fixed table layout and matching child column widths"
      contains: "min-w-[72px] sm:min-w-[90px]"
  key_links:
    - from: "src/components/schedule/stats-panel.tsx"
      to: "src/components/schedule/schedule-table.tsx"
      via: "replicated Tailwind classes min-w-[72px] sm:min-w-[90px] on child column th/td"
      pattern: "min-w-\\[72px\\] sm:min-w-\\[90px\\]"
---

<objective>
Apply CSS column-alignment changes to StatsPanel so its child columns visually align with ScheduleTable child columns above it.

Purpose: Satisfies UI-06 — when both ScheduleTable and StatsPanel are on screen (they are direct siblings in schedule-with-realtime.tsx), child columns line up vertically, making the layout visually coherent.

Output: Modified `src/components/schedule/stats-panel.tsx` with `table-layout: fixed`, matching min-widths on child columns, and matching label column padding.
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-stats-column-alignment/19-UI-SPEC.md
@src/components/schedule/stats-panel.tsx
@src/components/schedule/schedule-table.tsx

<interfaces>
<!-- Source of truth for column widths: ScheduleTable (schedule-table.tsx) -->
<!-- Executor must replicate these EXACT class strings; do not paraphrase. -->

ScheduleTable column class strings (verified from src/components/schedule/schedule-table.tsx):

- Date column `<th>` (line 278):
  `px-3 py-2 text-left text-sm font-semibold whitespace-nowrap border-b sticky left-0 z-10 bg-background`
- Date column `<td>` (line 316):
  `px-3 py-2 text-sm whitespace-nowrap font-mono sticky left-0 bg-background ...`
- Child column `<th>` (line 284):
  `px-1 py-2 text-left text-sm font-semibold border-b min-w-[72px] sm:min-w-[90px]`
- Child column `<td>` (line 322):
  `px-1 py-1`

StatsPanel current class strings (from src/components/schedule/stats-panel.tsx):

- Wrapper `<div>` (line 16): `border rounded-lg p-3 mt-4 bg-muted/30 text-sm`
- `<table>` (line 17): `w-full`
- Label `<th>` (line 20): `text-left font-medium pr-2 pb-1`
- Child header `<th>` (line 22): `text-left font-medium px-2 pb-1`
- Label `<td>` father (line 31): `text-blue-700 font-medium pr-2 py-1 align-top`
- Label `<td>` mother (line 43): `text-rose-700 font-medium pr-2 py-1 align-top`
- Child `<td>` father (line 35): `px-2 py-1 align-top`
- Child `<td>` mother (line 47): `px-2 py-1 align-top`
- Separator `<td>` (line 55): `py-1` — colSpan={stats.childStats.length + 1}
- Vapaa `<td>` (lines 62-65): `py-0.5 ...` — colSpan={stats.childStats.length + 1}

Container context — both rendered as siblings inside the same parent in schedule-with-realtime.tsx:
- `<ScheduleTable .../>` then `<StatsPanel .../>` — share parent width, no extra wrapping needed for them to occupy the same horizontal space when each has w-full.

Note: StatsPanel's wrapper `<div>` adds `p-3` padding which makes the StatsPanel table slightly narrower than ScheduleTable. The executor MUST account for this when choosing alignment approach (see action).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply table-layout:fixed and matching column widths to StatsPanel</name>
  <files>src/components/schedule/stats-panel.tsx</files>
  <read_first>
    - src/components/schedule/stats-panel.tsx (current implementation — to see existing JSX and class strings)
    - src/components/schedule/schedule-table.tsx (source of truth for column widths — verify class strings match between the two files exactly)
    - src/components/schedule/schedule-with-realtime.tsx (confirms ScheduleTable and StatsPanel are direct siblings under the same parent — see lines 40 and 48)
    - .planning/phases/19-stats-column-alignment/19-UI-SPEC.md (Layout Alignment Contract section is the binding spec)
  </read_first>
  <behavior>
    - Behavior 1: StatsPanel renders with a `<colgroup>` containing one `<col>` for the label column and N `<col>` elements for the N child columns.
    - Behavior 2: The `<table>` element has `tableLayout: "fixed"` applied via inline style.
    - Behavior 3: Each child column `<th>` and child column `<td>` has both `min-w-[72px]` and `sm:min-w-[90px]` classes (exact match to ScheduleTable).
    - Behavior 4: The label column `<th>` and the label `<td>` for both father and mother rows use horizontal padding `px-3` (matching ScheduleTable date column's `px-3`), replacing the current `pr-2`.
    - Behavior 5: Child column `<th>` and `<td>` use `px-1` (matching ScheduleTable child column padding), replacing the current `px-2`.
    - Behavior 6: The wrapper `<div>` no longer has horizontal `p-3` padding that would offset the table from ScheduleTable's edge; replace `p-3` with `py-3` so vertical padding is kept but horizontal padding is removed. (Vertical padding does not affect column alignment; horizontal padding does.)
    - Behavior 7: The separator row and the Vapaa rows (which use `colSpan={stats.childStats.length + 1}`) continue to span the full table width unchanged.
    - Behavior 8: All existing copy ("pv", "yksin", parent names, "vkl"), all existing colors (`text-blue-700`, `text-rose-700`, `text-muted-foreground`), and all existing data bindings (computeStats output) are preserved.
  </behavior>
  <action>
    Modify `src/components/schedule/stats-panel.tsx` to align child columns with ScheduleTable per UI-SPEC.md Layout Alignment Contract:

    1. Update the wrapper `<div>` className from `border rounded-lg p-3 mt-4 bg-muted/30 text-sm` to `border rounded-lg py-3 mt-4 bg-muted/30 text-sm` (replace `p-3` with `py-3` to remove horizontal padding so StatsPanel's table width matches ScheduleTable's table width edge-to-edge inside the shared parent).

    2. Add `style={{ tableLayout: "fixed" }}` to the `<table>` element (keep `className="w-full"`).

    3. Insert a `<colgroup>` as the first child of `<table>`, before `<thead>`. The colgroup must contain:
       - One `<col />` for the label column (no width style — content-driven via fixed layout fallback to declared widths on cells).
       - N `<col />` elements (where N = `stats.childStats.length`), one per child, generated by mapping `stats.childStats`. Each `<col />` gets a `key={child.childName}` prop.

    4. Update class strings on column elements:
       - Label `<th />` (currently `text-left font-medium pr-2 pb-1`) → `text-left font-medium px-3 pb-1`.
       - Child header `<th>` (currently `text-left font-medium px-2 pb-1`) → `text-left font-medium px-1 pb-1 min-w-[72px] sm:min-w-[90px]`.
       - Father label `<td>` (currently `text-blue-700 font-medium pr-2 py-1 align-top`) → `text-blue-700 font-medium px-3 py-1 align-top`.
       - Mother label `<td>` (currently `text-rose-700 font-medium pr-2 py-1 align-top`) → `text-rose-700 font-medium px-3 py-1 align-top`.
       - Father child `<td>` (currently `px-2 py-1 align-top`) → `px-1 py-1 align-top min-w-[72px] sm:min-w-[90px]`.
       - Mother child `<td>` (currently `px-2 py-1 align-top`) → `px-1 py-1 align-top min-w-[72px] sm:min-w-[90px]`.

    5. Leave the separator row `<td>` (with `colSpan={stats.childStats.length + 1}`) and the Vapaa rows (also `colSpan={stats.childStats.length + 1}`) UNCHANGED — they correctly span all columns.

    6. Do NOT change any imports, component signature, props, computeStats call, useMemo usage, or text content. Do NOT add new colors or fonts. Use only Tailwind v4 utility classes and the one inline style for tableLayout.

    7. Do NOT introduce a fenced JSX block as the executor — emit the actual JSX changes in-file via Edit tool calls.

    Rationale for `tableLayout: "fixed"` plus min-widths (per UI-SPEC.md): with fixed layout, the browser honors declared widths on cells rather than content-driven sizing, so when StatsPanel and ScheduleTable both use the same `min-w-[72px] sm:min-w-[90px]` on child columns AND have the same outer width (achieved by removing horizontal padding from the wrapper), child columns align edge-to-edge below each other.
  </action>
  <verify>
    <automated>cd /Users/jarno/src/vuoroasuminen &amp;&amp; npm run build 2>&amp;1 | tail -20 &amp;&amp; grep -n "min-w-\[72px\] sm:min-w-\[90px\]" src/components/schedule/stats-panel.tsx | grep -v '^[[:space:]]*//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "min-w-\[72px\] sm:min-w-\[90px\]" src/components/schedule/stats-panel.tsx` returns >= 4 (one on child header th, one on each child td in father and mother rows, possibly more).
    - Source assertion: `grep -n "tableLayout" src/components/schedule/stats-panel.tsx` matches one line containing `tableLayout: "fixed"`.
    - Source assertion: `grep -n "<colgroup>" src/components/schedule/stats-panel.tsx` matches exactly one line.
    - Source assertion: `grep -c "<col " src/components/schedule/stats-panel.tsx` returns >= 2 (label col + at least one child col).
    - Source assertion: `grep -n "py-3 mt-4 bg-muted/30" src/components/schedule/stats-panel.tsx` matches one line (wrapper updated from `p-3` to `py-3`).
    - Source assertion (regression guard): `grep -n " pr-2 " src/components/schedule/stats-panel.tsx` returns 0 lines (old `pr-2` padding fully removed from label cells).
    - Source assertion (regression guard): `grep -n " px-2 " src/components/schedule/stats-panel.tsx` returns 0 lines (old `px-2` padding fully removed from child cells).
    - Source assertion: `grep -c "text-blue-700" src/components/schedule/stats-panel.tsx` returns >= 2 (father label td plus father vapaa row — colors preserved).
    - Source assertion: `grep -c "text-rose-700" src/components/schedule/stats-panel.tsx` returns >= 2 (mother label td plus mother vapaa row — colors preserved).
    - Source assertion: `grep -c "computeStats" src/components/schedule/stats-panel.tsx` returns 2 (import + usage preserved).
    - CLI output: `npm run build` exits 0 with no new TypeScript or ESLint errors attributable to stats-panel.tsx.
    - CLI output: `npm run lint -- src/components/schedule/stats-panel.tsx` exits 0 with no warnings or errors for the modified file (if the project's lint script accepts file arguments; otherwise full `npm run lint` exits 0).
  </acceptance_criteria>
  <done>
    StatsPanel's table uses `tableLayout: "fixed"`, has a `<colgroup>` with one label col plus N child cols, all child column `<th>`/`<td>` carry `min-w-[72px] sm:min-w-[90px]`, label cells use `px-3`, child cells use `px-1`, wrapper uses `py-3` instead of `p-3`, and `npm run build` succeeds. Visual alignment with ScheduleTable child columns is now structurally guaranteed; visual verification happens in 19-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This is a rendering-only change — no new trust boundaries; no user input crosses; no server-side handler is modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | StatsPanel | accept | Same data already rendered today via `computeStats`; no new data is exposed by changing column widths. |
| T-19-02 | Tampering | stats-panel.tsx | accept | Pure CSS/class change. No new external dependencies, no package installs, no server-action calls. Existing typed `computeStats` import remains unchanged. |
| T-19-03 | Denial of Service | StatsPanel render | accept | `tableLayout: "fixed"` is a standard CSS feature with no performance regression; render path is unchanged (still `useMemo`-cached). |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — package legitimacy gate not applicable. |
</threat_model>

<verification>
- `grep -c "min-w-\[72px\] sm:min-w-\[90px\]" src/components/schedule/stats-panel.tsx` >= 4
- `grep -n "tableLayout: \"fixed\"" src/components/schedule/stats-panel.tsx` returns one match
- `grep -n "<colgroup>" src/components/schedule/stats-panel.tsx` returns one match
- `grep -n " pr-2 " src/components/schedule/stats-panel.tsx` returns 0 matches (regression guard)
- `grep -n " px-2 " src/components/schedule/stats-panel.tsx` returns 0 matches (regression guard)
- `npm run build` exits 0
</verification>

<success_criteria>
- StatsPanel's `<table>` declares fixed layout with a `<colgroup>` matching the column structure of ScheduleTable.
- Child column `<th>` and `<td>` elements carry the exact same min-width classes as ScheduleTable child columns (`min-w-[72px] sm:min-w-[90px]`).
- Wrapper horizontal padding removed so StatsPanel table fills the same horizontal extent as ScheduleTable inside their shared parent.
- All existing data, copy, and colors are preserved unchanged.
- Build passes with no new TypeScript or ESLint errors.
</success_criteria>

<output>
Create `.planning/phases/19-stats-column-alignment/19-01-SUMMARY.md` when done.
</output>
