---
phase: 16-schedule-table-reflow-and-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schedule/stats-panel.tsx
autonomous: true
requirements:
  - MOB-05

must_haves:
  truths:
    - "D-14: Statistics panel renders as a child-column grid (children as columns, parents as rows) on every viewport"
    - "D-16: Each parent/child cell shows the day count on line 1 and 'yksin N' as a smaller muted sub-line on line 2"
    - "D-15: A horizontal separator appears between the per-child rows and the Vapaa rows"
    - "D-15: Each Vapaa row spans the full grid width and shows 'ParentName X pv (Y vkl)' colored blue for Isä and rose for Äiti"
    - "D-17: The grid scales correctly when there is a single child (one data column) without special casing"
    - "computeStats() output is unchanged (logic untouched) — only its rendering is modified"
    - "Father labels use text-blue-700 and Mother labels use text-rose-700"
    - "D-18: The panel container uses 'mt-4' (was 'mb-4') because StatsPanel now renders below the schedule table; CSS grid or HTML table implementation per executor discretion"
  artifacts:
    - path: "src/components/schedule/stats-panel.tsx"
      provides: "Child-column stats grid replacing the prior flex-row layout"
      exports: ["StatsPanel"]
      contains: "<table"
  key_links:
    - from: "stats.childStats"
      to: "per-child cell rendering"
      via: ".map() inside Father and Mother <tr> rows"
      pattern: "stats\\.childStats\\.map"
    - from: "stats.parentFreeStats"
      to: "Vapaa rows"
      via: ".map() producing one full-width <tr> per parent"
      pattern: "stats\\.parentFreeStats\\.map"
    - from: "computeStats"
      to: "stats variable"
      via: "useMemo(() => computeStats(days, parents), [days, parents])"
      pattern: "computeStats"
---

<objective>
Redesign the StatsPanel layout to a child-column grid (children as columns, parents as rows) per locked decisions D-14 through D-18. This satisfies MOB-05's requirement that statistics display as a grid on mobile, while also reorganizing the desktop layout to use the same grid (D-14: grid on ALL viewports).

Purpose: The existing horizontal `flex items-center gap-4` layout overflows on mobile and presents data poorly when there are multiple children. The grid structure makes day counts directly comparable across children and parents, and degrades cleanly to a single column when only one child exists.

Output: Modified `src/components/schedule/stats-panel.tsx` that renders an HTML `<table>` with child-column headers, per-parent rows containing day-count + solo-days sub-line, a separator, and one full-width Vapaa row per parent.
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-schedule-table-reflow-and-stats/16-CONTEXT.md
@.planning/phases/16-schedule-table-reflow-and-stats/16-RESEARCH.md
@.planning/phases/16-schedule-table-reflow-and-stats/16-PATTERNS.md
@src/components/schedule/stats-panel.tsx
@src/lib/schedule/stats.ts

<interfaces>
<!-- Type contracts the executor will rely on. Extracted from src/lib/schedule/stats.ts -->

From src/lib/schedule/stats.ts (lines 4-22):

```typescript
export interface ChildStats {
  childName: string
  father: number
  mother: number
  soloFather: number
  soloMother: number
}

export interface ParentFreeStats {
  parentId: ParentId          // "father" | "mother"
  parentName: string
  childFreeDays: number
  childFreeWeekends: number
}

// computeStats(days, parents) returns:
//   { childStats: ChildStats[]; parentFreeStats: ParentFreeStats[] }
```

From src/lib/schedule/types.ts:
- `ParentId = "father" | "mother"`

Component props (interface unchanged):
```typescript
interface StatsPanelProps {
  days: ScheduleDay[]
  parents: Array<{ id: ParentId; name: string }>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign StatsPanel as child-column HTML table grid</name>
  <files>src/components/schedule/stats-panel.tsx</files>
  <read_first>
    - src/components/schedule/stats-panel.tsx (current file — full content; lines 19-48 are the layout to replace)
    - src/lib/schedule/stats.ts (computeStats return shape: stats.childStats[] and stats.parentFreeStats[])
    - .planning/phases/16-schedule-table-reflow-and-stats/16-CONTEXT.md (D-14, D-15, D-16, D-17, D-18 and the user-confirmed sketch in <specifics>)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-PATTERNS.md (stats-panel.tsx section — target structure example, lines 249-318)
    - src/components/schedule/schedule-cell.tsx (existing usage of text-blue-700 / text-rose-700 to keep colors consistent)
  </read_first>
  <action>
    Replace the layout JSX in `StatsPanel` (currently lines 19-48 of `src/components/schedule/stats-panel.tsx`) with an HTML `<table>` structured as a child-column grid per D-14, D-15, D-16, D-17, D-18.

    Keep imports (`useMemo`, `computeStats`, types), the `StatsPanelProps` interface, the `useMemo(() => computeStats(days, parents), [days, parents])` call, and the component signature unchanged. Remove the now-unused inline `parentName` helper (lines 15-16) and read parent display names from `stats.parentFreeStats[*].parentName` instead.

    Outer container: keep the existing classes BUT change `mb-4` to `mt-4` (StatsPanel now renders below the table per D-13). Final outer className: `border rounded-lg p-3 mt-4 bg-muted/30 text-sm`.

    Inside the container, render a single `<table className="w-full">` with this structure:

    1. `<thead>` containing one `<tr>` with:
       - Empty leading `<th>` (column for row labels): `className="text-left font-medium pr-2 pb-1"`
       - One `<th>` per entry in `stats.childStats`, keyed by `child.childName`, content `{child.childName}`, class `text-left font-medium px-2 pb-1`.

    2. `<tbody>` containing, in order:
       a. Father `<tr>`: leading `<td>` with class `text-blue-700 font-medium pr-2 py-1 align-top` showing the father's display name (look it up from `stats.parentFreeStats.find(p => p.parentId === "father")?.parentName` with fallback `"Isä"`). Then one `<td key={child.childName} className="px-2 py-1 align-top">` per `stats.childStats[*]` containing two stacked `<div>`s: line 1 `{child.father} pv`, line 2 `<div className="text-xs text-muted-foreground">yksin {child.soloFather}</div>` (D-16).

       b. Mother `<tr>`: same structure but `text-rose-700` for the label `<td>`, father's-name lookup replaced with mother's, body cells use `child.mother` / `child.soloMother`.

       c. Separator `<tr>`: a single `<td colSpan={stats.childStats.length + 1} className="py-1">` containing `<div className="border-t" />`.

       d. Vapaa rows: `stats.parentFreeStats.map((ps) => ...)` producing one `<tr key={ps.parentId}>` per parent. Inside, a single `<td colSpan={stats.childStats.length + 1} className={\`py-0.5 ${ps.parentId === "father" ? "text-blue-700" : "text-rose-700"}\`}>` with content `{ps.parentName} {ps.childFreeDays} pv ({ps.childFreeWeekends} vkl)`.

    Use `stats.childStats.length + 1` (label column + child data columns) for the colSpan on separator and Vapaa rows. Do NOT special-case single-child mode (D-17) — the table degrades naturally to two columns total when `childStats.length === 1`.

    Do NOT touch `computeStats` or any file in `src/lib/schedule/`. Do NOT add a new package. Do NOT add `useMediaQuery` or any JS-based responsive logic — the grid applies on all viewports per D-14.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/schedule/__tests__/stats.test.ts && grep -nE "<table|stats\\.childStats\\.map|stats\\.parentFreeStats\\.map|text-blue-700|text-rose-700|text-xs text-muted-foreground|colSpan=\\{stats\\.childStats\\.length \\+ 1\\}|mt-4" src/components/schedule/stats-panel.tsx | grep -v '^[[:space:]]*//' | wc -l | awk '{ if ($1 < 6) { print "FAIL: expected >=6 grid markers"; exit 1 } else print "OK" }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/schedule/stats-panel.tsx` contains the literal string `<table` exactly once (the stats grid)
    - `src/components/schedule/stats-panel.tsx` contains `mt-4` and does NOT contain `mb-4`
    - `src/components/schedule/stats-panel.tsx` contains `stats.childStats.map(` at least twice (one for Father row, one for Mother row) and contains `stats.parentFreeStats.map(` exactly once (Vapaa rows)
    - `src/components/schedule/stats-panel.tsx` contains the literal substring `colSpan={stats.childStats.length + 1}` at least twice (separator + Vapaa rows)
    - `src/components/schedule/stats-panel.tsx` contains `text-xs text-muted-foreground` (solo-days sub-line styling per D-16)
    - `src/components/schedule/stats-panel.tsx` contains both `text-blue-700` (Father) and `text-rose-700` (Mother)
    - `src/components/schedule/stats-panel.tsx` no longer contains `flex items-center gap-4`
    - `src/components/schedule/stats-panel.tsx` no longer contains the helper `const parentName = (id`
    - `npx tsc --noEmit` exits 0 (no TypeScript errors introduced)
    - `npx vitest run src/lib/schedule/__tests__/stats.test.ts` passes (computeStats logic unchanged)
    - `src/lib/schedule/stats.ts` is NOT modified (verify with `git diff --name-only src/lib/schedule/stats.ts` returns empty)
  </acceptance_criteria>
  <done>StatsPanel renders the new child-column grid; computeStats untouched; typecheck and stats tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DOM | Pure render; same `days`/`parents` props as before; no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | stats-panel.tsx render | accept | No new data flow; renders same trusted props (`days`, `parents`) that already pass through StatsPanel before this change. computeStats logic is unmodified. Threat surface is rendering-only per RESEARCH.md §Security Domain. |
| T-16-02 | Information Disclosure | stats-panel.tsx render | accept | Same data displayed as before, only rearranged into a grid. No new fields surfaced. |

Phase 16 is rendering-layer only — no new server actions, no new schema, no new packages, no auth/session changes. ASVS L1 categories V2/V3/V4/V5/V6 are not applicable; V5 input validation is unchanged because `NotesCell` and its `saveNotes` action are untouched in this plan.
</threat_model>

<verification>
- `npx tsc --noEmit` passes — no TypeScript errors
- `npx vitest run src/lib/schedule/__tests__/stats.test.ts` passes — computeStats unchanged
- `npx vitest run` (full suite) passes — no regressions in adjacent tests
- Visual: open dashboard on mobile-width browser; stats grid shows two columns of data when two children exist, child names as headers
</verification>

<success_criteria>
- StatsPanel renders an HTML `<table>` with the structure described in CONTEXT.md `<specifics>`
- Per-child cells display day count on line 1 and `yksin N` muted sub-line on line 2
- Father row uses `text-blue-700`, Mother row uses `text-rose-700`
- Separator and Vapaa rows use `colSpan={stats.childStats.length + 1}`
- No `flex items-center gap-4` remains anywhere in stats-panel.tsx
- Outer container uses `mt-4` (panel sits below the schedule table)
- `computeStats` and the stats test file are untouched
</success_criteria>

<output>
Create `.planning/phases/16-schedule-table-reflow-and-stats/16-01-SUMMARY.md` when done
</output>
