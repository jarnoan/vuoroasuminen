---
phase: 18-mobile-cell-and-note-interactions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schedule/schedule-table.tsx
autonomous: true
requirements:
  - UI-03
  - UI-05
tags: [mobile, ui, icon, note-row, schedule-table]

must_haves:
  truths:
    - "The mobile-only note affordance button shows a pen/pencil icon (Pencil from lucide-react), not a plus icon"
    - "On mobile, when a day has either persisted notes or an open note input, the note row sits directly underneath its day row with no visible top gap between them"
    - "On mobile, the note input is visually indented (pl-8) so it appears nested under the date column"
    - "On mobile, the day row loses its bottom border when a note row follows it, so the day + note pair forms one visual unit"
    - "Desktop layout (sm: and up) for the table, the desktop notes column, and the day row are visually unchanged"
  artifacts:
    - path: "src/components/schedule/schedule-table.tsx"
      provides: "Updated ScheduleTable with Pencil icon swap, note row pt-0 + pl-8, day-row conditional bottom-border removal"
      contains: "Pencil"
      contains_also:
        - "pl-8"
        - "pt-0"
  key_links:
    - from: "src/components/schedule/schedule-table.tsx note button"
      to: "lucide-react Pencil icon"
      via: "import { Pencil } from 'lucide-react' and <Pencil className=\"h-4 w-4\" /> JSX"
      pattern: "import \\{[^}]*Pencil[^}]*\\} from \"lucide-react\""
    - from: "src/components/schedule/schedule-table.tsx day <tr>"
      to: "the conditional note <tr> that follows it"
      via: "className gains max-sm:[&>td]:border-b-0 (or equivalent) when day.notes || notesOpenDates.has(day.date)"
      pattern: "max-sm:\\[&>td\\]:border-b-0"
---

<objective>
Apply two mobile-only visual changes to `ScheduleTable`:
1. UI-03: Swap the `PlusIcon` on the mobile note affordance button for the `Pencil` icon from lucide-react.
2. UI-05: Attach the mobile note `<tr>` visually to its day `<tr>` — zero top gap, left-indent the note content, and remove the day row's bottom border when a note row follows.

Purpose: The plus icon misleadingly suggests "add another column" rather than "edit notes for this day"; the pen icon communicates the actual action. The note row currently floats with vertical padding above it, making it ambiguous which day it belongs to — visually merging it with the day row above resolves that ambiguity.

Output: One modified file (`src/components/schedule/schedule-table.tsx`) — three surgical edits, no behavioral or data-flow changes.
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-mobile-cell-and-note-interactions/18-CONTEXT.md
@.planning/phases/18-mobile-cell-and-note-interactions/18-PATTERNS.md
@.planning/phases/18-mobile-cell-and-note-interactions/18-UI-SPEC.md
@.planning/milestones/v1.4-phases/16-schedule-table-reflow-and-stats/16-02-SUMMARY.md

<interfaces>
<!-- Existing rendering structure to preserve -->

Mobile note button (schedule-table.tsx ~lines 343–355) — current PlusIcon usage:
```tsx
<td className="px-1 py-1 max-sm:table-cell sm:hidden">
  {!day.notes && !notesOpenDates.has(day.date) ? (
    <button
      type="button"
      onClick={() => setNotesOpenDates(prev => new Set(prev).add(day.date))}
      aria-label={`Lisää muistiinpano — ${day.dayLabel}`}
      className="text-muted-foreground hover:text-foreground"
      style={{ touchAction: "manipulation" }}
    >
      <PlusIcon className="h-4 w-4" />
    </button>
  ) : null}
</td>
```

Day row `<tr>` (schedule-table.tsx ~lines 307–311) — current condition for note-row presence is `(day.notes || notesOpenDates.has(day.date))`. Same condition currently drives the conditional `<tr className="max-sm:table-row sm:hidden">…</tr>` that renders right after the day row inside the `<React.Fragment>`.

Mobile note row (schedule-table.tsx ~lines 364–383) — current padding `px-1 py-1 pb-1` on the colSpan td.

Table render uses `border-collapse` (line 275). Per PATTERNS.md the day-row borders come from `border-b` on individual `<td>` elements (the `<th>`s carry `border-b` and the row separators are implicit between rows; the mobile table relies on this for the visual line between days).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace PlusIcon with Pencil on the mobile note affordance button</name>
  <files>src/components/schedule/schedule-table.tsx</files>
  <read_first>
    - src/components/schedule/schedule-table.tsx (specifically the import block on line 4 and the mobile note button around line 352)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-CONTEXT.md (D-01 — Claude's discretion among Pencil/PenLine/Pen; UI-SPEC settled on Pencil)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-UI-SPEC.md (Component Inventory and Interaction Contract → UI-03 — `Pencil` is the chosen icon name)
  </read_first>
  <behavior>
    - The mobile-only note affordance button continues to appear exactly when it does today (`max-sm:table-cell sm:hidden` td AND `!day.notes && !notesOpenDates.has(day.date)`).
    - The button's `aria-label`, `style`, `className`, `onClick`, and `type` all remain unchanged.
    - The only visible change is the icon glyph — pencil instead of plus.
    - Icon size is `h-4 w-4` (16×16, unchanged).
    - The lucide-react `Pencil` icon is imported from `lucide-react`; `PlusIcon` is no longer referenced anywhere in this file.
  </behavior>
  <action>
    In `src/components/schedule/schedule-table.tsx`:

    1. In the import block (line 4 today: `import { PlusIcon } from "lucide-react"`), replace the named import `PlusIcon` with `Pencil`. Resulting import: `import { Pencil } from "lucide-react"`. Do not change any other import line.

    2. In the JSX (the mobile note affordance button, today `<PlusIcon className="h-4 w-4" />`), change the element to `<Pencil className="h-4 w-4" />`. Keep `className="h-4 w-4"` exactly as is.

    Do NOT add new props, do NOT change `aria-label`, `style`, or the wrapping conditional. Do NOT introduce any other icon import (no `PenLine`, no `Pen`).
  </action>
  <verify>
    <automated>cd /Users/jarno/src/vuoroasuminen &amp;&amp; grep -n "PlusIcon" src/components/schedule/schedule-table.tsx; if [ $? -eq 0 ]; then echo FAIL_PLUSICON_PRESENT; exit 1; fi; grep -n "import { Pencil } from \"lucide-react\"" src/components/schedule/schedule-table.tsx &amp;&amp; grep -n "&lt;Pencil className=\"h-4 w-4\" /&gt;" src/components/schedule/schedule-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PlusIcon" src/components/schedule/schedule-table.tsx` returns no matches (exit 1 acceptable; absence is the assertion)
    - `grep -n "import { Pencil } from \"lucide-react\"" src/components/schedule/schedule-table.tsx` returns exactly one match on the top-of-file import line
    - `grep -c "<Pencil " src/components/schedule/schedule-table.tsx` returns exactly 1
    - `grep -n "Pencil className=\"h-4 w-4\"" src/components/schedule/schedule-table.tsx` returns exactly one match
    - `grep -n "aria-label={\`Lisää muistiinpano" src/components/schedule/schedule-table.tsx` still returns the original aria-label line — the button's accessibility label is unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    The mobile note affordance button renders a pencil glyph from lucide-react instead of a plus. `PlusIcon` is no longer imported or referenced anywhere in the file. TypeScript compiles. UI-03 is satisfied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Visually merge the mobile note row with its day row — zero top gap, pl-8 indent, conditional bottom-border removal on the day row</name>
  <files>src/components/schedule/schedule-table.tsx</files>
  <read_first>
    - src/components/schedule/schedule-table.tsx (day row `<tr>` around lines 307–311 and conditional note `<tr>` around lines 364–383)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-CONTEXT.md (D-09, D-10, D-11 — locked)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-PATTERNS.md (UI-05 section — concrete className changes and `max-sm:[&>td]:border-b-0` approach)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-UI-SPEC.md (Interaction Contract → UI-05 Note Row Attribution)
    - .planning/milestones/v1.4-phases/16-schedule-table-reflow-and-stats/16-02-SUMMARY.md (Task 2 introduced the two-row notes layout — preserve `colSpan`, `max-sm:table-row sm:hidden`, the collapse-on-empty logic)
  </read_first>
  <behavior>
    - On mobile (`max-sm:`), the note `<tr>` that follows a day row sits flush against it with no visible top padding/margin and no border between them.
    - The note row content is indented by 32px on the left (`pl-8`) so the input appears under the date column's text content, not flush to the table's left edge.
    - The note row's bottom padding is unchanged (`pb-1`); the horizontal padding (was `px-1`) is replaced with `pl-8 pr-1` so the indent only applies to the left.
    - On the day `<tr>`, when a note row will follow (`day.notes || notesOpenDates.has(day.date)` is true), the day row's `<td>` cells must lose their bottom border on mobile only — applied via the descendant variant `max-sm:[&>td]:border-b-0`.
    - Desktop (`sm:` and up) is unchanged: the note `<tr>` continues to use `sm:hidden`; desktop day rows are unaffected because the border-removal class is `max-sm:`-scoped.
    - The `colSpan={colCount}` on the note `<td>` is preserved.
    - The `setNotesOpenDates` collapse-on-empty onSave callback (added in Phase 16 plan 02) is preserved unchanged.
    - The `data-date` and `data-today` attributes on the day `<tr>` are preserved.
  </behavior>
  <action>
    In `src/components/schedule/schedule-table.tsx`, make two surgical edits:

    1. **Day row className (lines 307–311 today)** — Currently:
       ```
       className={day.isToday ? "bg-yellow-50 dark:bg-yellow-950/20 scroll-mt-10" : "scroll-mt-10"}
       ```
       Replace with a composition that adds `max-sm:[&>td]:border-b-0` when the note row will follow. Use a join/template approach (not a ternary inside a ternary) for readability. Concrete shape:
       ```
       className={[
         day.isToday ? "bg-yellow-50 dark:bg-yellow-950/20 scroll-mt-10" : "scroll-mt-10",
         (day.notes || notesOpenDates.has(day.date)) ? "max-sm:[&>td]:border-b-0" : "",
       ].filter(Boolean).join(" ")}
       ```
       The condition `(day.notes || notesOpenDates.has(day.date))` MUST be identical to the condition that gates the conditional `<tr>` lower in the same Fragment — they are the same predicate.

    2. **Note row `<td>` className (line 366 today)** — Currently:
       ```
       className="px-1 py-1 pb-1"
       ```
       Replace with:
       ```
       className="pt-0 pb-1 pl-8 pr-1"
       ```
       This sets top padding to zero (D-09), keeps bottom padding at `pb-1` (unchanged), and replaces the horizontal `px-1` with `pl-8 pr-1` so the left indent (D-10) is applied without affecting the right padding. Do not touch the surrounding `<tr className="max-sm:table-row sm:hidden">` (so desktop continues to hide this row) and do not touch `colSpan={colCount}`.

    Do NOT modify the `NotesCell` component, the `handleNoteSave` function, the inner `setNotesOpenDates` onSave handler, or any other part of the file. Do NOT introduce a `divide-y` class. Do NOT touch the desktop notes `<td>` (the one with `max-sm:hidden` at line 356).
  </action>
  <verify>
    <automated>cd /Users/jarno/src/vuoroasuminen &amp;&amp; grep -n "max-sm:\[&amp;&gt;td\]:border-b-0" src/components/schedule/schedule-table.tsx &amp;&amp; grep -n "pt-0 pb-1 pl-8 pr-1" src/components/schedule/schedule-table.tsx &amp;&amp; grep -n "px-1 py-1 pb-1" src/components/schedule/schedule-table.tsx; if [ $? -eq 0 ]; then echo FAIL_OLD_NOTE_TD_CLASSES_STILL_PRESENT; exit 1; fi; grep -n "max-sm:table-row sm:hidden" src/components/schedule/schedule-table.tsx &amp;&amp; grep -n "colSpan={colCount}" src/components/schedule/schedule-table.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | tee /tmp/p18-02-tsc.log &amp;&amp; npx vitest run --reporter=default 2>&amp;1 | tee /tmp/p18-02-vitest.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max-sm:\[&>td\]:border-b-0" src/components/schedule/schedule-table.tsx` returns exactly 1
    - `grep -c "pt-0 pb-1 pl-8 pr-1" src/components/schedule/schedule-table.tsx` returns exactly 1
    - The legacy note-td className string `"px-1 py-1 pb-1"` no longer appears in the file (grep on that exact string returns 0 matches)
    - `grep -c "max-sm:table-row sm:hidden" src/components/schedule/schedule-table.tsx` returns exactly 1 (the note row's tr class is unchanged)
    - `grep -c "colSpan={colCount}" src/components/schedule/schedule-table.tsx` returns at least 2 (week-label row + note row both still use it)
    - `grep -n "data-date={day.date}" src/components/schedule/schedule-table.tsx` and `grep -n "data-today={day.isToday" src/components/schedule/schedule-table.tsx` still return matches (day row attributes preserved)
    - `grep -n "NotesCell" src/components/schedule/schedule-table.tsx` still returns at least 2 matches (desktop + mobile NotesCell usage preserved)
    - The condition `day.notes || notesOpenDates.has(day.date)` appears at least twice in the file (once on the day-row className, once gating the note-row JSX) — `grep -c "notesOpenDates.has(day.date)" src/components/schedule/schedule-table.tsx | awk '{if ($1 < 2) exit 1}'` succeeds
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>
    The mobile note row sits visually flush under its day row with a 32px left indent, the day row loses its bottom border when a note row follows (mobile only), desktop layout is unchanged, TypeScript compiles, and the existing Vitest suite passes. UI-05 is satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → schedule-table.tsx note button onClick | No new state surface introduced; existing `setNotesOpenDates` semantics preserved |
| lucide-react `Pencil` icon → DOM | Static SVG render, no user-controlled content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Information Disclosure | None — pure rendering change to icon glyph + Tailwind classes | accept | No data flow change, no new component reads. Existing NotesCell server action wiring untouched. |
| T-18-07 | Tampering | Note input (NotesCell) is unchanged; existing handleNoteSave → saveNotes Server Action with Zod validation continues to handle all user-supplied note text | accept | No new input surface introduced in this phase. |
| T-18-08 | Denial of Service | New className composition uses string `.join(" ")` on every render | accept | Trivial cost, runs only on day rows that already re-render on data changes. No regression vs. existing implementation. |
| T-18-09 | Cross-site Scripting | `aria-label`, day labels, and dayLabel template strings remain React-escaped JSX expressions | accept | React's default JSX escaping covers all user/derived strings; no `dangerouslySetInnerHTML` introduced. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages. `Pencil` is a named export from the existing `lucide-react@^1.7.0` dependency already in package.json. No supply-chain surface introduced. |
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit` passes
- Tests: `npx vitest run` passes with no regressions
- Lint: `npm run lint` passes
- Grep assertions in `<acceptance_criteria>` succeed
- Mobile visual verification is deferred to Plan 03 (combined human checkpoint for the whole phase)
</verification>

<success_criteria>
- ROADMAP Phase 18 SC-1 satisfied: on mobile, the button that reveals the note input shows a pen icon, not a plus icon
- ROADMAP Phase 18 SC-5 satisfied: on mobile, the note row is visually attributed to its day row
- Requirements UI-03 and UI-05 fully addressed
- Desktop layout (sm: and up) unchanged
</success_criteria>

<output>
Create `.planning/phases/18-mobile-cell-and-note-interactions/18-02-SUMMARY.md` when done.
</output>
