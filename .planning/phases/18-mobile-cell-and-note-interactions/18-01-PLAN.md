---
phase: 18-mobile-cell-and-note-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schedule/schedule-cell.tsx
autonomous: true
requirements:
  - UI-04
tags: [mobile, touch, ux, long-press, vibration, schedule-cell]

must_haves:
  truths:
    - "On a touch device (@media (hover: none)), long-pressing a parent custody cell triggers a 1s background-color fade from the parent color toward red"
    - "When the 1s arm timer fires, the device vibrates once for 100ms via navigator.vibrate(100)"
    - "When armed, the entire cell appears as a full-width destructive (red) button with white 'Tyhjennä' text, replacing the parent name"
    - "Tapping anywhere on the armed cell invokes onClear and returns the cell to idle state"
    - "If the user releases the pointer before 1s (or moves >8px), the cell snaps back to its parent color instantly with no reverse animation"
    - "If 2s passes after arming without a tap, the cell auto-disarms back to idle"
    - "On a hover device (@media (hover: hover)), the existing corner × button continues to appear on hover and the long-press / full-cell behavior is suppressed"
  artifacts:
    - path: "src/components/schedule/schedule-cell.tsx"
      provides: "Updated ScheduleCell with isHolding state, 1s color-fade transition, vibration, full-cell armed render, and @media (hover) split"
      contains: "isHolding"
      contains_also:
        - "navigator.vibrate"
        - "[@media(hover:none)]"
        - "[@media(hover:hover)]"
        - "Tyhjennä"
        - "bg-destructive"
  key_links:
    - from: "src/components/schedule/schedule-cell.tsx"
      to: "src/components/schedule/schedule-table.tsx"
      via: "ScheduleCell onClear prop wired to handleClear (existing — unchanged)"
      pattern: "onClear=\\{handleClear\\}"
    - from: "src/components/schedule/schedule-cell.tsx isArmed callback"
      to: "browser navigator.vibrate"
      via: "navigator.vibrate(100) call inside arm-timer setTimeout"
      pattern: "navigator\\.vibrate\\(100\\)"
---

<objective>
Extend `ScheduleCell` so the existing long-press clear guard (Phase 15) becomes a full-cell "Tyhjennä" interaction on touch devices: a 1s color fade during the hold, haptic vibration on arm, full-cell red destructive render with "Tyhjennä" copy, tap-anywhere to clear, instant snap-back on cancel. Desktop hover behavior is unchanged.

Purpose: Mobile users need clearer, friendlier feedback that a long-press is registering. The current corner × on mobile is small, easily mis-tapped, and gives no visual feedback during the hold. The full-cell red fade + vibration + large tap target makes clearing a deliberate, recoverable interaction.

Output: A single modified file (`src/components/schedule/schedule-cell.tsx`) with the new `isHolding` state, `@media (hover: …)` Tailwind variants, vibration call, and conditional full-cell render path.
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-mobile-cell-and-note-interactions/18-CONTEXT.md
@.planning/phases/18-mobile-cell-and-note-interactions/18-PATTERNS.md
@.planning/phases/18-mobile-cell-and-note-interactions/18-UI-SPEC.md
@.planning/milestones/v1.4-phases/15-header-clear-guard-and-toolbar/15-02-SUMMARY.md

<interfaces>
<!-- Current ScheduleCell contract — preserved unchanged in Phase 18 -->

From src/components/schedule/schedule-cell.tsx:
```typescript
interface ScheduleCellProps {
  entryId: string
  parentId: ParentId
  status: "draft" | "published"
  childName: string
  onToggle: (entryId: string, newParentId: ParentId) => void
  onClear: (entryId: string) => void
  parents: Array<{ id: ParentId; name: string }>
}
```

Parent-color class map (preserved, unchanged):
- father.published: `bg-blue-500 text-white hover:bg-blue-600`
- father.draft:     `bg-blue-200 text-blue-800 hover:bg-blue-300`
- mother.published: `bg-rose-500 text-white hover:bg-rose-600`
- mother.draft:     `bg-rose-200 text-rose-800 hover:bg-rose-300`

From src/lib/schedule/types.ts:
```typescript
export type ParentId = "father" | "mother"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add isHolding state, vibration, and @media (hover:…) split for the full-cell Tyhjennä render in ScheduleCell</name>
  <files>src/components/schedule/schedule-cell.tsx</files>
  <read_first>
    - src/components/schedule/schedule-cell.tsx (current implementation — preserve all Phase 15 machinery)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-CONTEXT.md (D-02 through D-08 are non-negotiable)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-PATTERNS.md (line-by-line extension instructions for this file)
    - .planning/phases/18-mobile-cell-and-note-interactions/18-UI-SPEC.md (Interaction Contract → UI-04 state machine; Color → UI-04 transition color contract)
    - .planning/milestones/v1.4-phases/15-header-clear-guard-and-toolbar/15-02-SUMMARY.md (why the timer cleanup useEffect exists; do not remove it)
  </read_first>
  <behavior>
    - State machine for a single ScheduleCell on a touch device (`@media (hover: none)`):
      - idle → pointerdown → holding (sets isHolding=true, starts 1000ms arm timer; cell button gains a `transition-colors duration-1000` class with destination color `bg-destructive`, so the background colour-fades from the parent color toward red over 1s)
      - holding + (pointerup OR pointercancel OR pointermove >8px from start) before 1000ms → idle (isHolding=false; armTimer cleared; transition class removed so the snap-back is instant)
      - holding + 1000ms timer fires → armed (isArmed=true, isHolding=false, `navigator.vibrate(100)` called, 2000ms disarm timer scheduled; cell renders as `bg-destructive text-white` with the literal text "Tyhjennä")
      - armed + tap anywhere on the cell → onClear(entryId) is called; isArmed=false; isHolding=false; disarm timer cleared
      - armed + 2000ms with no tap → auto-disarm: isArmed=false; cell renders parent color + parent name again
    - State machine for a single ScheduleCell on a hover device (`@media (hover: hover)`):
      - The existing corner × button is the only clear affordance and is shown only on `group-hover` / focus.
      - The long-press fade and full-cell render MUST be suppressed: the cell button itself never shows the destructive color, never shows "Tyhjennä", and tapping it never invokes onClear.
      - Phase 15 hover behavior (× on hover → onClear) is preserved exactly.
    - navigator.vibrate must be feature-detected (`typeof navigator !== "undefined" && typeof navigator.vibrate === "function"`) before being called so SSR and unsupported browsers do not throw.
    - The existing useEffect that clears armTimerRef and disarmTimerRef on unmount stays in place.
  </behavior>
  <action>
    Modify `src/components/schedule/schedule-cell.tsx` in place per CONTEXT.md decisions D-02..D-08 and PATTERNS.md. Concrete changes:

    1. Add a new state hook alongside `isArmed`: `const [isHolding, setIsHolding] = useState(false)`. Keep `isArmed`, `armTimerRef`, `disarmTimerRef`, `startXRef`, `startYRef`, and the unmount-cleanup `useEffect` exactly as they are.

    2. Update `handleCellPointerDown` so it calls `setIsHolding(true)` immediately (before scheduling the arm timer). Inside the existing 1000ms `setTimeout` callback, in this order: call `setIsHolding(false)`, call `setIsArmed(true)`, feature-detect and call `navigator.vibrate(100)`, then schedule the 2000ms `disarmTimerRef` `setTimeout` that calls `setIsArmed(false)`.

    3. Extend `cancelArm()` so it additionally calls `setIsHolding(false)`. Do not touch `disarmTimerRef` from `cancelArm` (the existing semantics — cancel only the arm timer — must be preserved).

    4. Replace the existing single cell `<button>` className expression with a composition that uses Tailwind v4 arbitrary `[@media(hover:none)]:` and `[@media(hover:hover)]:` variants per UI-SPEC and PATTERNS. The classes must encode three render states:
       - idle: parent `colorClass` plus `transition-colors`
       - holding (isHolding && !isArmed): parent `colorClass` AND `[@media(hover:none)]:[transition:background-color_1s_linear] [@media(hover:none)]:bg-destructive` so the touch-device background animates toward the destructive token over 1s while hover devices remain on the parent color
       - armed (isArmed): `[@media(hover:none)]:bg-destructive [@media(hover:none)]:text-white` while on hover devices the parent `colorClass` continues to apply
       Always include `w-full h-full min-h-[40px] rounded-md font-medium text-sm` and keep `style={{ touchAction: "manipulation" }}`.

    5. Change the cell button label to render `{isArmed ? "Tyhjennä" : displayName}`. Per D-07 the label only flips when fully armed; the holding state continues to show the parent name. On hover devices `isArmed` will never be set via the touch path, but to comply with D-04 (hover devices keep existing behavior unchanged) the armed-text swap MUST NOT visually appear on hover devices — wrap the conditional label in a span whose class hides the "Tyhjennä" variant on hover devices, or render the parent name only on hover devices. Implementation choice: render `<span className="[@media(hover:hover)]:hidden">{isArmed ? "Tyhjennä" : displayName}</span>` and `<span className="[@media(hover:none)]:hidden">{displayName}</span>` inside the button so that hover devices always show the parent name regardless of `isArmed`.

    6. Add a conditional `onClick` to the cell button: when `isArmed` is true on a touch device, tap-anywhere triggers `onClear(entryId)` and resets `isArmed`, `isHolding`, and clears `disarmTimerRef` exactly like the existing corner × onClick handler. When `isArmed` is false, keep the existing `onToggle(entryId, newParentId)` behavior. (Because hover devices never reach `isArmed=true`, they continue to receive only the toggle onClick.)

    7. The corner × `<button>` MUST remain in the JSX but become hidden on touch devices. Replace its current `${isArmed ? "max-sm:opacity-100" : "max-sm:opacity-0"}` segment with `[@media(hover:none)]:hidden`. Keep its desktop hover behavior `sm:opacity-0 sm:group-hover:opacity-100 focus:opacity-100 focus-visible:opacity-100` unchanged. Keep its `aria-label="Tyhjennä"` and `title="Tyhjennä tämä päivä"` exactly as today.

    8. Keep the wrapper `<div className="relative group w-full h-full">` exactly as is; do not introduce new wrapper elements.

    Do NOT change the `ScheduleCellProps` interface, the `colorMap`, the imports list (other than what is already imported), the `useEffect` cleanup, or the existing `handleCellPointerUp` / `handleCellPointerCancel` / `handleCellPointerMove` functions.
  </action>
  <verify>
    <automated>cd /Users/jarno/src/vuoroasuminen &amp;&amp; npx tsc --noEmit 2>&amp;1 | tee /tmp/p18-01-tsc.log &amp;&amp; npx vitest run --reporter=default 2>&amp;1 | tee /tmp/p18-01-vitest.log &amp;&amp; grep -n "isHolding" src/components/schedule/schedule-cell.tsx &amp;&amp; grep -n "navigator.vibrate(100)" src/components/schedule/schedule-cell.tsx &amp;&amp; grep -n "@media(hover:none)" src/components/schedule/schedule-cell.tsx &amp;&amp; grep -n "@media(hover:hover)" src/components/schedule/schedule-cell.tsx &amp;&amp; grep -n "bg-destructive" src/components/schedule/schedule-cell.tsx &amp;&amp; grep -c "Tyhjennä" src/components/schedule/schedule-cell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no errors involving `schedule-cell.tsx`
    - `npx vitest run` exits 0; no regression in the existing suite
    - `grep -n "isHolding" src/components/schedule/schedule-cell.tsx` returns at least 4 matches (declaration + true-set in pointerdown + false-set inside arm-timer callback + false-set in cancelArm)
    - `grep -n "setIsHolding(true)" src/components/schedule/schedule-cell.tsx` returns exactly one occurrence inside `handleCellPointerDown`
    - `grep -n "setIsHolding(false)" src/components/schedule/schedule-cell.tsx` returns at least 2 occurrences (one inside the arm-timer callback before setIsArmed, one inside cancelArm)
    - `grep -n "navigator.vibrate(100)" src/components/schedule/schedule-cell.tsx` returns exactly one occurrence inside the arm-timer setTimeout callback
    - The `navigator.vibrate` call is preceded by a feature-detection guard — `grep -B1 "navigator.vibrate(100)" src/components/schedule/schedule-cell.tsx` shows a `navigator !== "undefined"` or `navigator.vibrate` truthy check on the line immediately before
    - `grep -c "@media(hover:none)" src/components/schedule/schedule-cell.tsx` returns at least 3 (cell button transition + cell button armed bg + corner × hidden)
    - `grep -c "@media(hover:hover)" src/components/schedule/schedule-cell.tsx` returns at least 1 (hover-device label visibility)
    - `grep -n "bg-destructive" src/components/schedule/schedule-cell.tsx` returns at least 1 match
    - `grep -c "Tyhjennä" src/components/schedule/schedule-cell.tsx` returns at least 2 (armed cell label "Tyhjennä" + existing × button "Tyhjennä tämä päivä")
    - `grep -n "max-sm:opacity-100" src/components/schedule/schedule-cell.tsx | grep -v '^#' | wc -l` returns 0 — the legacy mobile-armed `×` visibility class is gone
    - `grep -n "max-sm:opacity-0" src/components/schedule/schedule-cell.tsx | grep -v '^#' | wc -l` returns 0 — same as above
    - `grep -n "useEffect(()" src/components/schedule/schedule-cell.tsx` still shows the existing unmount-cleanup effect (not deleted)
    - `grep -n "touchAction:" src/components/schedule/schedule-cell.tsx` still shows `touchAction: "manipulation"` on the cell button
    - `grep -n "aria-label=\"Tyhjennä\"" src/components/schedule/schedule-cell.tsx` returns at least one occurrence (corner × button still labelled)
  </acceptance_criteria>
  <done>
    schedule-cell.tsx contains the new isHolding state, the navigator.vibrate(100) feature-detected call, the @media (hover:…) variant split, conditional armed onClick, and the conditional armed label — while still compiling, passing the existing Vitest suite, and preserving the Phase 15 hover-device corner × behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user pointer events → DOM event handlers | Browser-controlled — handler must not assume monotonic timing |
| isArmed/isHolding state → React render | React-controlled — must avoid state updates after unmount |
| `navigator.vibrate(100)` → device vibration motor | Browser API — must be feature-detected before call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | New `isArmed` full-cell onClick path → onClear(entryId) | mitigate | Reuse the existing `onClear` Server Action wiring (no change to actions/schedule.ts). Optimistic-revert and Zod validation already cover the mutation. No new mutation surface introduced. |
| T-18-02 | Denial of Service | `navigator.vibrate(100)` abuse on rapid arm cycles | accept | Single 100ms pulse per arm; auto-disarm at 2s plus the >8px pointermove cancel make it impossible to vibrate faster than once per ~1s of deliberate user input. Browser-level rate-limit on `navigator.vibrate` provides additional defense. |
| T-18-03 | Information Disclosure | None — purely rendering layer, no new data exposure | accept | No new data is read from props or external sources; the change is to className composition and an existing onClear call. |
| T-18-04 | Tampering | React state update on unmounted component if armTimer or disarmTimer fires after component unmounts (e.g., view window changes) | mitigate | Existing useEffect cleanup (added in Phase 15 per T-15-04) clears both timers on unmount. The new `setIsHolding` state shares the same `armTimerRef` lifecycle — protected by the same cleanup. |
| T-18-05 | Repudiation | Tap-to-clear with no explicit confirm | accept | Two-step interaction (1s hold + tap) is itself the confirmation; reverts via `handleClear` optimistic-revert if Server Action fails. The auto-disarm after 2s mitigates accidental long-press without action. Per CONTEXT.md D-02/D-03 (locked decisions). |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. `navigator.vibrate` is a built-in browser API. No supply-chain surface introduced. |
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit` passes
- Tests: `npx vitest run` passes with no regressions
- Lint: `npm run lint` passes
- Grep assertions in `<acceptance_criteria>` succeed
- Mobile human-verify is deferred to Plan 03 (combined human checkpoint for the whole phase)
</verification>

<success_criteria>
- ROADMAP Phase 18 SC-2 satisfied: on mobile, long-pressing a parent custody cell triggers a brief vibration and transforms the entire cell into a full-width "Tyhjennä" button
- ROADMAP Phase 18 SC-3 satisfied: tapping the "Tyhjennä" button clears the cell; auto-disarm timeout still applies if not tapped
- ROADMAP Phase 18 SC-4 satisfied: on desktop, the existing hover × clear behavior is unchanged
- Requirement UI-04 fully addressed
</success_criteria>

<output>
Create `.planning/phases/18-mobile-cell-and-note-interactions/18-01-SUMMARY.md` when done.
</output>
