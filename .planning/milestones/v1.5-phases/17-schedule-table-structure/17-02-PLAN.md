---
phase: 17-schedule-table-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/components/schedule/schedule-table.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "On desktop (sm and above), the schedule table no longer has an inner scroll container — page scrolls instead"
    - "On mobile (below sm), scroll behavior is unchanged from v1.4 (was already full-page scroll)"
    - "The <thead> remains sticky to the viewport top on both mobile and desktop (using existing `sticky top-0` classes)"
    - "On mount, today's row scrolls to the top of the visible area, not the center, with scroll-margin-top offsetting the sticky thead height"
    - "ExtendPanel and ClearPanel below the table continue to scroll naturally with the page (no special handling required)"
  artifacts:
    - path: "src/components/schedule/schedule-table.tsx"
      provides: "Wrapper <div> with no overflow/height classes; auto-scroll useEffect with block:start; today <tr> with scroll-mt-* class"
      contains: "scroll-mt-"
  key_links:
    - from: "schedule-table.tsx wrapper <div>"
      to: "page scroll on desktop"
      via: "absence of sm:overflow-y-auto / sm:h-[calc(...)] classes"
      pattern: "<div>\\s*\\n\\s*<table className=\"w-full border-collapse\">"
    - from: "schedule-table.tsx useEffect auto-scroll"
      to: "today's <tr> via document.querySelector('[data-today=\"true\"]')"
      via: "scrollIntoView with block:start"
      pattern: "block:\\s*\"start\""
    - from: "today <tr>"
      to: "sticky thead offset compensation"
      via: "scroll-mt-* Tailwind class"
      pattern: "scroll-mt-"
---

<objective>
Convert the ScheduleTable from a desktop-only inner scroll container to full-page scroll on all viewports, while preserving the sticky `<thead>`. Update the auto-scroll-to-today behavior so today appears at the top of the visible area instead of centered, and add `scroll-margin-top` to today's row so the sticky thead does not cover it.

Purpose: Make the entire page scroll as one unit on desktop (UI-02), matching the mobile behavior shipped in v1.4 and eliminating the awkward dual-scrollbar feel.

Output: Updated `schedule-table.tsx` with the wrapper `<div>` stripped of `sm:overflow-y-auto` / `sm:h-[calc(100svh-8rem)]`, the auto-scroll `useEffect` using `block: "start"`, and the today `<tr>` carrying a `scroll-mt-*` class merged with its existing `bg-yellow-*` highlight.
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-schedule-table-structure/17-CONTEXT.md
@.planning/phases/17-schedule-table-structure/17-PATTERNS.md
@.planning/phases/17-schedule-table-structure/17-01-SUMMARY.md
@src/components/schedule/schedule-table.tsx
@src/components/schedule/dashboard-shell.tsx

<interfaces>
<!-- Layout context extracted from codebase. -->

From `src/components/schedule/dashboard-shell.tsx` (verified — no `overflow-hidden` constraint):
- Outer wrapper: `<div className="min-h-screen flex flex-col">`
- `<main className="flex-1 p-4">` contains `<ScheduleWithRealtime>`, `<ExtendPanel>`, `<ClearPanel>`
- No ancestor enforces `overflow: hidden`, so `position: sticky` on `<thead>` will work correctly once the inner scroll container is removed.

Sticky-thead Tailwind classes already present on the `<thead>` in `schedule-table.tsx` (KEEP THESE — D-08):
- `sticky top-0 z-10 bg-background`

STATE.md Phase v1.4 lessons reapplied here:
- `overflow-clip` rather than `overflow-hidden` on iOS — current shell already avoids this trap. No new ancestor overflow added.
- `100svh` is the iOS-safe viewport unit. Plan 17-01 leaves `<thead>` sticky CSS untouched. This plan REMOVES the `100svh` usage (it was only inside the inner scroll container we are deleting), so iOS toolbar safety regression is not possible.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove desktop inner scroll container and update auto-scroll-to-today behavior</name>
  <files>src/components/schedule/schedule-table.tsx</files>
  <read_first>
    - src/components/schedule/schedule-table.tsx (current state — wrapper <div> at line 254, auto-scroll useEffect at lines 85-90, today <tr> at lines 285-288)
    - .planning/phases/17-schedule-table-structure/17-PATTERNS.md (Change 1 and Change 2 — exact target JSX, decisions D-06 through D-09)
    - .planning/phases/17-schedule-table-structure/17-CONTEXT.md (Decisions D-06, D-07, D-08, D-09)
    - src/components/schedule/dashboard-shell.tsx (lines 59-73 — confirm no overflow constraint on ancestors of <main>)
    - .planning/phases/17-schedule-table-structure/17-01-SUMMARY.md (verify Plan 01 landed: getISOWeek import and week label row present)
  </read_first>
  <action>
    Three edits to `src/components/schedule/schedule-table.tsx`. Apply per D-06, D-07, D-08, D-09:

    Edit A (D-06) — Wrapper `<div>` declasses. The outermost element of the return is currently:
      `<div className="sm:overflow-y-auto sm:h-[calc(100svh-8rem)]">`
    Replace this with a bare `<div>` (no className at all). The wrapper element itself must stay so the JSX tree shape is unchanged for child key/ref behavior; only the className string is removed. Do NOT add any new className. Do NOT replace with a fragment.

    Rationale: Mobile (<sm) was already full-page scroll because these `sm:` classes were inactive at that breakpoint — no mobile regression. Desktop now inherits page scroll from the body/html scrollbar.

    Edit B (D-07) — Auto-scroll `useEffect`. The current effect calls:
      `todayRow.scrollIntoView({ behavior: "instant", block: "center" })`
    Change `block: "center"` to `block: "start"` so today's row appears at the top of the visible area. Keep `behavior: "instant"` and keep the surrounding `useEffect(() => { ... }, [])` shape and the `document.querySelector('[data-today="true"]')` lookup. Do NOT change the dependency array. Do NOT add a setTimeout or RAF; the effect must remain synchronous on mount as today.

    Edit C (D-07) — Today `<tr>` scroll-margin compensation. The current today row is:
      `<tr data-date={day.date} data-today={day.isToday ? "true" : undefined} className={day.isToday ? "bg-yellow-50 dark:bg-yellow-950/20" : undefined}>`
    Update the `className` prop so that `scroll-mt-10` is applied on every row (today and non-today alike — `scroll-margin-top` is only consulted when a row is the target of `scrollIntoView`, so applying it unconditionally is cheap and avoids the conditional-class string race when the today row is also styled). Concrete target shape:
      `className={day.isToday ? "bg-yellow-50 dark:bg-yellow-950/20 scroll-mt-10" : "scroll-mt-10"}`
    Both branches of the conditional include `scroll-mt-10`. The yellow highlight branch concatenates it after the existing two color classes, separated by a single space. Do NOT use `clsx` or template strings — match the existing inline conditional style already used in this file. Use class `scroll-mt-10` exactly (= 2.5rem = 40px), which matches the rendered `<thead>` height (px-3 py-2 on a single text-sm row ≈ 36-40px). If during human-verification (Plan 03) the today row appears clipped or floats too low, the executor of a follow-up plan may adjust to `scroll-mt-8` or `scroll-mt-12`; this plan ships with `scroll-mt-10` per D-07/PATTERNS.

    Do NOT modify in this plan:
      - The week label row from Plan 01 — leave the `<tr>` containing `Viikko {getISOWeek(...)}` untouched.
      - The `<thead>` element or its `sticky top-0 z-10 bg-background` classes (D-08 — already correct).
      - Any handler functions, refs, or state.
      - The `colCount` derivation.
      - The mobile notes `<tr>` or the `max-sm:hidden` / `sm:hidden` notes column logic.
      - The `data-date` and `data-today` attributes on the today `<tr>` — only the className is updated.
      - `ExtendPanel` and `ClearPanel` placement (D-09 — they live in `<main>` in dashboard-shell.tsx and need no changes here).
  </action>
  <verify>
    <automated>
      # 1. Build succeeds — catches JSX/className/typeError regressions
      npm run build 2>&1 | tail -40

      # 2. Lint passes
      npm run lint -- src/components/schedule/schedule-table.tsx 2>&1 | tail -20

      # 3. The desktop inner-scroll classes are gone from the wrapper
      ! grep -nE 'sm:overflow-y-auto' src/components/schedule/schedule-table.tsx
      # Expected: exit 0
      ! grep -nE 'sm:h-\[calc\(100svh' src/components/schedule/schedule-table.tsx
      # Expected: exit 0

      # 4. The auto-scroll block argument is now "start"
      grep -nE 'block:\s*"start"' src/components/schedule/schedule-table.tsx
      # Expected: exactly one match

      # 5. The previous "center" argument is gone (filter out comments first to avoid header-prose false positives)
      grep -v '^\s*//' src/components/schedule/schedule-table.tsx | grep -v '^\s*\*' | grep -cE 'block:\s*"center"'
      # Expected: 0

      # 6. The today <tr> className branches both include scroll-mt-10
      grep -nE 'className=\{day\.isToday \? "bg-yellow-50 dark:bg-yellow-950/20 scroll-mt-10" : "scroll-mt-10"\}' src/components/schedule/schedule-table.tsx
      # Expected: exactly one match

      # 7. The sticky thead is still present (D-08 unchanged)
      grep -nE 'className="sticky top-0 z-10 bg-background"' src/components/schedule/schedule-table.tsx
      # Expected: exactly one match

      # 8. The week label row from Plan 01 is still intact (regression check)
      grep -nE 'Viikko \{getISOWeek\(new Date\(day\.date\)\)\}' src/components/schedule/schedule-table.tsx
      # Expected: exactly one match
      grep -nE 'import \{[^}]*getISOWeek[^}]*\} from "date-fns"' src/components/schedule/schedule-table.tsx
      # Expected: exactly one match
    </automated>
  </verify>
  <acceptance_criteria>
    - src/components/schedule/schedule-table.tsx does NOT contain `sm:overflow-y-auto`
    - src/components/schedule/schedule-table.tsx does NOT contain `sm:h-[calc(100svh-8rem)]`
    - src/components/schedule/schedule-table.tsx contains exactly one `scrollIntoView({ behavior: "instant", block: "start" })` call (no `block: "center"` in source, excluding comments)
    - src/components/schedule/schedule-table.tsx contains the literal `className={day.isToday ? "bg-yellow-50 dark:bg-yellow-950/20 scroll-mt-10" : "scroll-mt-10"}`
    - src/components/schedule/schedule-table.tsx still contains the Plan-01 outputs: `import { getISOWeek }` from `"date-fns"` and the `Viikko {getISOWeek(new Date(day.date))}` JSX
    - src/components/schedule/schedule-table.tsx still contains `className="sticky top-0 z-10 bg-background"` on the `<thead>` (D-08 unchanged)
    - `npm run build` exits 0
    - `npm run lint -- src/components/schedule/schedule-table.tsx` exits 0
    - No new ancestor `overflow-hidden` introduced in this plan (verified by reading dashboard-shell.tsx in `read_first`; this plan does not modify that file)
  </acceptance_criteria>
  <done>
    Wrapper `<div>` has no className. The auto-scroll effect uses `block: "start"`. The today `<tr>` carries `scroll-mt-10` on both branches of its className conditional. Build and lint pass. The week label row from Plan 01 is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render (browser DOM) | Layout-only change — no new data crosses any trust boundary in this plan. |
| user scroll input | User scroll position drives DOM behavior; no exposed scroll API to untrusted code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Denial of Service | Auto-scroll `useEffect` runs `document.querySelector` once on mount | accept | Single querySelector + scrollIntoView, no observers, no loops. Cost is constant per mount. |
| T-17-05 | Tampering | `scroll-mt-10` Tailwind class | accept | Static class string compiled by Tailwind at build; no dynamic class injection from user input. |
| T-17-06 | Information Disclosure | Removal of inner scroll container changes which DOM region scrolls | accept | No new data exposed; layout-only change. Sticky thead continues to behave the same way relative to the user. |
| T-17-SC | Tampering | npm installs | accept | No new packages installed in this plan. No Package Legitimacy Audit required. |
</threat_model>

<verification>
- Build (`npm run build`) succeeds.
- Lint (`npm run lint -- src/components/schedule/schedule-table.tsx`) is clean.
- Grep confirms: no `sm:overflow-y-auto`, no `sm:h-[calc(100svh-8rem)]`, no `block: "center"` (outside comments), exactly one `block: "start"`, exactly one literal today-tr className matching the target, and the Plan 01 week-label artifacts are still present.
- Functional/visual verification is performed in Plan 03 (human-verify checkpoint), not here.
</verification>

<success_criteria>
- UI-02 functionally complete at the component level: inner scroll container removed, full-page scroll active on desktop, sticky thead preserved (D-08), today auto-scrolls to the top with `scroll-mt-10` offset (D-07), supporting panels untouched (D-09).
- All `must_haves.truths` are true as code/render facts; visual confirmation is captured in Plan 03.
- No regression in Plan 01 outputs.
</success_criteria>

<output>
Create `.planning/phases/17-schedule-table-structure/17-02-SUMMARY.md` when done.
</output>
