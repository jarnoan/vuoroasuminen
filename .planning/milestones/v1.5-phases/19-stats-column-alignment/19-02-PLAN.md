---
phase: 19-stats-column-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified: []
autonomous: false
requirements:
  - UI-06

must_haves:
  truths:
    - "At 360px viewport, each child column in StatsPanel aligns vertically with the same child column in ScheduleTable"
    - "At 1280px viewport, each child column in StatsPanel aligns vertically with the same child column in ScheduleTable"
    - "The alignment is visually verified by the operator with both elements on screen simultaneously"
  artifacts:
    - path: ".planning/phases/19-stats-column-alignment/19-02-SUMMARY.md"
      provides: "Human verification record for SC-1..SC-3"
      contains: "approved"
  key_links:
    - from: "ScheduleTable child column visual edges"
      to: "StatsPanel child column visual edges"
      via: "shared min-w-[72px] sm:min-w-[90px] + tableLayout:fixed + shared parent width"
      pattern: "visual column-edge alignment"
---

<objective>
Human-verify the three success criteria for Phase 19 (UI-06) by inspecting the running app at both mobile (360px) and desktop (1280px) viewports.

Purpose: The change in 19-01 is CSS-only — automated checks confirm the classes are present in source, but only a human can confirm the columns visually line up across the boundary between the two `<table>` elements at each viewport.

Output: Operator confirmation that SC-1, SC-2, and SC-3 from ROADMAP Phase 19 all pass, recorded in 19-02-SUMMARY.md.
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-stats-column-alignment/19-UI-SPEC.md
@.planning/phases/19-stats-column-alignment/19-01-SUMMARY.md
@src/components/schedule/stats-panel.tsx
@src/components/schedule/schedule-table.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify SC-1..SC-3 at 360px and 1280px viewports</name>
  <files></files>
  <read_first>
    - .planning/phases/19-stats-column-alignment/19-UI-SPEC.md (Success Criteria Verification section)
    - .planning/phases/19-stats-column-alignment/19-01-SUMMARY.md (what was actually changed in 19-01)
  </read_first>
  <action>
    Pause execution and present the verification steps in `<how-to-verify>` to the operator. Do not proceed until the operator confirms with "approved" or reports specific misalignments. If the dev server is not already running, start it with `npm run dev` and surface the URL to the operator. Then write `.planning/phases/19-stats-column-alignment/19-02-SUMMARY.md` capturing the operator's pass/fail outcome per success criterion at each viewport.
  </action>
  <what-built>
    Plan 19-01 modified `src/components/schedule/stats-panel.tsx`:
    - Added `tableLayout: "fixed"` inline style on the `<table>`.
    - Added a `<colgroup>` containing one label col plus one col per child.
    - Applied `min-w-[72px] sm:min-w-[90px]` to every child column `<th>` and `<td>` (exact replication of ScheduleTable child column widths).
    - Changed label column padding from `pr-2` to `px-3` (matching ScheduleTable date column horizontal padding).
    - Changed child cell padding from `px-2` to `px-1` (matching ScheduleTable child cell padding).
    - Replaced wrapper `p-3` with `py-3` so StatsPanel table edges align with ScheduleTable table edges inside their shared parent.
  </what-built>
  <how-to-verify>
    1. Start the dev server if not already running: `npm run dev` (default port 3000).
    2. Sign in as a parent who has access to a populated schedule. If no data is present, advance the view window and click a few cells so that the schedule shows at least one full week with child cells assigned to each parent.
    3. Open Chrome DevTools, enable device toolbar, and set viewport to **360 x 640** (iPhone SE size or custom). Reload the page if needed.
    4. Scroll until both the ScheduleTable and the StatsPanel are visible on screen at the same time. (StatsPanel sits directly below the table — depending on view window size, you may need to scroll to a point where the bottom of the table and the StatsPanel are both visible.)
    5. **SC-1 + SC-3 (mobile):** Draw an imaginary vertical line down from the left edge of each child column header in ScheduleTable. Confirm the left edge of the matching child column in StatsPanel is on the same vertical line. Repeat for the right edge of each child column. Repeat for every child column (there are typically 2 or 3).
       - PASS criterion: column edges are within roughly 1-2px of perfect alignment for every child column at 360px.
       - FAIL criterion: any child column is visibly offset (more than ~3px) from its corresponding column above.
    6. Switch viewport to **1280 x 800** (standard desktop) and reload.
    7. **SC-1 + SC-3 (desktop):** Repeat the imaginary-line check at desktop width. Confirm child columns still align.
    8. **SC-2:** Confirm that at both viewports, the alignment is visible WITHOUT scrolling between the two elements — i.e. both can be on screen simultaneously, and the alignment is apparent at a glance.
    9. Also sanity-check that nothing was visually broken by the change:
       - The "Vapaa" rows (Isä X pv (Y vkl), Äiti X pv (Y vkl)) still span the full table width.
       - The horizontal separator line between the parent-day rows and the Vapaa rows still spans the full width.
       - Father labels are still blue, mother labels still rose, "yksin" sub-values are still muted.
       - The "pv" and "yksin X" text is not clipped or wrapped awkwardly inside the now-narrower (px-1) child cells. If text overflows at 360px, note it as an issue.
    10. If anything fails, describe the failure (which viewport, which column, what is misaligned) so it can be addressed in a follow-up plan or revision of 19-01.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms SC-1, SC-2, SC-3 all PASS at both 360px and 1280px viewports per the steps in `<how-to-verify>`. Operator response is recorded verbatim in 19-02-SUMMARY.md.</human-check>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: At 360px viewport, every child column in StatsPanel aligns to its corresponding child column in ScheduleTable within ~2px on both left and right edges.
    - Behavior assertion: At 1280px viewport, every child column in StatsPanel aligns to its corresponding child column in ScheduleTable within ~2px on both left and right edges.
    - Behavior assertion: Both ScheduleTable and StatsPanel can be on screen simultaneously at both viewports.
    - Behavior assertion: No regression in StatsPanel rendering — Vapaa rows still span full width, separator line is intact, colors and copy unchanged, "X pv" / "yksin Y" values are not clipped at 360px.
    - Operator confirmation: 19-02-SUMMARY.md records explicit pass/fail for SC-1, SC-2, SC-3.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all three success criteria pass at both viewports. Otherwise describe the misalignment (viewport, column index, observed offset) so 19-01 can be revised.</resume-signal>
  <done>
    Operator has confirmed SC-1, SC-2, and SC-3 all pass at both 360px and 1280px viewports. 19-02-SUMMARY.md captures the verification outcome.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Verification-only plan — no code or config changes; no new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19V-01 | Information Disclosure | Verification screenshots / notes | accept | Operator performs verification locally; no screenshots posted externally. Verification notes contain no PII (only column alignment outcomes). |
| T-19V-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- 19-02-SUMMARY.md exists and contains explicit pass/fail for SC-1, SC-2, SC-3 at both viewport sizes.
- If any SC failed, the SUMMARY links the failure to a remediation plan (revision of 19-01 or a new plan).
</verification>

<success_criteria>
- Operator confirms visual alignment of StatsPanel child columns with ScheduleTable child columns at 360px viewport.
- Operator confirms visual alignment at 1280px viewport.
- No visual regressions noted in StatsPanel (Vapaa rows, separator, colors, copy, text clipping).
</success_criteria>

<output>
Create `.planning/phases/19-stats-column-alignment/19-02-SUMMARY.md` when done.
</output>
