---
phase: 16-schedule-table-reflow-and-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/schedule/schedule-table.tsx
  - src/components/schedule/schedule-with-realtime.tsx
autonomous: true
requirements:
  - MOB-01
  - MOB-01b
  - MOB-05

must_haves:
  truths:
    - "D-01: On mobile (below sm:), the schedule table fits within a 360px viewport without horizontal scrolling"
    - "D-06: D-08: On mobile, the notes column header and the per-row notes cell in the main day row are hidden via max-sm:hidden"
    - "D-09: On mobile, when day.notes is non-empty, a second <tr> renders below the main day row with a single <td colSpan={colCount}> wrapping NotesCell"
    - "D-10: D-11: On mobile, when day.notes is empty, a PlusIcon button in the main row reveals a focusable NotesCell when tapped (per-row notesOpen state); the second-row NotesCell is fully editable — same component, same tap-to-edit behavior"
    - "D-12: On desktop (sm: and above), the layout is unchanged — one <tr> per day with notes as the final <td> in the same row"
    - "D-03: On desktop, the inner scroll container uses sm:overflow-y-auto sm:h-[calc(100svh-8rem)] (was overflow-y-auto h-[calc(100vh-8rem)])"
    - "D-02: On mobile, no height constraint or overflow control is applied to the scroll container — the page scrolls naturally; ViewToolbar is not sticky on mobile"
    - "D-05: The renderAbove prop is removed from ScheduleTable's interface, destructuring, and JSX call site"
    - "D-13: In ScheduleWithRealtime, StatsPanel is rendered as a sibling AFTER <ScheduleTable /> inside <RealtimeProvider>, not via renderAbove"
    - "D-07: Child custody columns use min-w-[72px] sm:min-w-[90px] (narrower on mobile, original on desktop)"
    - "D-04: The Päivä (date) <th> and per-row date <td> become sticky with position:sticky left-0 z-10 bg-background to anchor as a frozen first column; no overflow-hidden ancestor present on thead"
  artifacts:
    - path: "src/components/schedule/schedule-table.tsx"
      provides: "Mobile-reflowed schedule table: hidden notes column on mobile, conditional second-row notes, add-notes affordance, responsive scroll container, sticky date column"
      exports: ["ScheduleTable"]
      contains: "max-sm:hidden"
    - path: "src/components/schedule/schedule-with-realtime.tsx"
      provides: "StatsPanel moved outside ScheduleTable; rendered as sibling after ScheduleTable inside RealtimeProvider"
      exports: ["ScheduleWithRealtime"]
  key_links:
    - from: "ScheduleTable scroll container"
      to: "page scroll on mobile / contained scroll on desktop"
      via: "sm: prefix on overflow-y-auto and h-[calc(100svh-8rem)]"
      pattern: "sm:overflow-y-auto sm:h-\\[calc\\(100svh-8rem\\)\\]"
    - from: "mobile notes second-row <tr>"
      to: "NotesCell editor"
      via: "conditional render when day.notes !== '' || notesOpenDates.has(day.date)"
      pattern: "max-sm:table-row sm:hidden"
    - from: "add-notes PlusIcon button"
      to: "notesOpenDates state"
      via: "onClick sets a Set<string> entry to reveal NotesCell"
      pattern: "setNotesOpenDates"
    - from: "ScheduleWithRealtime"
      to: "StatsPanel"
      via: "JSX sibling after <ScheduleTable /> within <RealtimeProvider>"
      pattern: "<StatsPanel days=\\{days\\} parents=\\{parents\\} />"
---

<objective>
Restructure `ScheduleTable` to fit 360–430px viewports without horizontal scrolling (MOB-01), render notes on a second row below each day on mobile while keeping the desktop single-row layout unchanged (MOB-01b), and move `StatsPanel` out of `ScheduleTable` to a sibling position inside `ScheduleWithRealtime` so the stats panel sits below the table on all viewports (MOB-05 positioning).

Purpose: Single atomic refactor across two files because the `renderAbove` prop is the contract between them — splitting the work would introduce a transient TypeScript error (Pitfall 4 in RESEARCH.md). All visible mobile reflow work lives here; the stats grid redesign is in Plan 01 (parallel, disjoint file set).

Output:
- `src/components/schedule/schedule-table.tsx`: scroll container scoped to `sm:` (D-01, D-03), iOS sticky thead check, notes column hidden on mobile, sticky date column, narrower child columns on mobile, two-row notes layout with PlusIcon "add notes" affordance, `renderAbove` prop fully removed.
- `src/components/schedule/schedule-with-realtime.tsx`: `<StatsPanel>` rendered as a sibling after `<ScheduleTable>` inside `<RealtimeProvider>`; `renderAbove={...}` prop removed from the `<ScheduleTable>` call.
</objective>

<execution_context>
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarno/src/vuoroasuminen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-schedule-table-reflow-and-stats/16-CONTEXT.md
@.planning/phases/16-schedule-table-reflow-and-stats/16-RESEARCH.md
@.planning/phases/16-schedule-table-reflow-and-stats/16-PATTERNS.md
@src/components/schedule/schedule-table.tsx
@src/components/schedule/schedule-with-realtime.tsx
@src/components/schedule/notes-cell.tsx
@src/components/schedule/clear-panel.tsx
@src/components/schedule/view-toolbar.tsx
@src/components/schedule/schedule-cell.tsx

<interfaces>
<!-- Contracts the executor must respect. Extracted from the codebase. -->

From src/components/schedule/schedule-table.tsx (existing):
```typescript
interface ScheduleTableProps {
  days: ScheduleDay[]
  setDays: React.Dispatch<React.SetStateAction<ScheduleDay[]>>
  realtimeRef?: React.RefObject<((entry: RealtimeEntry) => void) | null>
  publishRef?: React.RefObject<(() => void) | null>
  renderAbove?: (days: ScheduleDay[]) => React.ReactNode   // ← REMOVE in this plan
  parents: Array<{ id: ParentId; name: string }>
}

// Existing variables inside the component:
const childNames = days[0]?.cells.map((c) => c.childName) ?? []
const colCount = childNames.length + 2  // Date + children + Notes — reuse as-is for colSpan
async function handleNoteSave(entryId: string, notes: string)   // already exists; reuse
```

From src/components/schedule/notes-cell.tsx (consumer contract):
```typescript
interface NotesCellProps {
  entryId: string | null
  value: string
  onSave: (entryId: string, notes: string) => void
}
// NotesCell is an <input type="text"> that auto-saves on blur. No ref needed for focus
// because rendering it via conditional state will mount it; native autoFocus is NOT used,
// so the executor MAY add autoFocus to the second-row NotesCell when revealed via the
// "add notes" button (Claude's discretion per D-11) — see <action> below.
```

From src/components/schedule/clear-panel.tsx (analog for max-sm:hidden / sm:hidden CSS toggle):
```tsx
<input type="date" className="sm:hidden ..." />            // mobile only
<Button className="font-semibold hidden sm:flex" />        // desktop only
```

From src/components/schedule/view-toolbar.tsx (lucide-react icon import pattern):
```typescript
import { CalendarIcon, ChevronLeft } from "lucide-react"
```
For this plan: `import { PlusIcon } from "lucide-react"` (added to schedule-table.tsx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scroll container + column widths + sticky date column + notes column hidden on mobile</name>
  <files>src/components/schedule/schedule-table.tsx</files>
  <read_first>
    - src/components/schedule/schedule-table.tsx (current full file; focus on lines 218-300 — the JSX return block)
    - src/components/schedule/clear-panel.tsx (lines 97-129 — analog for max-sm:hidden / sm:hidden CSS toggle)
    - src/components/schedule/view-toolbar.tsx (lines 1-30 — lucide-react import pattern; reuse same import style)
    - src/components/schedule/schedule-cell.tsx (existing use of `max-sm:` and `style={{ touchAction: "manipulation" }}` patterns)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-CONTEXT.md (D-01 through D-08)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-RESEARCH.md (Pattern 2, Pattern 3, Pitfall 2, Pitfall 5)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-PATTERNS.md (Change 1 and Change 2 sections — exact before/after snippets for lines 221, 229-235, 236-238, 286-291)
  </read_first>
  <action>
    Apply four changes to the JSX in `src/components/schedule/schedule-table.tsx`. Do not modify the handlers, refs, or `useEffect` blocks; only the JSX from line 218 down. Reuse the existing `colCount = childNames.length + 2` variable as-is (per RESEARCH.md Pitfall 1, the hidden notes column does not affect colSpan layout).

    Change A — Scroll container (D-01, D-03):
    Replace the scroll wrapper `<div className="overflow-y-auto h-[calc(100vh-8rem)]">` (currently line 221) with `<div className="sm:overflow-y-auto sm:h-[calc(100svh-8rem)]">`. Both prefixes are required together (Pitfall 5).

    Change B — Date column sticky (D-07 part 2):
    The `<th>` for "Päivä" (currently line 225) must add sticky positioning. Final className: `px-3 py-2 text-left text-sm font-semibold whitespace-nowrap border-b sticky left-0 z-10 bg-background`. Apply the same sticky pattern to the per-row date `<td>` (currently line 258) — final className: `px-3 py-2 text-sm whitespace-nowrap font-mono sticky left-0 bg-background`. The date `<td>` on the today row must keep its yellow highlight: do NOT add `bg-background` to the row's `<tr>` (it already gets `bg-yellow-50 dark:bg-yellow-950/20` from the existing isToday branch). For the today row the sticky date cell's `bg-background` would override the yellow — use Tailwind's group-aware approach: add `bg-background data-[today=true]:bg-yellow-50 dark:data-[today=true]:bg-yellow-950/20` on the `<td>` AND set `data-today={day.isToday ? "true" : undefined}` on the date `<td>` (the attribute is already on the `<tr>`; replicate it on the `<td>` so the selector hits). If a simpler equivalent works (Claude's discretion), accept it as long as the today-row date cell still appears yellow.

    Change C — Child column widths (D-07 part 1):
    For the per-child header `<th>` map (currently lines 228-235), change `min-w-[90px]` to `min-w-[72px] sm:min-w-[90px]`. Final className for the `<th>`: `px-1 py-2 text-left text-sm font-semibold border-b min-w-[72px] sm:min-w-[90px]`.

    Change D — Notes column hidden on mobile (D-06, D-08):
    - Notes `<th>` (currently line 236-238): add `max-sm:hidden`. Final className: `px-1 py-2 text-left text-sm font-semibold border-b min-w-[160px] max-sm:hidden`.
    - Notes `<td>` in the main day `<tr>` (currently lines 286-292): add `max-sm:hidden`. Final className: `px-1 py-1 max-sm:hidden`. Leave the inner `<NotesCell>` element and its props unchanged.

    Do not change `colCount`. Do not add `overflow-x-auto`. Do not wrap the `<table>` in another scroll container. Do not modify the week separator `<tr>` (it already uses `colSpan={colCount}` correctly). Do not touch any handler.

    iOS sticky thead sanity check (D-04): verify by reading the file that no ancestor of `thead` has `overflow-hidden`. The only overflow control is `sm:overflow-y-auto` on the scroll container (not `overflow-hidden`). No change needed; if `overflow-hidden` is discovered on any ancestor in this file, replace with `overflow-clip`. Record the result in the task's commit body as `iOS sticky check: clean`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'sm:overflow-y-auto sm:h-\[calc(100svh-8rem)\]' | awk '{ if ($1 != 1) { print "FAIL: expected exactly one sm:overflow-y-auto sm:h-[calc(100svh-8rem)] occurrence, got "$1; exit 1 } else print "OK1" }' && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'min-w-\[72px\] sm:min-w-\[90px\]' | awk '{ if ($1 < 1) { print "FAIL: expected child column responsive min-w"; exit 1 } else print "OK2" }' && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -cE 'max-sm:hidden' | awk '{ if ($1 < 2) { print "FAIL: expected >=2 max-sm:hidden occurrences (notes th + notes td)"; exit 1 } else print "OK3" }' && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'overflow-hidden' | awk '{ if ($1 != 0) { print "FAIL: overflow-hidden present, must be overflow-clip"; exit 1 } else print "OK4" }' && ! grep -nE 'h-\[calc\(100vh-8rem\)\]' src/components/schedule/schedule-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/schedule/schedule-table.tsx` contains the literal string `sm:overflow-y-auto sm:h-[calc(100svh-8rem)]` exactly once
    - `src/components/schedule/schedule-table.tsx` does NOT contain the literal string `h-[calc(100vh-8rem)]` (old non-svh value gone) and does NOT contain `overflow-y-auto h-[calc` (unprefixed form gone)
    - `src/components/schedule/schedule-table.tsx` contains `min-w-[72px] sm:min-w-[90px]` in the per-child `<th>` block
    - `src/components/schedule/schedule-table.tsx` does NOT contain `overflow-hidden` anywhere
    - `src/components/schedule/schedule-table.tsx` contains `max-sm:hidden` at least twice (on the notes `<th>` and on the notes `<td>` in the main row)
    - The Päivä `<th>` className contains the substring `sticky left-0 z-10 bg-background`
    - The per-row date `<td>` is sticky (className contains `sticky left-0`) and still renders yellow on the today row (verified by manual inspection — but `data-today` attribute or equivalent class condition is present on the `<td>`)
    - `colCount` variable definition unchanged (still `childNames.length + 2`)
    - `npx tsc --noEmit` exits 0
    - The week separator `<tr>` continues to use `colSpan={colCount}` (no change)
  </acceptance_criteria>
  <done>Mobile scroll container, sticky date column, responsive child widths, and hidden notes column are all in place; desktop unaffected; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Two-row notes layout on mobile with PlusIcon add-notes affordance</name>
  <files>src/components/schedule/schedule-table.tsx</files>
  <read_first>
    - src/components/schedule/schedule-table.tsx (current state — Task 1 is already applied; re-read to see post-Task-1 layout especially the React.Fragment block lines 242-294)
    - src/components/schedule/notes-cell.tsx (full file — NotesCell signature, blur-save behavior, no autoFocus)
    - src/components/schedule/schedule-cell.tsx (existing `style={{ touchAction: "manipulation" }}` pattern at line 100)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-CONTEXT.md (D-09, D-10, D-11, D-12; <specifics> for confirmed UX)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-RESEARCH.md (Pattern 7, Pitfall 3, Pitfall 6, Open Question 1 — per-row notesOpen Set<string> state is the recommended approach)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-PATTERNS.md (Change 3 — exact target snippets for second-row `<tr>` and add-notes `<td>`)
  </read_first>
  <action>
    Add the mobile-only second-row notes layout and the "add notes" PlusIcon affordance to `src/components/schedule/schedule-table.tsx`. All changes apply to the existing `React.Fragment key={day.date}` block inside the `days.map(...)` body (currently lines 242-294 in the pre-Task-1 numbering — line numbers may shift after Task 1).

    Add import:
    `import { PlusIcon } from "lucide-react"` — add to the existing import section near the top of the file. If the file does not already import from `lucide-react`, this is a new import line.

    Add `useState` to the existing `useState` imports from `react` (the file currently imports `React, { useEffect, useCallback }` — change to `React, { useEffect, useCallback, useState }` ONLY if `useState` is not already imported; verify before editing).

    Add per-row open-state inside the `ScheduleTable` component body, near the other top-level hooks (above `handleToggle` / after the existing `useEffect` hooks):
    ```
    const [notesOpenDates, setNotesOpenDates] = useState<Set<string>>(new Set())
    ```

    Inside the existing `<React.Fragment key={day.date}>` block, after the main day `<tr>` (the one closing with `</tr>` around line 293 of the original file), insert a conditional second-row `<tr>` BEFORE the closing `</React.Fragment>`. Conditions and structure:

    - Render the second `<tr>` only when `(day.notes !== "" && day.notes !== null && day.notes !== undefined) || notesOpenDates.has(day.date)`. Use a simple truthy check `day.notes || notesOpenDates.has(day.date)` if the schedule-day type treats empty as `""`; consult the existing usage of `day.notes` elsewhere in this file (line 289 `value={day.notes}`) to confirm the type.
    - The `<tr>` className: `max-sm:table-row sm:hidden`.
    - Inside the `<tr>`, render exactly one `<td colSpan={colCount} className="px-1 py-1 pb-1">` containing a `<NotesCell>` with the same props as the main-row NotesCell: `entryId={day.notesEntryId}`, `value={day.notes}`. For `onSave`, wrap `handleNoteSave` so that after a save where the new notes value is empty (`!notes`), the row is collapsed by removing `day.date` from `notesOpenDates`:
      ```
      onSave={(entryId, notes) => {
        handleNoteSave(entryId, notes)
        if (!notes) {
          setNotesOpenDates(prev => {
            const next = new Set(prev)
            next.delete(day.date)
            return next
          })
        }
      }}
      ```

    Add the "add notes" affordance inside the main day `<tr>` (D-11). Insert a new `<td>` AFTER the last per-child `<td>` (i.e., after the `day.cells.map` block) and BEFORE the existing notes `<td>` (now `max-sm:hidden`). The new `<td>` className: `px-1 py-1 max-sm:table-cell sm:hidden`. Its children render the PlusIcon button only when `!day.notes && !notesOpenDates.has(day.date)`; otherwise render an empty cell (so the row keeps the same number of mobile-visible columns regardless). The button must be:
    ```
    <button
      type="button"
      onClick={() => setNotesOpenDates(prev => new Set(prev).add(day.date))}
      aria-label={`Lisää muistiinpano — ${day.dayLabel}`}
      className="text-muted-foreground hover:text-foreground"
      style={{ touchAction: "manipulation" }}
    >
      <PlusIcon className="h-4 w-4" />
    </button>
    ```

    Preserve the existing `data-today` attribute on the main `<tr>` only — do NOT add `data-today` to the new notes `<tr>`. The `scrollIntoView` selector targets the main row and must continue to do so (Pitfall 6). Keep the week separator `<tr>` rendering position unchanged (it runs BEFORE the main `<tr>` inside the Fragment — Pitfall 3).

    Discretion notes:
    - Icon choice: `PlusIcon` per the RESEARCH/PATTERNS recommendation. Do not switch to `PencilIcon`.
    - Position of the add-notes `<td>`: after the last child cell, before the desktop notes `<td>`. This keeps the mobile-visible column count consistent.
    - `touch-action: manipulation` is added to the add-notes button per the existing pattern in `schedule-cell.tsx` (D's discretion item). Do NOT add it to the NotesCell `<input>` (native inputs handle the 300ms tap delay).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'PlusIcon' | awk '{ if ($1 < 2) { print "FAIL: expected PlusIcon import + usage"; exit 1 } else print "OK1" }' && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'max-sm:table-row sm:hidden' | awk '{ if ($1 != 1) { print "FAIL: expected exactly one second-row <tr> class"; exit 1 } else print "OK2" }' && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'notesOpenDates' | awk '{ if ($1 < 4) { print "FAIL: expected notesOpenDates declaration + has + add + delete = at least 4 references"; exit 1 } else print "OK3" }' && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'max-sm:table-cell sm:hidden' | awk '{ if ($1 < 1) { print "FAIL: expected add-notes <td> mobile-only class"; exit 1 } else print "OK4" }' && grep -v '^[[:space:]]*//' src/components/schedule/schedule-table.tsx | grep -c 'colSpan={colCount}' | awk '{ if ($1 < 2) { print "FAIL: expected colSpan={colCount} on week separator AND second-row notes"; exit 1 } else print "OK5" }' && npx vitest run src/lib/schedule/__tests__/stats.test.ts src/lib/schedule/__tests__/generate-default.test.ts src/actions/schedule.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/schedule/schedule-table.tsx` imports `PlusIcon` from `lucide-react`
    - `src/components/schedule/schedule-table.tsx` declares `const [notesOpenDates, setNotesOpenDates] = useState<Set<string>>(new Set())`
    - `src/components/schedule/schedule-table.tsx` contains exactly one occurrence of `max-sm:table-row sm:hidden` (the second-row notes `<tr>` className)
    - `src/components/schedule/schedule-table.tsx` contains `colSpan={colCount}` at least twice (week separator + second-row notes `<td>`)
    - `src/components/schedule/schedule-table.tsx` contains `max-sm:table-cell sm:hidden` at least once (add-notes `<td>` wrapper)
    - `src/components/schedule/schedule-table.tsx` contains `setNotesOpenDates(prev => new Set(prev).add(day.date))` (or equivalent identifier substring `setNotesOpenDates` followed by `.add(day.date)`)
    - `src/components/schedule/schedule-table.tsx` contains `next.delete(day.date)` (collapse-on-empty-save behavior)
    - `src/components/schedule/schedule-table.tsx` contains `style={{ touchAction: "manipulation" }}` on the add-notes button
    - `data-today="true"` is set on the main `<tr>` only (verify by grep — exactly one `data-today` attribute in the file)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` for stats + generate-default + schedule action tests exits 0 (no regressions in adjacent test files)
  </acceptance_criteria>
  <done>Mobile second-row notes layout, add-notes PlusIcon, and per-row open-state are in place; desktop unchanged; typecheck and adjacent tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Remove renderAbove and move StatsPanel to sibling position in ScheduleWithRealtime</name>
  <files>src/components/schedule/schedule-table.tsx, src/components/schedule/schedule-with-realtime.tsx</files>
  <read_first>
    - src/components/schedule/schedule-table.tsx (post-Task-2 state — lines 19-28 prop interface; line 220 `{renderAbove?.(days)}` call site)
    - src/components/schedule/schedule-with-realtime.tsx (full file; lines 37-48 are the JSX return block to restructure)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-CONTEXT.md (D-05, D-13)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-RESEARCH.md (Pitfall 4 — must update both files atomically)
    - .planning/phases/16-schedule-table-reflow-and-stats/16-PATTERNS.md (Change 4 in schedule-table.tsx section; schedule-with-realtime.tsx section)
  </read_first>
  <action>
    Atomic two-file refactor that removes the `renderAbove` prop and moves `StatsPanel` to a sibling position. This MUST be performed in one task to avoid a transient TypeScript error (Pitfall 4).

    In `src/components/schedule/schedule-table.tsx`:
    1. Remove the `renderAbove?: (days: ScheduleDay[]) => React.ReactNode` line from the `ScheduleTableProps` interface (currently line 24).
    2. Remove `renderAbove` from the destructured props in the `export function ScheduleTable(...)` signature (currently line 28). After: `export function ScheduleTable({ days, setDays, realtimeRef, publishRef, parents }: ScheduleTableProps)`.
    3. Remove the JSX call `{renderAbove?.(days)}` (currently line 220). The fragment `<>` still opens; the next child is now the scroll container `<div>`. Decide whether to keep the fragment `<>...</>` wrapper or drop it (Claude's discretion — if dropping, return the `<div>` directly).

    In `src/components/schedule/schedule-with-realtime.tsx`:
    1. Inside the `<RealtimeProvider>` JSX (currently lines 38-47), remove the `renderAbove={(days) => <StatsPanel days={days} parents={parents} />}` prop from the `<ScheduleTable ... />` call.
    2. Add `<StatsPanel days={days} parents={parents} />` as a sibling JSX element directly AFTER `</ScheduleTable>` (or `<ScheduleTable .../>` self-close) and BEFORE `</RealtimeProvider>`. Keep the existing import `import { StatsPanel } from "./stats-panel"` (already on line 5 — do not remove it).

    Final shape (schedule-with-realtime.tsx return block):
    ```
    return (
      <RealtimeProvider onEntryChange={handleEntryChange} onRefresh={handleRefresh} viewStart={viewStart}>
        <ScheduleTable
          days={days}
          setDays={setDays}
          realtimeRef={realtimeRef}
          publishRef={publishRef}
          parents={parents}
        />
        <StatsPanel days={days} parents={parents} />
      </RealtimeProvider>
    )
    ```

    Do not modify `RealtimeProvider`. Do not change the `StatsPanel` import path. Do not move `StatsPanel` outside `RealtimeProvider` (it must stay a child of the provider so future realtime hooks remain available; though it currently does not consume the context, keeping it inside is consistent with the existing tree).
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -nE 'renderAbove' src/components/schedule/schedule-table.tsx && ! grep -nE 'renderAbove' src/components/schedule/schedule-with-realtime.tsx && grep -c '<StatsPanel days={days} parents={parents} />' src/components/schedule/schedule-with-realtime.tsx | awk '{ if ($1 != 1) { print "FAIL: expected exactly one StatsPanel sibling render"; exit 1 } else print "OK" }' && npx vitest run && (npx eslint src/components/schedule/schedule-table.tsx src/components/schedule/schedule-with-realtime.tsx src/components/schedule/stats-panel.tsx || echo "Lint warnings reviewed")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'renderAbove' src/components/schedule/schedule-table.tsx` returns nothing (prop, destructure, and call site all removed)
    - `grep -n 'renderAbove' src/components/schedule/schedule-with-realtime.tsx` returns nothing
    - `src/components/schedule/schedule-with-realtime.tsx` contains exactly one `<StatsPanel days={days} parents={parents} />` JSX element
    - The `StatsPanel` JSX appears AFTER `<ScheduleTable ... />` in the source (verifiable by line-number ordering)
    - `import { StatsPanel } from "./stats-panel"` remains in `src/components/schedule/schedule-with-realtime.tsx`
    - `npx tsc --noEmit` exits 0 (no transient TS errors — both files updated together)
    - `npx vitest run` (full suite) exits 0
    - The `ScheduleTableProps` interface in `schedule-table.tsx` no longer mentions `renderAbove` (verified by grep)
    - `dashboard-shell.tsx` is NOT modified (verify with `git diff --name-only src/components/schedule/dashboard-shell.tsx` returns empty)
  </acceptance_criteria>
  <done>renderAbove fully removed from both files; StatsPanel renders as sibling after ScheduleTable inside RealtimeProvider; typecheck and full test suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DOM | Pure render restructure; same handlers and same Server Action calls (`toggleCell`, `saveNotes`, `clearCell`) as before |
| client → state | New `notesOpenDates: Set<string>` is client-local UI state; no server impact; no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | NotesCell second-row instance | accept | The second-row NotesCell calls the same `handleNoteSave` → `saveNotes` Server Action that the desktop NotesCell already uses. No new input surface; same Zod validation on the action input applies. |
| T-16-04 | Denial of Service | notesOpenDates Set growth | accept | Set keys are user-visible day dates within the schedule window (~84 days). Memory is bounded; no DoS surface. |
| T-16-05 | Information Disclosure | data-today exposure on date <td> | accept | The `data-today` attribute is already present on the main `<tr>` and is observable in DOM today. Replicating onto the date `<td>` for sticky-cell highlight does not increase exposure. |

Phase 16 is rendering-only — no new server actions, no schema changes, no auth/session changes, no new packages. ASVS L1 categories V2 (Authentication), V3 (Session Management), V4 (Access Control), V6 (Cryptography) are not applicable. V5 (Input Validation) is unchanged because `NotesCell` and the `saveNotes` Server Action are not modified in this plan.

No `[ASSUMED]` or `[SUS]` packages are introduced — no `npm install` runs in this plan. Package Legitimacy Gate is N/A.
</threat_model>

<verification>
- `npx tsc --noEmit` passes — both files compile together; no transient `renderAbove` errors
- `npx vitest run` (full suite) passes — no regressions in stats / schedule action / dashboard-page tests
- `grep -n 'renderAbove' src/components/schedule/` returns nothing
- Manual mobile DevTools check (360px wide): table has no horizontal scroll; notes column hidden; second-row notes appears when day has notes
- Manual mobile DevTools check: tapping the PlusIcon on a day with no notes reveals a focusable NotesCell on the second row
- Manual desktop check (≥ 640px wide): single-row layout unchanged; notes column visible; StatsPanel renders below the table; sticky thead still works inside the scroll container
- iOS sticky thead sanity: no `overflow-hidden` ancestor present (verified by grep)
</verification>

<success_criteria>
- Mobile (below `sm:`) view fits within 360px viewport without horizontal scrolling — verified visually in DevTools
- Mobile shows the notes text on a second row directly below each day's main row when notes exist; PlusIcon appears in the main row when notes are empty
- Desktop (`sm:` and above) layout is byte-identical to pre-Phase-16 behavior except for the unprefixed-to-`sm:` migration on the scroll container (which is functionally equivalent on desktop)
- `StatsPanel` renders below `ScheduleTable` on all viewports — escaped from inside the scroll container
- `renderAbove` prop is completely removed from both files; no dead code remains
- All tests in `npx vitest run` pass
</success_criteria>

<output>
Create `.planning/phases/16-schedule-table-reflow-and-stats/16-02-SUMMARY.md` when done
</output>
