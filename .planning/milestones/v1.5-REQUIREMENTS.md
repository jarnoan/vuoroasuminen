# Requirements Archive: v1.5 UI Refinements

**Archived:** 2026-05-25
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Vuoroasuminen

**Defined:** 2026-05-20
**Core Value:** Both parents always see the same up-to-date custody schedule, reflected in their Google Calendars, without manual coordination.

## v1.5 Requirements

Requirements for v1.5 UI Refinements. All changes are rendering-layer only — no schema changes, no new Server Actions.

### UI (Schedule Display + Interaction)

- [x] **UI-01**: User sees a week number label row above each Monday in the schedule table (e.g. "Viikko 21")
- [x] **UI-02**: On desktop, the entire page scrolls — the schedule table has no separate scroll container; the sticky `<thead>` behaves the same as on mobile
- [x] **UI-03**: On mobile, the note affordance button shows a pen icon instead of a plus icon
- [x] **UI-04**: On mobile, long-pressing a parent cell arms a "Tyhjennä" state: the cell transforms into a full-cell "Tyhjennä" button (replacing the corner-X behavior); the device vibrates via `navigator.vibrate()` when armed
- [x] **UI-05**: On mobile, the note row is visually connected to its day row — the design makes clear the note belongs to the day above it
- [x] **UI-06**: Stats panel child columns are aligned with the same child columns in the schedule table above (same widths)

## Future Requirements

### Audit Log

- **AUDT-01**: User can see who last changed a cell and when (per-cell change history)
- **AUDT-02**: Change history is accessible without disrupting the schedule view

## Out of Scope

| Feature | Reason |
|---------|--------|
| Per-cell change history | Deferred to future milestone (AUDT-01, AUDT-02) |
| Week number shown per row (not per week) | User confirmed: one row per week boundary, above Monday |
| Full-page scroll with no sticky header | Sticky `<thead>` is required; only the separate scroll container is removed |
| Custom vibration pattern | `navigator.vibrate(100)` single pulse is sufficient; patterns add complexity |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| UI-01 | Phase 17 | Complete |
| UI-02 | Phase 17 | Complete |
| UI-03 | Phase 18 | Complete |
| UI-04 | Phase 18 | Complete |
| UI-05 | Phase 18 | Complete |
| UI-06 | Phase 19 | Complete |

**Coverage:**
- v1.5 requirements: 6 total
- Mapped to phases: 6
- Unmapped: 0 ✓

---
*Requirements defined: 2026-05-20*
*Last updated: 2026-05-20 after initial definition*
